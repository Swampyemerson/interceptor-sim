# Progress — milestone roll-up

> **Canonical live state → [`project_state.json`](project_state.json)** (view: `dashboard.html`). This milestone roll-up is a subordinate view of the contract.

| Milestone | What it proves | Gate | Status |
|---|---|---|---|
| M0 Toolchain | PX4 SITL + Gazebo boot headless; MAVSDK arms/takes off/lands | `scripts/check_m0.sh` exits 0 | ✅ 2026-07-04 — exit 0; peak alt 1.656 m (bar: ≥1.6 m), landed at 0.014 m; verifier-confirmed (`logs/m0_takeoff_20260704T225304Z.csv`) |
| M1 Camera | Frames from `gz_x500_mono_cam` into Python via gz-transport | N frames at expected resolution | ✅ 2026-07-04 — exit 0; 10/10 frames @ 1280×960 RGB, real content (stddev 13.2), verifier-confirmed (`logs/m1_frames_20260704T230509Z/`) |
| M2 AprilTag | Tag detected live; bearing/rel-position vs ground truth | detection rate + pose error logged | ✅ 2026-07-04 — exit 0; detection_rate 1.000, mean err_norm 0.0861 m (bar: ≤0.25 m), mean range 4.888 m (`logs/m2_detect_20260704T233941Z.csv`). Stands on the validated **wide (99.7°) lens**. (A Tier-2 60° narrow-lens variant was tested and **rejected** — ADR-0024 3rd addendum — so M2's wide-lens numbers are current. The one keeper: `scripts/check_m2_envelope.py` now *measures* the detection envelope instead of assuming it — ~6 m ad-hoc on the wide lens, remeasure if the lens ever changes.) |
| M3 Static intercept | Close on static tag, hold 2 m standoff | final standoff error < 0.5 m | ✅ 2026-07-05 — exit 0; final standoff err 0.018 m / 0.035 m across two verifier runs (bar: <0.5 m), detection coverage 1.000, settle ~10 s, no overshoot; verifier-confirmed (`logs/m3_intercept_20260705T000619Z.csv`, `...000818Z.csv`) |
| M4 Moving target | Pursuit vs pro-nav on moving tag | < 1 m closest approach @ ≥ 2 m/s | ✅ 2026-07-05 — exit 0; pro-nav miss 0.402/0.277/0.443 m across three gate runs (bar <1 m) vs pursuit 2.544/2.109/2.048 m on identical 2.0 m/s crossing paths — pro-nav 4.6-7.6× tighter; camera-only guidance verifier-confirmed incl. no-cheat numeric check (`logs/m4_intercept_*_20260705T03*.csv`) |
| M4.5 Realism (S1–S4, ADR-0010) | FPV speeds + two-stage sensor handoff + maneuvering targets | per-sub-step scripts (`check_s1.sh`, `check_s2.sh`, …) | ✅ 2026-07-08 (closed with M5) — S1 ✅ (`--fpv`, 0.94 m vs 3 m/s from hover; hover capped ~3 m/s); S2 ✅ 2026-07-05 (external-cue dash→camera-only handoff; 6 m/s crosser — uncatchable from hover — now ~1.1-2.3 m; honesty audits pass; tracking A/B: Kalata rejected in Gazebo, ADR-0013); S3/S4 (path suite + adaptation proof) folded into the M5 final Monte-Carlo and covered there (ADR-0036: weave/jink/oblique arms in the 96-flight matrix — see the M5 row) |
| Perception design | Real-interceptor perception half — ground stereo + onboard fusion + compute/latency + kill mechanism | design docs + ADRs | ✅ 2026-07-06 — ADR-0015..0024; `docs/{perception_design,stereo_design,compute_setup,ground_modality,seeker_upgrades,kill_mechanism}.md` |
| Root cause + solutions | Why fast-crosser misses ~1.4 m; cost-first fix research (5 lanes) | diagnosis + consolidated plan | ✅ 2026-07-06 — miss is KINEMATIC not perceptual (ADR-0023: ZEM-vs-miss r²=0.96 at handoff, 0.99 at freeze, n=41); proximity Pk metric ratified (ADR-0025); Tier-1 terminal levers don't move it in Gazebo (ADR-0023 addendum); `docs/terminal_solutions_plan.md` |
| M5 Polish | Monte-Carlo, plots, README, demo GIF | `scripts/check_m5.sh` exits 0 | ✅ 2026-07-08 — **FINAL batch DONE (ADR-0036, n=96, `scripts/check_m5.sh` PASS, verifier-confirmed):** adopted running-start deployment profile + corrected σ_R constants under the realistic degraded cue. **Pk@2.5 m 27%→100%** (vs ADR-0029's old hover geometry), mean miss 1.08 m / median 0.93 m, 96.9% clean; the whole 6/9/12 m/s FPV band is catchable (12 m/s was uncatchable from hover). Laws TIED on every path (kinematic regime, now ~1 m not ~3.6 m); maneuvers survive (jink cheap ~1.0 m, weave worst ~1.4 m); east/world_x sign channel CLEAN (oblique L2R 0.63/R2L 0.38). Demo video DONE (ADR-0032). Supersedes ADR-0029/0030/0031 numbers. Plots `docs/images/m5_*.png`; the earlier arc: running-start Gazebo-confirmed (ADR-0028 add.), dash-track fix (ADR-0030). |
| Post-M5: markerless seeker arc | Kill the AprilTag — camera-only intercept on a tag-less body | `scripts/check_seeker_v2.sh` exits 0 | ✅ 2026-07-08 — off-the-shelf viable (ADR-0038, 6/8 clean, +1.03 m median); timing levers don't recover it (ADR-0039); fine-tune v1 closes acquisition but positives-only insufficient (ADR-0040); **v2 hard negatives: pollution 0.751→0.000, range 0.056→0.935, probe 8/8, per-tick audit green — clean-rate 6/8 honest FAIL; residual +1 m = terminal bearing-noise throughput (guidance-side), NOT detection** (ADR-0042, check 6/6, verifier-confirmed) |
| Post-M5: fusion capstone | Covariance-gated mid-course fusion (EKF + markerless + cue) | per-arm audits + results-ADR | ✅ 2026-07-08 — **CLOSED (ADR-0044, ~56-flight ladder):** mid-course cue fusion WORKS under the markerless seeker — hand-set polar FusedTrack beat fusion-off 8/8 paired seeds (−0.356 m median), rescued both chronic failures, 16/16 clean, SURVIVED WORST cue 8/8; covariance gating did NOT earn its keep (variance blowup EXPECTED, 5/8 + 7.66 m tail at WORST — council F1 confirmed in flight); honesty boundary held (0 post-latch cue updates, 32 EKF flights). Design ADR-0041 (council-reviewed); EKF exonerated of ADR-0037 artifact; per-tick audit debt paid; ADR-0043 terminal levers = pre-registered null |

| Phase 2: real-pipeline spine (sim-to-real blueprint) | Replace every MOCK with a REAL, implementable pipeline — ground stereo rig → NN detect → triangulate → real link → drone flies on the genuinely computed cue | per-task gates (`check_t16.sh`, `check_t17.sh`, `check_t19.sh`) + ADRs | 🟩 SPINE DONE 2026-07-08/09 (ADR-0045..0053) — plan adversarially reviewed (ADR-0045, 18 binding amendments) + councils F1/F2/F4. **T16** stereo rig world/model + capture harness (verifier-gated). **T17** single-class ground detector (ADR-0047 acquire-then-track builder ruling; ADR-0049 gate PASS but necessary-not-sufficient — one range/size). **T18** triangulation + σ_R harness; pivotal checkpoint (ADR-0050) real σ_R at handoff UNDER the mock's assumption (no red flag); **σ_R∝R² VALIDATED across 50-160 m (ADR-0053, exp 2.003, c matches model 0.98×)**. **T19** compute split (ADR-0046 offline-replay / ADR-0048 dual-gate / ADR-0051 precompute): **check_t19.sh exit 0, drone flies on the real stereo cue tracking to 0.99 m median, verifier-gated.** Honest findings the sim CAUGHT: the clock-epoch bug = the ground↔drone PPS/RTK time-sync problem (ADR-0052); the detector's out-of-domain bias (ADR-0053) → T17-v2 in progress. Design docs: T22 `fpv_fidelity_design.md`, T23 `sim_to_real_gaps.md` (26 gaps, 23 flatter), T24 `nn_transfer_plan.md`. **Open (updated 2026-07-10):** T22 build, T25 demo video (tooling assemble-ready, precondition met). T17-v2 ✅ (ADR-0055, ground_v2 fixes the out-of-domain bias); T20 fusion DEFERRED as a probable full null (ADR-0055 verifier note — hand-set `--fuse-midcourse` stays recommended); T21 ✅ (ADR-0056 + the ADR-0058 detect-then-track fix). |
| Post-spine: markerless maneuvering fix (detect-then-track) | Markerless seeker holds a 12 m/s maneuvering **camera-only** terminal on a tag-less body — the seeker half of the jam-resistance story (the "works comms-denied" intercept claim itself stays **HELD**, ADR-0059) | ADR-0058 + 4-arm paired A/B + Fable reviews + verifier | ✅ 2026-07-09 — **COMMITTED d10c9ca, verifier-PASS on all 5 claims.** ADR-0056 found the T21 limit (markerless phantoms at 12 m/s+weave/jink → 12/16 false handoffs, miss 4-9 m while AprilTag holds 8/8); attempts 1-3 failed or were comms-available crutches (ADR-0057). **Detect-then-track (builder's insight — NN acquires ONCE, cv2 CSRT follows frame-to-frame, NN re-validates every 8 frames):** camera-terminal Pk@2.5 weave **3/16→14/14**, jink **1/8→14/15**, phantom handoffs **12→0**, **0/155 FALSE** terminal dets (AprilTag ceiling 8/8 @1.64 m). Honesty re-earned (legal_cue_pos() runtime latch + AST pins); two Fable reviews + a micro-review hardened it (inverted-confidence gate killed, RTF-invariant coast clock, cap 12<18 m phantom offset). Builder maneuver-fix directive SATISFIED. |
| Post-spine: onboard seeker v3 (maneuvering retrain) | Reduce the mis-lock phantom at the SOURCE with maneuvering-augmented training data | frame-level v2-vs-v3 gates G1-G5 + reframed verdict | ✅ 2026-07-10 — **CLOSED as an honest NULL (ADR-0061): v3 does NOT ship.** It regresses flight recall (v2 0.47→0.19 maneuver; worse on ALL 10 held-out flights, sign p=0.002) while acing its own static-grid val — static far-range data did not transfer to the flight problem. The pre-registered scorer fixes (forward audit, `f6427af`) correctly PREVENTED a false win. `drone_finetuned_v2.onnx` stays deployed; the null is benign — detect-then-track (ADR-0058, 14/14) already holds the maneuver. |
| Sim-to-real design review + hardening | Audit the sim→real gap; convert unknown concerns to MEASURED findings + fixes; cost-effective real-build path | 11-agent design-review workflow + per-finding validation + ADRs | ✅ 2026-07-10 — **review delivered + its headline findings VALIDATED IN FLIGHT.** `docs/design_review_sim_to_real_2026-07-10.md`: ranked gaps + variable table (each → bench-measurable) + staged BOM ($0 desk → ~$257 Stage-0 → ~$1.05-1.25k). **Blur (G1)** bounded in sim (bench closes it). **Camera-pitch (ADR-0060)** measured — ~35% of dash ticks target above FoV → up-tilt mount (#35, now load-bearing — **SUPERSEDED: fixed-mount adoption later FAILED the pre-registered Stage-1 parity re-fly (ADR-0068 addendum, worse 7/8 compensated) and a fixed tilt only relocates the in-view window under the swinging dash pitch (ADR-0076 add #18j-fix); the pointing lever is now ADAPTIVE tilt #46/ADR-0065 — see docs/project_state.json graveyard**). **Jam fail-closed (ADR-0059)** — MC FLEW: fail-closed demonstrated (witness 12→2→0 across 15/18/22 m cutoffs), fix validated **FAIL-SAFE, not recovery**; recovery arm (coast-search) = **NULL** — binding constraint is onboard perception at 15-21 m; "works comms-denied" stays HELD. **ADR-0062** guidance-correctness fixes shipped (FIX-A derotation A/B-validated 7/8 tighter; FIX-B inert). Three overnight audits dispositioned in `docs/audit_findings_tracker.md` (81 findings). Follow-ups tracked #30-#44 + docs/next.md queue. **Burn-down (`94198e8`, #36 refresh):** 18 findings → FIXED (13 UNTRACKED via doc/repro fixes, 4 already-landed, plus DEEP-N1 via the #42 guard), 7 → TRACKED, 3 → PARTIAL; tracker recounted **UNTRACKED 51 → 27**. |
| Statistics hardening (Pk n=72 campaign) | A defensible "Pk ≥ 95% (95% CI)" headline on the adopted post-FIX-A config | Wilson/CP lower bound on n=72 fresh weave flights (#33) | ✅ 2026-07-10 — **DONE (ADR-0064, `d81e046`):** fresh post-FIX-A weave n=72 (4×16 sub-arms seed 1042/2042/3042/4042 + 8 `fixA_on` seed-42 flights) → **Pk@2.8 m 72/72 = Clopper-Pearson LB 95.0%** — the ≥95%-CI bar CLEARED; **Pk@2.5 m 71/72 = 98.6% point / 92.5% CI-LB** (the one 2.757 m miss handed off cleanly — terminal dispersion, not a failure mode). Weave/12 m/s ONLY — never pooled across paths/speeds; always quote radius-explicit (never a bare "95% Pk"). `logs/mc_pk72_weave_s{1042,2042,3042,4042}.csv` + `logs/mc_fixA_on_weave.csv`; sizing harness `scripts/stats_hardening_options.sh`. Supersedes the r2 n=16 79.4%-LB framing. |
| #35 Up-tilt camera mount A/B | Does a fixed up-tilt mount recover the dash-FoV gap (ADR-0060) and feed the comms-denied recovery NULL (ADR-0059)? | paired A/B vs level (up00), 4 arms × n=8, same seeds — FoV coverage, first-detection range, uncompensated terminal | ✅ 2026-07-10 — **FLOWN + analyzed (ADR-0067). Mechanism CONFIRMED:** dash-above-FoV **32% → 0%** at every tilt (in-FoV 61% → ~90%) — the ADR-0060 gap closes. **But the paired acquisition gain is small (+2–3 m median vs up00)** — the interim "14.3 → 21.9 m" read was confounded by the old r2 baseline (it detects 5.3 m later than up00 on identical seeds; catch documented in ADR-0067) — **and the uncompensated terminal cost is dose-dependent** (miss med 1.81 → 2.79 m, Pk@2.5 8/8 → 2/8 by 35°; guidance assumes zero mount — worst-case by construction, n=8 Wilson-CI language only). **NO mount adopted**; #40 mount-compose gates the terminal re-fly (up15 = leading candidate — **SUPERSEDED: the #40 gates FLEW — Stage-1 terminal re-fly = pre-registered parity FAIL (up15-compensated worse 7/8) → NO fixed-mount adoption; Stage-2 tilted recovery re-test = NULL (ADR-0068 addenda); fixed up-tilt adoption graveyarded, adaptive tilt #46/ADR-0065 is the open lever**), any adoption, and the tilted recovery re-test. Evidence: `logs/mc_uptilt_weave12_up*.csv` + `logs/uptilt_ab_analysis_20260710.txt` (committed). Adaptive tilt (ADR-0065, #46) gained standing from this result. |
| Realistic-quad r2l intercept (ADR-0076) | Diagnose + fix the realistic 3D quad target's right-to-left intercept (0/8 within the 2.5 m Pk gate vs l2r 8/8) | root cause + a validated fix path | 🟨→🟩→🟨 (superseded — see end of row) 2026-07-12/15 — **DIAGNOSED then LARGELY RESOLVED by the real-build pivot.** The saga's "r2l 0%" = r2l engaged 8/8 but at 3.25–4.27 m, i.e. **0/8 inside the 2.5 m Pk gate** (a Pk-gate miss, never a no-engagement null). The phantom-blades theory (add #13–#16) was WALKED BACK (add #17): phantom-removal regresses l2r, and the true r2l floor is the **aspect-biased camera BEARING (ADR-0056) corrupting the fused mid-course dash aim** so it under-commits east. **add #18 (2026-07-15) — the real-build pivot cracks it:** the real interceptor has NO ground cue → the `--coded-dash` architecture (open-loop collision-lead dash → camera-only terminal, cue stack dropped) removes that fused dash and **lifts r2l 0/8→5/8 @Pk, mean 3.97→2.68 m** on the paired quad_v2/seed123/weave A/B (partly refuting add #17's "no fix reaches it"). Cost: l2r loosens 0.61→1.37 m (the cue helped l2r) but stays 8/8 @Pk; net gap 6.5×→2×, combined Pk@2.5m 8/16→13/16. Residual r2l gap = the ADR-0056 bearing aspect-bias (the honest remaining lever). New: `--coded-dash`/collision-lead heading, `--dash-heading/target-err` robustness knobs, `scripts/coded_dash_arm.sh`, portable `flight/` core (11 tests). Deployed v2 + billboard 72/72 (ADR-0064) UNAFFECTED. **⚠ SUPERSEDED IN PART (2026-07-15/16, ADR-0076 add #18g/#18h/#18k; authoritative: docs/project_state.json):** the in-gate Pk reads above were open-loop dash-ballistics MIRAGES, not camera-guided intercepts (dash-only control matched with-terminal 0.30 vs 0.29 m; a 0-ENGAGE arm scored 7/7 <1 m) — no camera-guided 3D-quad intercept exists in the dataset. What survives: the coded-dash ARCHITECTURE win (dropping the corrupted fused dash) + the crossing-bias as open-loop AIM calibration. And "residual = ADR-0056 bearing aspect-bias" is RETRACTED: the binding wall is FLIGHT-DYNAMIC detector recall/pointing (0.8% in flight vs 100% static 8-22 m; ~40° dash pitch parks the target at frame-top) — adaptive tilt #46/ADR-0065 is the dominant lever, real-data detector demoted to hypothesis. |
| Real hardware build — code gearing (P0, memory [[real-build-pivot]]) | Gear the code for the REAL interceptor (docs/hardware_order_list.md): CODED open-loop dash → CAMERA-ONLY terminal, no cue | Fable real-build plan P0–P3; per-slice commits + tests | 🟩 IN PROGRESS 2026-07-15 — **P0.1 `--coded-dash` architecture BUILT+VALIDATED** (retroactive r2l A/B above). **P0.3 slice 1** portable `flight/` package (geometry + `collision_lead_heading`, 11/11 tests, no gz/gt/cue imports → runs on the real Pi). Robustness sweeps (dash-heading/target-pos error — the open-loop-aim tolerance) IN FLIGHT. NEXT: wire m4→flight/, extract the pro-nav terminal, port the Pi5/Hailo YOLO path + camera calibration + MAVSDK-serial/TELEM2. **[SUPERSEDED same-day — see docs/project_state.json (launch_aim + terminal) + ADR-0076 add #18b/#18c: the robustness sweeps are DONE (48/48 acquired/engaged up to ±30°; per add #18g read as DASH-robustness, not perception proof — the camera-terminal kill claim itself was retracted, add #18g/#18h); the flight/ core is COMPLETE (geometry + alpha-beta/Kalata estimator + pro-nav N=5 + Brown-Conrady undistortion, 25+ tests) and WIRED — m4's coded-dash aim calls flight.guidance. Still open: the Pi5/Hailo YOLO path port + MAVSDK-serial/TELEM2 (hardware unordered, HAT deferred).]** |

| Tier-1 build phase: hardware ordered + the software layer landed | The real build starts — target-drone stack + seeker kit + interceptor brain bought; every Tier-1 field tool exists in code before the parts arrive | build_plan P0–P2 gates (`docs/project_state.json`); functional checks, not measured gates | 🟩 IN PROGRESS 2026-07-20 — **HARDWARE ORDERED** (Fable GO-WITH-FIXES, `20ff234`): target-drone stack (ArduPilot/Matek H743 after the SpeedyBee dead-end, `6bb5d8f`) + seeker kit (OV9281 + Pi 5) + interceptor brain (6C Mini + M10, front-loaded past the money gate — logged); must-add top-up ORDERED same day (~$116 Amazon + ~$300 vendor: consumables/tools/cables/18650s/smoke-stopper/checker/+2 packs, `d34b390`; still missing: soldering iron, Kakute microSD, LiPo bag/glasses/blanket, Pi cooler, PD bank, tripod+head, buzzer; ⚠ 6C Mini + PM02 possible-duplicate flagged — verify, return one pair). **Tier-1 SOFTWARE layer landed same day, all adversarially verified** (`69ab3f2`, `4272fdf`, `1bdd310`, `e73f447`): `pi_capture.py` (Pi 5/OV9281 session recorder, logs actual exposure vs the ≤1 ms spec), `tripod_score.py` (P0.3 two-curve money-gate scorer — tag envelope gates the ~$740 Tier-2 order, NN recall gates only the Hailo), `field_score.py` ArduPilot DataFlash .BIN support (PX4-interceptor + ArduPilot-target CPA first-class), the cam-offset/model-swap guard (m4 exits 2 on mismatch — no silent uncompensated runs), deploy seeker loop MAVSDK path **SITL-validated** (3 real defects fixed; `scripts/check_deploy_sitl.sh` exit 0), camera paper-check **CLEARS** (`docs/camera_paper_check.md`: 118° horizontal; px/deg penalty corrected ~30%→~15%); dashboard grew the Build tab (schema v2.3, workbench view). Binding wall unchanged: in-view detection (ground-background + phantom-competition) + the acquisition/blur gap the tripod session measures. Next physical step: build the target + seeker rig, bench the 6C Mini props-off in parallel (`docs/project_state.json` headline/build_plan). |

| 2026-07-25 what's-left build push | Drive the whole open backlog to zero: the interceptor-size arithmetic, the sim bugs, the field-day P0s, CI, and the full doc/contract/artifact sweep | `docs/audit_2026-07-25_whats_left.md` (8-agent audit) + per-item verification; `run_tests.sh` green; ADRs 0084-0089 | 🟩 2026-07-25 — **RAM/KILL RADIUS RATIFIED 0.35 m** (ADR-0084, arithmetic from the ordered 5-inch pair: interceptor Mark5 Pro half-span 177.8 mm + target Source One V6 174.8 mm = 0.353 m contact envelope; retires the 7-inch 0.5 m; the 5-inch carries the full seeker at T/W ~6:1 so size is not payload-limited; `field_score` default + aim budget (±3.0°→±2.4°) + every visible Pk surface swept). **SIM BUGS FIXED:** the coded-dash camera terminal `frozen_vworld` zero-command latch (the camera-arm sweep verdicts were measured on a terminal that never steered → downgraded to PENDING RE-FLY; SITL confirms 0/104 ENGAGE ticks now zero-command) + its blind auditor (`audit_per_tick` check (e) FAILs a zero-command run) + wall-clock→sim-clock + inert `--law pip` flag guards. **FIELD-DAY P0s CLOSED:** `--quad-decimate` plumbed+stamped, `gen_tripod_mission` geometry corrected (refuses off-protocol), capture matrix re-briefed for the beam-facing placard + incidence-aware curve-(a) scoring, `range_truth_join` integrity checks hoisted onto the manual field-day path, desk-fps whitelist (ADR-0087). **PRINT ARTIFACTS** built + dimensionally verified (placard 0.35 m tag, checkerboard, mount `.scad` — `hardware/prints/`, `scripts/print_artifacts/`). **FAILSAFE 7** (post-GO offboard loss → SAFE, ADR-0086) + `docs/kill_day_protocol.md` + `docs/regulatory_site_capture.md`. **CI GREEN** (was 73/73 red — gitignored-fixture + dep-rot + stage-gating fixed; suite 428→592 tests; a `pupil_apriltags` Detector-churn segfault fixed by a per-decimate cache). **Tier-1 fully ORDERED** (builder 2026-07-25). Contract + dashboard + hosted artifact re-rendered; NEXT/README/PROGRESS/real_build swept. ADRs 0084 (ram/size), 0085 (#16 altitude datum), 0086 (#22 FAILSAFE 7), 0087 (desk-fps), 0088 (wedge contradiction resolved), 0089 (2nd TX). |

| Bench bring-up + the instrument sweep | Prove the real hardware chain works, and stop trusting instruments that were never checked | Hardware measurements on the Pi 5 + `run_tests.sh` green + ADRs 0090–0094 | 🟩 2026-07-26 → 2026-08-10 — **THE PI 5 SEEKER RIG IS UP AND MEASURED.** skr-01…skr-05 done (Debian 13 not Bookworm; camera found on CSI port **0**, not the default 1; exposure applied **994 µs**, inside the ≤1 ms spec that tripod day's motion-blur read depends on). **skr-07 THE FRAME-RATE FINDING** (ADR-0090): the seeker had been running at 30 fps because *nobody set* picamera2's `FrameDurationLimits` — not the sensor (143), not the CPU. Measured sustained through 750 s soaks with no throttling: **96.6 fps** at `quad_decimate=2.0`, **38.2** at 1.0, CPU-YOLO **6.09**. `R_streak_burn` goes as 1/fps, so this cuts the range burned forming the handoff ~3.2×. **brn-01…brn-05 PASS** — MAVSDK OFFBOARD over a real serial UART, props off: the one link the sim never exercised. Own-state age gate sized from that bench at **0.25 s, ON** (ADR-0093). **The flight camera source then measured ON HARDWARE at its pinned rate: 60.27 fps** (uncapped 114.9), closing ADR-0090's last follow-up. **INSTRUMENT SWEEP (2026-08-10):** the scorer was found ranging to the **camera lens** against a criterion ADR-0084 defines as centre-to-centre; an interim "+0.208 m lens lever, 0/16 → 12/16" re-score was itself **RETRACTED the same night** — the +0.208 m was landing-gear height (a height-above-takeoff subtracted from a world z), and the offline re-scorer over all 168 flights (`scripts/rescore_cpa.py`, `docs/rescore_2026-08-10.md`) puts the true correction at **3/16 logged → 5/16 interpolated** inside the 0.35 m ram radius. Contact range remains unreached at the median; the −0.37 m vertical bias finding stands. `run_tests.sh` was found **RED for 12 days / 9 commits** — ADR-0093 had certified "519 passed", which is exactly `pytest tests/`, never running the 173-test `flight/tests/` half where the contradicting test lived; fixed, plus a producer→consumer contract on the range calibration (an under-powered fit was being consumed as calibrated, on a number that multiplies into `a_cmd`), two tests that could never run, and skip-enforcement so this class cannot recur. **739 → 850 passed, ALL GREEN.** Builder decisions cleared: demos retired, regulatory closed (own land), 2nd TX to buy. ADRs 0090–0094. |

Details per milestone live in `docs/next.md` (current work) and `docs/decisions.md` (choices).
