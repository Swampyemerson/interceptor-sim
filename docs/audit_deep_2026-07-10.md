# Morning Audit Report — Interceptor Sim
*Overnight, 5 dimensions, verifier-adjudicated. Verdicts applied: false-positives dropped, severities corrected, missed items folded in.*

## 1. Headline

The project is **sound at its spine**: the no-cheat honesty boundary holds (no *active* ground-truth leak found in any live path across all 5 audits), and every headline number independently recomputed **byte-matched its local CSV exactly** (mc_final_all.csv n=96, the weave12 14/14, M2/M3/M4). The real problems are all at the **edges**, and there are four of them worth losing sleep over: (1) **nothing is committed** — the entire evidence base lives only on this disk and the current culmination headline (14/14 maneuvering) has *zero* scripted gate; (2) **two secondary docs are two arcs stale** and break the "comms-denied is HELD everywhere" promise the project brags about; (3) the **highest-stakes honesty/guidance logic has no sim-free test coverage and there is no test runner at all**; (4) the thesis question — "ever intercepted while jammed?" — is honestly **"no, never, in any config."** None of these are fatal; most are sim-free-fixable tonight.

---

## 2. BLOCKERS / HIGH — what the main session should FIX

### H1 — No headline CSV is committed; the current culmination headline has no gate `[reproducibility, HIGH, sim-free-now]`
`.gitignore` ignores `logs/` wholesale (`git ls-files logs/` empty). Every "Source" cite in README/portfolio_bullets points into that gitignored dir. The M5 headline at least has `check_m5.sh` (regen + assert — I ran it, PASS), but the **ADR-0058 culmination arm (14/14, median 2.03 m, 0/155 false) has no scripted gate at all** — only `analyze_track_ab.py` run by hand against a local-only CSV (`no check_t21.sh exists, only plot_t21_maneuver.py`). A disk loss makes the published numbers permanently unverifiable given ~1–5 m run-to-run noise.
**Fix:** gitignore exceptions (`!logs/mc_final_*.csv`, `!logs/mc_t21_*.csv`, `!logs/mc_realistic_*.csv`, the 4 M3/M4 gate CSVs) — aggregates are tiny (~100 KB total); add a `check_t21.sh` that re-asserts the ADR-0058 three-way attribution from the committed CSV (`analyze_track_ab.py` already computes it).

### H2 — Handoff-streak state machine (the one-way honesty latch trigger) has zero sim-free coverage `[test-quality, HIGH, sim-free-now + sim-gated]`
The N-consecutive-in-range streak that fires `handoff_done=True` and closes the cue channel lives inline in `m4_intercept.py:2438-2504`; `grep` for `handoff_streak/HANDOFF_STREAK/EARLY_HANDOFF` in `tests/` returns **zero**. The one "pin" (`test_cue_staleness.py:295-305`) only does `any()` over *all* `BoolOp(And)` nodes in the file — it does not target the handoff gate or verify increment/reset/threshold. A regression (streak not reset on cue-inconsistent det, counts stale dets, FPV silently drops the 2-streak) hands off on a phantom and only a full Gazebo gate catches it.
**Fix now (sim-free):** tighten the pin to locate the actual `if in_handoff_range and cue_consistent:` / `if handoff_streak >= …` by AST shape. **Then (gated):** extract `update_handoff_streak(...)`, unit-test exhaustively, re-run check_s2/jam to confirm byte-identical.

### H3 — `interviewer_prep.md` breaks the "HELD everywhere" promise `[portfolio, HIGH, sim-free-now]`
README:157-158 promises `'"works comms-denied" is a held claim everywhere.'` But `interviewer_prep.md:16` leads with the jam claim unqualified, and **`:248-249` asserts it a second time** ("even if the link is jammed mid-flight, the interceptor finishes on its own") — `grep` for HELD/ADR-0059 in that file returns nothing. This is the doc README bills as "the hard questions, honestly answered"; an interviewer catches the project overclaiming in the exact artifact that brags about not overclaiming.
**Fix:** add the HELD box + ADR-0059 Q&A to `interviewer_prep.md` (both :16 and :248-249). Same pass on `portfolio_visuals.md` beat 4 (:200-203).

### H4 — `WRITEUP.md` is two arcs stale — contradicts the current headline and undersells the biggest win `[portfolio, HIGH, sim-free-now]`
§5 line 56 headlines "Pooled Pk@2.5 m = 27%" (ADR-0029, n=48) — directly contradicts README's superseded-to-**100%** (ADR-0036, n=96). Worse, **§6 line 74 actively calls the markerless seeker "#1 unbuilt risk… NOT simulated"** — now *false* (ADR-0038–0058 built and Gazebo-tested it), so the doc reads the project's biggest post-M5 achievement as vaporware. §1 declares comms-denied "the whole point" with no ADR-0059 hold. Both files at commit `53a72dc`/2026-07-07; the provenance footer (`:112`) even guarantees "every number traces to ADR-0008 through ADR-0034" while the project is at **ADR-0060**.
**Fix:** dated banner at top ("Written pre-ADR-0036/0059 — see README for current"), then currency pass: swap in ADR-0036 numbers, fix §6 line 74, fold in the markerless/detect-then-track arc, update the footer envelope.

### H5 — "~96% of the miss is locked in at handoff (r²≈0.96)" conflates variance-explained with fraction-of-miss `[portfolio, HIGH, sim-free-now]`
r²=0.96 = 96% of run-to-run *variance* explained by ZEM@handoff — **not** 96% of any flight's miss magnitude. The project's own capacity bound (0.72 m recoverable vs 1.69 m delivered) says a perfect seeker cuts only **~25%**, i.e. ~75% locked in, not 96%. The misread appears in three surfaces: `portfolio_bullets.md:40-41`, `:148`, `interviewer_prep.md:24`. README:50 gets it right ("miss tracks ZEM@handoff, r²=0.96" — correlation). A stats-literate interviewer punctures this in one sentence, and it taints the project's *strongest* diagnosis. (This also surfaced in reproducibility at low severity — same defect, canonical fix here.)
**Fix:** everywhere render it as "final miss is a near-deterministic function of handoff geometry (r²≈0.96 vs ZEM); physical correction capacity 0.72 m vs 1.69 m delivered → perfect seeker cuts ~25%." Never render r² as "% of the miss."

### H6 — The thesis has never been demonstrated: no jam has ever been flown, in any config `[portfolio, HIGH, sim-gated + sim-free-now]`
ADR-0059 (`decisions.md:1358-1362`) is explicit: every ADR-0058 arm ran the cue mock full-duration — **the gate code has never been run against a mid-dash jam.** Post-latch immunity is by construction (structural latch + AST audits), a strong but *different* kind of evidence; pre-latch jam defeats the adopted config; the fix is built but unflown. README's HELD box + the self-audit story survive this honestly, but H3/H4 would make the builder contradict himself mid-answer.
**Fix (sim-gated, single highest-leverage batch in the project):** fly `scripts/mc_jam_arm.sh` at idle load — paired: adopted-config-with-fix + default-config control + no-jam control. **Fix now (sim-free):** pre-stage the two-step follow-up Q&A in `interviewer_prep.md` ("never demonstrated end-to-end; here is exactly what IS: structural latch, audits, fail-closed finding, built fix, queued batch").

---

## 3. MEDIUM / LOW — grouped, terse

**Correctness (medium):** The **range-increase breakoff (`BREAKOFF_RANGE_INCREASES`, m4:2736-2749) has no noise deadband and no trend gate** — strict `> last_fresh_range` over 3 consecutive fresh dets. Known-size range (`fx*span/w_px`, nn_seeker:235 / finetuned:182 / two_stage:165) is pixel-quantized (~2-3 cm/px near 2 m), so **pure jitter with no edge-clip** can produce 3 monotone rises and false-trigger an early breakoff; a bottom-edge frame-clip or a shrinking `--track` CSRT box does the same (L/R/top clips are already self-masked). Critically, the ADR-0056 reject/coast gates are **absolute-divergence** (`|meas−x_hat|`), not trend gates — so this survives *even with the maneuver-hardening config enabled*, not just in the default-off case the original finding assumed. AprilTag path is spared (smooth solvePnP range). *Fix: reject frame-border boxes from the range channel + add a deadband/dead-reckoned-plausibility gate on the breakoff.*
**Correctness (low):** all-zero `frozen_vworld=(0,0)` latch if ENGAGE begins inside `TERMINAL_FREEZE_RANGE_M` (m4:2709-2715) — reachable only with a pathological non-fpv `--target-start` in (0.5, 2.0) m; add a nonzero-vector fallback. Cue-staleness can't distinguish jam from a benign >1 s dropout (m4:534-558) — documented deliberate fail-open, low-prob compound. MEAS_STALE_S wall-time gate (m3:305-307) — **verifier downgraded to nit**: the *entire* estimator/control stack is wall-timed and already mitigated by the RTF~1 batch mandate; documentation note, not a defect. (Missed root cause worth a line: predict() integrates a fixed nominal `dt` while correct() scales by wall `dt_since` — a real predict/correct dt inconsistency under loop saturation, distinct from RTF-scaling.)

**Honesty (medium → the real "so what"):** No *active* leak anywhere, but coverage is narrower than the boundary it claims. The only AST no-gt pin is scoped to `detect_track.py` alone (`test_honesty_static.py:841,856`), while the live `--track` measurement flows through **`two_stage_seeker.py` (unpinned, and it holds real `gt_frames` reads at :280/:320-321 in offline `_run_eval`)** and a third live producer `FinetunedNNSeeker` (also unpinned). The elevating fact: **no check — static or runtime — would catch GT wired into the seeker's *measurement*.** `check_s2.sh`'s gating no-cheat (c) verifies the command tracks the camera's own lambda; if GT fed the seeker's bearing/range, lambda itself becomes GT-derived and (c) passes perfectly. The only check that could catch it, (d) `corr(d_cmd,d_gt)`, is advisory and never gates. *Fix: parametrized AST scan over every live seeker module, allowlisting the one offline `_run_eval`.*
**Honesty (low):** m4 gt-checks scoped to `run_acquire_and_engage` only — `run_bench()` reads gt (:2970/:2994) + logs a cmd (:2998), and `main()` TAKEOFF reads gt (:3322), both unaudited (logging-only, no live leak). The docstring's "only function that builds a velocity command" is inaccurate. Also: the m4 checks catch `ast.Name 'gt_*'` but **not attribute-form** (`tracker.gt_range`) — strictly weaker in shape than the detect_track pin. All static checks key on the `gt_`/`ground_truth` *naming* convention; a true-target value under another name passes everything — the "re-earn the no-cheat audit" mandate is the only backstop, and it's process, not an enforced test.

**Reproducibility (medium):** **Hero demo CSV missing** — README:729 cites `logs/m4_intercept_pronav_20260707T211601Z.csv` (miss 0.632 m) as present; it's absent, and `demo_out/README.md:12,:189` also point the build pipeline at it as source-of-truth. Only `portfolio_visuals.md:247` carries the caveat. *Fix: recover from backups + commit, or copy the caveat into README + demo_out.*
**Reproducibility (low/nit):** Two README rows cite CSVs that don't exist even locally (ADR-0028/0030; `decisions.md:1012` = 3 dead cites) — but both explicitly superseded by the verified M5 batch, so no live headline rests on them. ADR-0060 pitch numbers trace to PX4 ulogs outside the repo (rotated + `*.ulg` gitignored) — re-run `dash_pitch_probe.py` with a `--dump-summary-csv`. README repo-map says `plots/` "checked in" but Pk/miss-CDF plots are gitignored (only rig_/stereo_ tracked). **M4 selection disclosure:** "three independent gate runs" (0.402/0.277/0.443) are the *three tightest* of 8 pronav runs — three earlier completed runs read 1.041/1.043/1.123 (all *failing* the <1.0 gate), a fourth pair crashed mid-pronav; paired pursuits were ~2× worse (implying a legit pre-tuning config change), but ADR-0009/README disclose none of it, and the reported pursuit 2.544 was itself `clean=0`. The README M4 log-glob doesn't even resolve to the real stamps (032238/032528/033250Z). `check_m5.sh` is a loose regression-catcher (regens only if merged file absent, wide assert bands) not a tight repro. *One-line ADR-0009 addendum + fix the cites.* **The verification record itself is a positive:** every recomputed number matched; hold-claims consistently marked HELD.

**Test-quality (medium, all sim-free-fixable):** Re-acquire gate tested only at `coast_dt=0` (radius==base==8) — aging/cap formula (detect_track.py:317-320) and `frames_since_loss` lifecycle (:386-408) unexercised (verifier: anti-phantom rides the *constant* cap, so downgraded to medium). `_cam_implied_ne` geometry tested only at degenerate `bearing=0/psi=0` — a **bearing or heading sign error passes the whole sim-free suite** (the exact class GOALS.md warns about); the identical NE mapping is duplicated at m4:2469-2471 with no cross-check. **`COAST_STALE_S`'s value is unpinned** — change 1.0→10.0 and all 86 tests stay green while behavioral tests exercise a horizon production no longer uses (the comment at :51-53 misdescribes what protects it). Two NN parity tests (`test_ground_station.py:535/:647`) `importorskip onnxruntime` → **skip on every run**, and no gate runs them under `.venv-seeker` (which has onnxruntime 1.27 + cv2 + pytest). **No automated test runner** — the only `pytest` in `scripts/` is `check_seeker_v2.sh:86`, and it runs *only* `test_honesty_static.py`; no CI, no pre-commit, no `run_tests.sh`. Untested pure helpers: `wrap_pi`/`kalata_alpha_beta`/`compute_v_close`/`solve_intercept_time` (recompute_min_gt_range_from_csv is a *cross-check*, not the headline scorer — verifier correction). **Plus missed:** `DetectThenTracker.process()` + `_nn_beats_track` (the whole ACQUIRE/TRACK anti-phantom state machine, detect_track.py:328-435) untested; `validate_track_kind()` (the safety guard refusing KCF/unknown, :65-93) untested; the staleness-clock *source* `last_cue_recv_sim_t = sim_clock.t` (m4:2030) unpinned though the predicate is; `_iou`/`_degenerate` untested.
**Test-quality (low/nit — positive assessment on the honesty pins):** The honesty pins are **genuinely AST-based with mutation calibration** (4 injected-mutation tests + anti-vacuity asserts) — fail *loud* (StopIteration), not silent; only cosmetic coupling to source-line text. `test_ekf_tracker`'s alpha-beta gate uses substring checks (`'if args.tracker == "ekf":' in src`) — a rename leaving old text in a comment slips through; convert to AST. `test_seed_ctx_runtime_latch_belt` bare-returns on missing cv2 (silent PASS; latent, cv2 present today) — use `pytest.importorskip`. Seeker-side pixel→bearing intrinsics chain has no offline test.

**Portfolio (medium/low):** 30-sec pitch juxtaposes "a real computed pipeline, not a mock" with the 14/14 (which rode the **mock cue**, per design-review G6) — verifier downgraded to medium (pitch opens "simulated," so the hardware read is weak; residual is the missing mock-cue scope on 14/14). Resume one-liner's unscoped word "**verified**" (n=14, one weave arm, one empty world, mock cue, jink pre-fix) — medium. M5 "fleet scale" bullet omits that n=96 flew the **AprilTag sensor** (neighboring bullets are markerless → conflation) and never defines "clean" — medium. Jink 14/15 quoted without the pre-fix-code caveat README carries (`bullets:57-62` vs README:136-137/G14) — leaning low. `PROGRESS.md:18` calls detect-then-track "the jam-resistance headline" one row above the HOLD at :20 — low (internal doc). "Default config hands off fine under jam" stated declaratively (README:150-152) but is code-trace inference — low (sits in a heavily-hedged box; ADR-0059 itself asserts it). 0/155 with no environment caveat (G8: gate radii are "an empirical accident" of this empty world) — low. Two Clopper-Pearson bounds circulate (77% camera-terminal / 79% pooled) with no denominator note — low. Header meta-claim "Nothing here overclaims" is itself an overclaim — nit. **Missed diff-able staleness:** superseded ADR-0030 degraded-cue numbers (9 m/s→1.19, 12 m/s→1.48) still circulate as *current* on `bullets:153-154` + `interviewer_prep:24` (README:61 marks them superseded — flown under the old ~180× sigma_R). **Constructive:** the design review's "Bottom line for the portfolio narrative" (`design_review_sim_to_real_2026-07-10.md:284`) crisply lists defensible-vs-HOLD claims — mirroring that one paragraph into README/bullets/interviewer_prep closes H3, H4, and the Clopper-Pearson item in a single edit.

---

## 4. Honesty & reproducibility verdict (the spine)

**No-cheat boundary: HELD.** Across all five audits, **zero active ground-truth leaks** were found in any live guidance or seeker path. `gt_*` stays confined to scoring/logging; the cue is structurally unreadable post-handoff; guidance sees camera + own-state EKF only. The honesty *tests* are real (AST-based, mutation-calibrated, anti-vacuity-guarded), not theater. The caveat is **defense-in-depth, not a violation**: the static pin doesn't reach the live seeker modules and *no* check (static or gating-runtime) would catch GT fed into the seeker's *measurement* — a latent structural gap, not a present leak (H-medium above).

**Numeric traceability: HELD on this disk, FAILED as committed evidence.** Every headline number independently recomputed **matched its CSV exactly** — mc_final_all.csv (n=96, clean 93/96, mean 1.084, median 0.929, Pk 0.531/0.792/0.917/1.0), the weave12 arm (16/16 med 2.11; 14/14 med 2.03; 155 dets, 0 false), M2/M3/M4. Held claims (comms-denied, Pk≥95%) are consistently marked HELD on the *primary* surfaces. **But** the entire evidence base is gitignored (H1), the current culmination headline has no gate, the hero-demo CSV is gone, and a swarm of dead/stale cites persist. The numbers are *true*; they are not yet *durable* or *self-verifying on a clean clone*.

**Net:** the portfolio's integrity is intact and defensible — the failures are of *packaging and durability*, not of substance.

---

## 5. The one hardest interviewer question

> **"Your thesis is comms-denied intercept. Show me one flight where the link was jammed and the intercept completed."**

**Answer today: there is none — no jam has been injected in any flown arm, in any configuration** (ADR-0059: every ADR-0058 arm ran the cue full-duration). Post-latch immunity is by construction (structural latch + AST audits — strong, but a *different kind* of evidence); pre-latch jam defeats the adopted config; the fix is built but unflown.

**Do the materials answer it?** The *updated* ones (README HELD box, portfolio_bullets held-list, the self-audit story) survive the chain credibly and even turn it into a strength. **But H3 and H4 sabotage it** — `interviewer_prep.md:16/248-249` and `WRITEUP.md §1` would make the builder contradict himself mid-answer. And no material yet pre-stages the killer follow-up ("So the one scenario the project exists to prove has never been demonstrated end-to-end?" — *correct*). **Flying `mc_jam_arm.sh` is the only thing that converts this from a well-managed hole into a result.**

---

## 6. Recommended overnight fix order

**Sim-free first (do all of these tonight, no Gazebo needed):**
1. **Doc currency pass** (closes H3, H4, H5, PROGRESS.md, stale ADR-0030 numbers in one sweep): add HELD box + ADR-0059 Q&A to `interviewer_prep.md` (:16, :248-249) and pre-stage the H6 two-step follow-up; dated banner + currency pass on `WRITEUP.md` (§5 numbers, §6 line 74, footer envelope); fix r² wording in `bullets:40-41,148` + `interviewer_prep:24`; annotate/remove the superseded ADR-0030 numbers in `bullets:153-154`; fix `PROGRESS.md:18`; **mirror `design_review:284`'s defensible-vs-HOLD paragraph into README/bullets/interviewer_prep.**
2. **Commit the evidence** (closes H1): gitignore exceptions for `mc_final_*`, `mc_t21_*`, `mc_realistic_*`, the hero-demo CSV (recover from backup) + 4 M3/M4 gate CSVs (~100 KB); write `check_t21.sh` re-asserting the ADR-0058 numbers from the committed CSV; fix the dead cites (README ADR-0028/0030, decisions.md:1012), the M4 log-glob, the `plots/` repo-map line; add the M4 selection-disclosure addendum to ADR-0009; re-run `dash_pitch_probe.py --dump-summary-csv` and commit.
3. **Wire up testing** (closes the test-quality swarm): add `scripts/run_tests.sh` (main `.venv` full suite + the `.venv-seeker` onnx pair) and prepend it to every sim gate; pin `COAST_STALE_S == HORIZON_S`; tighten the handoff-streak AST pin (H2 step 1); parametrize `_cam_implied_ne` over nonzero/negative `(psi, bearing)` + cross-check the m4 duplicate; add unit tests for `validate_track_kind`, `_iou`/`_degenerate`, `_seed_ok` radius-aging/cap + `frames_since_loss` lifecycle, the staleness-clock source, and `wrap_pi`/`kalata_alpha_beta`/`compute_v_close`/`solve_intercept_time`; convert the ekf substring gate to AST; `pytest.importorskip('cv2')`.
4. **Correctness hardening** (the medium): add a noise deadband + dead-reckoned-plausibility gate to `BREAKOFF_RANGE_INCREASES` and reject frame-border boxes from the range channel (keep bearing); add the nonzero `frozen_vworld` fallback.

**Sim-gated (needs idle Gazebo, RTF~1, one sim at a time — queue after the sim-free work):**
5. **Fly `scripts/mc_jam_arm.sh`** — adopted-fix + default-config control + no-jam control. Single highest-leverage batch in the project; closes H6 and converts the thesis from held to demonstrated.
6. Extract `update_handoff_streak(...)` to a pure fn, unit-test exhaustively, re-run `check_s2`/jam to confirm byte-identical (H2 step 2).
7. Queued follow-ons: jink r2 n=16 + weave n=48 (retire the pre-fix jink caveat); G8 clutter/decoy world batch (retire the 0/155 one-world caveat).