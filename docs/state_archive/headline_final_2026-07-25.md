# Final `headline` block — archived verbatim 2026-07-26 (tracker redesign)

Removed from `docs/project_state.json` when the NOW bar (`now{}`) replaced the headline
(builder complaint 2026-07-25: accreted dated inserts made the tracker unreadable).
Nothing below is edited; it is the exact final content of every field of the block.

- `headline.date`: 2026-07-25
- `headline.evidence`: ["docs/decisions.md ADR-0076 add #18k", "NEXT.md ROUND-5 CORRECTION banner", "pointing stage changelog 2026-07-17 (fixed-tilt FINAL)"]

## headline.text (verbatim)

HARDWARE ORDERED (2026-07-20, Fable GO-WITH-FIXES): target-drone stack + seeker kit + interceptor brain. [2026-07-21 the binding wall is now DECOMPOSED: both in-view mechanisms settled — ground-clutter REFUTED in sim, phantom-competition CONFIRMED-but-modest + mount-removable — and the ~0 in-flight recall is a POINTING deficit (75% of terminal ticks have the target OUT of frame under the dash pitch; the fixed up-tilt is the fix), NOT a detector-in-view failure. The real-data detector's remaining job is the OUTDOOR terrain/appearance gap the sim can't test.] Next physical step: add the must-add build/field layer, then build the target + seeker rig + bench the 6C Mini in parallel. [2026-07-20 late] The Tier-1 SOFTWARE layer landed (all adversarially verified): pi_capture.py session recorder, tripod_score.py two-curve money-gate scorer, field_score ArduPilot .BIN, the cam-offset/model-swap guard, deploy seeker loop SITL-validated (3 real MAVSDK defects fixed), camera paper-check CLEARS (118 deg HORIZONTAL; px/deg penalty corrected ~30%->~15%); the dashboard grew the Build tab (Tier-1 workbench view). [2026-07-20b] Must-add TOP-UP ORDERED (~$116 Amazon + ~$300 vendor): solder consumables/wiring/tools/jumpers/data-cable/18650s/smoke-stopper/LiPo-checker + 2 more 6S packs. Builder 2026-07-20: no duplicate order (verified); remaining TOOLS owned (iron, multimeter, prop wrench, zip ties); FAA registration good to go. Still missing before build: Kakute microSD, LiPo bag/glasses/blanket, Pi cooler, PD power bank, tripod+head (unless owned), buzzer. [2026-07-21] PLACARD SIZED 0.35 m (P0.2 — print it); ARM64 stack proof PASS (pyapriltags landmine fixed); repo PUSHED to private GitHub (Swampyemerson/interceptor-sim), first CI wired (it actually stayed RED until the 2026-07-25 fix below — the earlier 'green' claim was false). [2026-07-25 BUILD PUSH] The 2026-07-25 what's-left sweep (docs/audit_2026-07-25_whats_left.md, 8-agent workflow) drove the backlog to zero: RAM/KILL RADIUS RATIFIED at 0.35 m (ADR-0084 — derived from the ordered 5-inch pair's contact envelope, sum of half-spans 177.8+174.8 mm; retires the 7-inch 0.5 m; field_score default swept + every visible Pk surface); the 5-inch airframe carries the full seeker (dry 525 g, first-kill AUW 770 g, T/W ~7.8:1) so size is not payload-limited. SIM BUGS FIXED: the coded-dash camera terminal frozen_vworld zero-command latch (verdicts through it were measured on a terminal that never steered) + its blind auditor (audit_per_tick now FAILS a zero-command run) + wall-clock scheduling → sim-clock. FIELD-DAY P0s CLOSED: --quad-decimate plumbed end-to-end + stamped; gen_tripod_mission geometry corrected (refuses off-protocol); capture matrix re-briefed for the beam-facing placard + incidence-aware curve-(a) scoring; range_truth_join integrity checks hoisted onto the manual field-day path; desk-fps whitelist (ADR-0087). PRINT ARTIFACTS built + dimensionally verified (placard 0.35 m tag, checkerboard, mount .scad — hardware/prints/, scripts/print_artifacts/). FAILSAFE 7 (post-GO offboard loss → SAFE, ADR-0086) + kill_day_protocol.md + regulatory_site_capture.md written. CI GREEN (was 73/73 red — gitignored-fixture + dep-rot + stage-gating all fixed; suite 428→592 tests). TIER-1 fully ORDERED (builder 2026-07-25). ADRs 0084-0089. [2026-07-25 eve] PHYSICAL BUILD STARTED — target frame carbon + Pixhawk 6C Mini + Pi 5 reported IN HAND by the builder; target frame skeleton assembly begun (mechanical front half of tgt-03: dry-fit, no final torque until the powered build; no solder/power work until the smoke-stopper consumables arrive). Sim: the m4 negative-r_hat coast-arm inhibit landed (86d60b3) and the 5 camera arms are RE-FLYING on the fully-fixed terminal (~40 flights, dash-only twins reused). [2026-07-25 late] CAMERA-ARM RE-FLY FLOWN + SCORED on the fixed terminal: the zero-command downgrades RESOLVE to a CONFIRMATION (no camera earn at 0/5/15 deg aim error; at correct aim now ~parity with the dash, not a liability; LOS-lag comp confirmed NEGATIVE; audit (e) 0/674 zero-command ticks). The last desk-side field-day gap also closed: the per-frame incidence-RATE producer is BUILT (range_truth_join --incidence --mount-index-deg; protocol 4.2b's decode-rate-vs-(range x incidence) row is now reachable; producer->consumer contract test; two consumer defects found+fixed: partial-denominator cells, silent mount-index contradiction). [2026-07-25 T25] The T25 demo is RE-RENDERED with the ram-radius + ADR-0066 claims pass (net-class R named as such, ram 0.35 m at both caption sites, comms-denied HELD, hardened n=72 CI claim on the end card, billboard-stand-in disclosure; captions verified in-pixels; recipe scripts/video/t25_render_plan.md). Found en route + fixed: ADR-0064's median was median_high (1.784 -> corrected 1.752; Pk/CI table unaffected) and kill_mechanism.md §7 still recommended the retired 0.5 m. CAVEAT: the separate onboard reel + 2 tracked stills still bake the old un-named 1.5 m caption and are un-re-renderable (hero CSV rotated off disk) -- do not publish those three as ram kills (demo_out/README.md).

## headline.next_probe (verbatim)

RE-FLY the coded-dash camera arms (frozen_vworld fixed 2026-07-25 — the terminal now actually commands horizontal velocity; the prior 'camera adds ~nothing / camera can't defend aim' verdicts were partly measured through the zero-command latch and are DOWNGRADED to PENDING RE-FLY). Then the physical arc: the tripod AprilTag afternoon (curve-a decode envelope = the $740 money gate; all desk P0s now closed + print artifacts ready) → build + bench (FAILSAFE 7 + the P4 gates) → the kill ladder (docs/kill_day_protocol.md).
