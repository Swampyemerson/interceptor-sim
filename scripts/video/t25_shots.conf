# T25 shot list (docs/t25_storyboard.md) -- consumed by scripts/video/assemble_t25.sh.
# Format:  id|type|source|opts
#   type=card : PNG still, opts dur=SECONDS
#   type=clip : .mp4 file OR a directory of frame_%06d.png, opts speed=F
#               (speed != 1 => assemble_t25.sh burns the "SLOW MOTION (retimed)"
#                watermark; prefer baking slow-mo in hud_overlay.py instead)
# Relative paths resolve against the repo root. Missing sources are SKIPPED
# with a warning so a partial cut can be previewed while renders are pending.
# Shot->source provenance + the exact render command for each pending clip:
# scripts/video/t25_render_plan.md
#
# READY TODAY (offline): shots 0 and 8 -- run scripts/video/make_t25_cards.py.
# PENDING SIM RENDERS:   shots 1-7 (see the render plan).

0|card|demo_out/t25/cards/shot0_title.png|dur=4
1|clip|demo_out/t25/shot1_ground_stereo.mp4|
2|clip|demo_out/t25/shot2_track_cue_up.mp4|
3|clip|demo_out/t25/shot3_launch_dash.mp4|
4|clip|demo_out/t25/shot4_terminal_acquire.mp4|
5|clip|demo_out/t25/shot5_handoff.mp4|
6|clip|demo_out/t25/shot6_terminal_slowmo.mp4|
7|clip|demo_out/t25/shot7_chase.mp4|
8|card|demo_out/t25/cards/shot8_endcard.png|dur=6
