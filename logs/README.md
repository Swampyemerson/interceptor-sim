# logs/ — committed evidence, not scratch

Every file tracked here is a small aggregate CSV/JSON that a published number
traces to (audit finding DEEP-H1: committed so the evidence survives disk
loss). Bulk per-run telemetry is gitignored and regenerable. If a number is
quoted anywhere in this repo, the provenance pointer leads either here, to a
gate script, or to a written derivation.
