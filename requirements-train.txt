# TRAINING VENV (.venv-seeker-train / .venv-seeker-train-gpu) -- YOLO fine-tunes.
#
# HEAVY (~3 GB with CUDA wheels) and needed by NO gate: nothing in
# scripts/run_tests.sh or .github/workflows/ci.yml imports ultralytics or torch.
# Install only to retrain or re-export a detector.
#
#   CPU:  python3 -m venv .venv-seeker-train
#         .venv-seeker-train/bin/pip install -r requirements-train.txt
#   GPU:  python3 -m venv .venv-seeker-train-gpu
#         .venv-seeker-train-gpu/bin/pip install \
#             --index-url https://download.pytorch.org/whl/cu126 torch torchvision
#         .venv-seeker-train-gpu/bin/pip install -r requirements-train.txt
#
# The GPU venv is ~23x the CPU one on this RTX 4070 (memory: gpu-training-unlocked).
# WSL2 trap: there is no hard OOM -- a batch over ~8 GB silently spills to RAM and
# runs ~4x slower. Keep batch 32 / imgsz 640, one training run at a time.
#
# Pinned to `.venv-seeker-train/bin/pip freeze --local`, verified 2026-07-25.
# The torch lines are the CPU builds; the GPU venv holds the matching +cu126
# wheels, which pip must fetch from the PyTorch index (see above).
ultralytics==8.4.90
ultralytics-thop==2.0.20
torch==2.12.1
torchvision==0.27.1
onnx==1.22.0        # .pt -> .onnx export
onnxslim==0.1.94    # ultralytics' export-time graph simplifier
onnxruntime==1.27.0 # export smoke-tests (scripts/seeker/nn_tier/onnx_smoke_test*.py)
numpy==2.4.4        # NOTE: ultralytics pins older than the main venv's 2.5.1
opencv-python==5.0.0.93
