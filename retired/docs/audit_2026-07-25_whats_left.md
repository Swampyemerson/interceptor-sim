# What's-left audit — 2026-07-25 (8-agent workflow, Opus 5)

> Produced by workflow `wf_4dd90290-67e` (6 mappers + completeness/priority critics, 106 raw items).
> Snapshot at HEAD `fad4c11`. This is an AUDIT, not the contract — fold accepted items into
> `docs/project_state.json` + docs/next.md as they are actioned. Known map errata (per the
> completeness critic): reader 1's crossing_sign item is WRONG (fixed in 1911ce9); CI red-count
> discrepancies resolved live to 73/73 failures.


## Reader 1 — State contract (project_state.json)

### [open] Contract is 2 commits stale: the Wave-2 field-tools + tripod-protocol tranche never landed in project_state.json
- **Priority:** P0 — the canonical state under-reports the one campaign (tripod day) that is next and that gates ~$740 of spend
- **Evidence:** git log --oneline -6 -- docs/project_state.json (newest = 0a41fd9; HEAD = eb748e1, prior f3a3bca); project_state.json:updated_by head "Remaining: field-tools+protocol (in flight)"; python3 dump of all 10 stage changelogs shows max date 2026-07-24

`git log -- docs/project_state.json` ends at 0a41fd9, but two later commits shipped substantive work: f3a3bca (Wave-2 field tools + protocol: 11 files, +1063 lines across scripts/field/ and a 378-line tripod_test_protocol.md overhaul) and eb748e1 (tripod_score --help crash fix). The contract's `updated_by` still describes field-tools+protocol as "(in flight)". Per the project's own rule ("an update that isn't in the contract didn't happen"), the tripod day's readiness is currently under-reported by the canonical surface. Also: NO stage changelog anywhere carries a 2026-07-25 entry, so the entire review-2 fix tranche (flight-code blockers, evidence-chain fixes, prevention infra) is visible only in the `updated_by` prose blob and not on the per-stage cards the dashboard renders.

### [builder-decision] RAM-RADIUS 0.36 m correction is raised but NOT ratified — and it is the number that decides the Tier-2 order
- **Priority:** P0 — a one-way ~$510 spend hangs on it and the ruling may change the airframe class
- **Evidence:** docs/project_state.json:616 (stages[6].note); mirrored in docs/next.md "builder DECISIONS: … #17 ram-radius/Tier-2"

stages[6] (kill) carries "⚠️ RAM-RADIUS CORRECTION … RAISED TO THE BUILDER, not yet ratified": the 0.5 m ram radius quoted project-wide was a 7-inch number; both ordered aircraft are 5-inch, so max tip-to-tip contact is ~0.36 m. Against that bar the best measured batch scores 4/96 @0.36 m and 0/96 @0.25 m, while the open-loop dash floor is ~0.71 m (0.43 m after ADR-0083's +5° trim). The stage itself says this "should be settled BEFORE the Tier-2 order" and that the only physical widener is a bigger interceptor. Until the builder rules, the P2 money gate's downstream logic (buy the 5-inch Tier-2 kit) is proceeding on an unratified premise.

### [open] The contract's entire first-kill premise ("first kills fly the AprilTag on Pi 5 CPU") rests on a seeker that does not exist in flight code
- **Priority:** P0 — the money gate certifies a capability whose flight-side implementation is unbuilt, so a PASS at the tripod would be vacuous
- **Evidence:** grep -rln 'pyapriltags|pupil_apriltags' *.py → no hits under flight/; flight/deploy/seeker_loop.py:13-22 ("DEFAULT WEIGHTS = the REAL-DATA n-mono model"); docs/project_state.json constraints[0] pi5-compute; bom_tiers[1].gate

constraint `pi5-compute`, constraint `honesty-boundary` ("staged baseline seeker"), stages[8].note and the bom_tiers Tier-2 gate all assert that the first real kills fly an AprilTag baseline seeker on the Pi 5 CPU, and curve (a) of the money gate is exactly that seeker's decode envelope. But `flight/deploy/seeker_loop.py` and `flight/deploy/real_flight.py` contain zero AprilTag code (grep for apriltag/pyapriltags across flight/deploy/ returns nothing); the deploy loop's default weights are the NN (n-mono.onnx). Tag decode exists only in offline/sim tooling (scripts/apriltag_detector.py, scripts/seeker/tripod_score.py). real_flight takes a `detector=` argument so the socket exists, but nothing fills it with a tag detection box.

### [open] §0 narrative big_lever body still asserts the REFUTED "camera defends imperfect aim" and a "pending" GL test that already returned NULL
- **Priority:** P0 — a refuted conclusion is live on the hosted artifact, the surface the builder and outside readers actually read
- **Evidence:** docs/project_state.json:74 (narrative.big_lever.text, points 2 and 3); dashboard.html:811 renders it verbatim; contradicted by narrative.big_lever.headline and stages[5].changelog 2026-07-24 GL entry; `python3 scripts/render_dashboard.py --check` prints "prose wording NOT checked"

The headline of narrative.big_lever was corrected on 2026-07-24 ("the camera does NOT defend imperfect aim: it lost to its own control at 0°, 5° AND 15°"), but the body text directly under it still reads "(2) the camera defends imperfect aim; (3) the LAG knob on the level arm (GL, pending) is the last shot…". GL was flown and is a NULL (1.44 vs 1.32, 5/8), recorded in the terminal changelog the same day. This is the exact failure mode CLAUDE.md's "sweep the always-visible surfaces the SAME turn" rule exists to stop, and it survives because render_dashboard's drift guard checks NUMBERS only, not prose (its own output says "prose wording NOT checked").

### [open] The §0 four-phase program block is a superseded roadmap presented as the forward plan
- **Priority:** P1 — the always-visible roadmap points at a measured dead end; cheap to fix, expensive if acted on
- **Evidence:** docs/project_state.json narrative.program.phases[0] (tone "next", test "NEXT: … a dash-only control arm") vs narrative.levers.rows[2] ("⛔ NOT a do-first lever"); dashboard.html:874,877; docs/next.md SETTLED CONCLUSIONS §3 + "fov_guidance.py stays parked"

narrative.program still leads with Phase A (accel-capped dash + loft + modest wedge) at tone "next", with a test line saying "NEXT: lighter cap / loft-only … a dash-only control arm to settle whether the intercept is camera-driven". Both are done and settled: the dash-only controls were flown 2026-07-24 (Adash/Bdash/G20dash) and the same contract's own levers table now says that lever is "⛔ NOT a do-first lever … Keep the dash LEVEL and uncapped". Phases B/C/D are shown as "GATED ON A" while docs/next.md records Phase B as parked ("flight/fov_guidance.py stays parked — Phase B FOV-hold was the wrong lever"). A reader of §0 is handed a plan the project has already abandoned.

### [open] The single OPEN contradiction (wedge-sizing-vs-accel-cap) has a resolution path that no longer exists
- **Priority:** P1 — it gates the printable wedge angle, which is a P3/P4 build step and a hard bench gate
- **Evidence:** docs/project_state.json contradictions[0] (status open, resolution text); narrative.levers.rows[2] at :191 references "the wedge-vs-level ledger entry" — grep for 'wedge-vs-level' returns exactly 1 hit, that dangling reference; constraints[9] forward-camera-boom (~11° level margin)

The ledger entry's resolution reads "the serialized Gazebo confirmation of the accel-capped dash settles the joint accel-cap + wedge sizing". That Gazebo work ran, and the verdict killed the accel-cap (net-negative at correct aim: EAL 1.05 vs uncapped 0.71; framed CG 1.92 vs level 1.32). With the cap abandoned, claim_b's premise ("sized to the CAPPED ~20° pitch") is void and the wedge reverts to being sized against the uncapped ~35-40° dash pitch — which re-opens the prop-clearance collision (the flush mount clears ~11°). The contradiction is therefore neither resolved nor correctly stated; it now needs re-derivation as "wedge angle vs prop clearance on an uncapped dash", and the narrative already gestures at a "wedge-vs-level ledger entry" that does not exist in the ledger.

### [open] Two stages contradict their own changelogs: kill says field_score is "planned, not built"; real_data says the IoU validation "has NOT run"
- **Priority:** P1 — stale notes are how the "NOTHING ORDERED YET" miss happened; same class, already once burned
- **Evidence:** docs/project_state.json:616 vs stages[6].changelog[1..2]; docs/project_state.json:649 vs stages[7].changelog 2026-07-21; scripts/field_score.py:282 (load_track_from_bin) confirms the .BIN parser exists

stages[6].note opens "field_score.py (CPA from dual GPS logs) is planned, not built" while stages[6].changelog 2026-07-18 says "scripts/field_score.py built" and 2026-07-20 adds ArduPilot .BIN support (verified in code at scripts/field_score.py:282 load_track_from_bin). stages[7].note says "the tag-box-vs-gt-box IoU-vs-range validation in the apriltag world has NOT run" while stages[7].changelog 2026-07-21 says "P0.2 CLOSED — auto-labeler VALIDATED … IoU mean 0.965 / min 0.951". Both are self-contradictions inside a single stage object, invisible to the drift check (which validates JSON structure and numbers, not internal prose consistency), and both under-report completed work.

### [open] FAA / Remote-ID status is contradictory across three places in the contract, and the specifics were never captured
- **Priority:** P1 — a legal blocker with no data workaround; the written-permission question for a deliberate mid-air is the riskiest unrecorded item
- **Evidence:** docs/project_state.json:1589 (build_tab ground, "STILL OPEN"), :1082 (bom_tiers field layer, "FAA reg + Remote-ID/FRIA decision still open") vs stages[8].note ("BUILDER UNBLOCKED 2026-07-24")

stages[8].note says "BUILDER UNBLOCKED 2026-07-24 … he HAS the regulatory basis AND a site", but bom_tiers[0] field-layer note still reads "FAA reg + Remote-ID/FRIA decision still open" and build_tab ground says "STILL OPEN: the field choice — FRIA field ($0) vs a broadcast Remote-ID module" with gnd-05 flagged as a hard gate. The stage note itself concedes the specifics — which regime, which site, and whether a deliberate two-aircraft mid-air is permitted IN WRITING — are "not yet captured here". The kill-day venue question is explicitly separate from the tripod-day one and is unrecorded.

### [open] bom_tiers must-add lists are stale against build_tab: items marked STILL MISSING are recorded as OWNED elsewhere, and the real gap list is buried
- **Priority:** P1 — one missing microSD silently voids the target track and therefore every scored pass on field day
- **Evidence:** docs/project_state.json:1076 (bom_tiers must-add build layer) vs :1565 (build_tab ground, "iron + zip ties OWNED"); :1082 vs build_tab field-layer note

bom_tiers[0]'s must-add build layer says "STILL MISSING: the soldering IRON itself … zip ties, multimeter if unowned", but build_tab ground parts record "iron + zip ties OWNED (builder confirmed)" and "prop wrench + multimeter OWNED". The genuinely missing items are scattered across two blobs: Kakute microSD (no card = no target log = no scored pass), fireproof charge bag + ANSI Z87 glasses + fire blanket, lost-model buzzer, Pi 5 active cooler, ≥27 W USB-C PD power bank, tripod + ball head + inclinometer, cones + tape measure — plus the unresolved "re-source Pi + SD to recover ~$120". A shopping list a builder acts on should not need cross-referencing two prose blobs.

### [open] build_plan P0 task strings still carry "not yet run" / "still open" parentheticals for work its own summary marks DONE; the schema has no per-task status
- **Priority:** P1 — a checklist that lies about its own completion is the project's documented failure mode, and the fix is a schema field plus one sweep
- **Evidence:** docs/project_state.json build_plan.phases[0].tasks[1] and [3] vs build_plan.summary "**P0 SOFTWARE IS CLOSED.**"; no 'status' key on any element of build_plan.phases[*].tasks or build_tab.subsystems[*].steps

build_plan.phases[0].tasks lists 8 items whose text still says e.g. "Validate the auto-labeler … (real_data_pipeline.md: not yet run)" and "Plumbing: link --cam-fwd-offset-m … (terminal changelog: still open)", while the same phase's summary says "P0 SOFTWARE IS CLOSED" and docs/next.md marks P0.2 and P0.4 done with commits (b0a947e, 4272fdf). Structurally, build_plan tasks and build_tab steps have no `status`/`done` field at all, so "which phase items are incomplete" is only recoverable by parsing a 3,000-character summary blob — the exact shape that lets a stale item sit unnoticed.

### [open] detector stage's `active` version names the sim model while the hardware default is now the real-data n-mono model
- **Priority:** P1 — the active-version field is the single thing the orientation ritual trusts, and it disagrees with the deployed default
- **Evidence:** docs/project_state.json:423 (stages[2].active) vs flight/deploy/seeker_loop.py:13-22; key_numbers[6] ("Real-data NN vs deployed sim-NN … 44.2% vs 1.1%")

stages[2].active reads "drone_finetuned_quad_v2.onnx @640, conf 0.25", but flight/deploy/seeker_loop.py's header states the DEFAULT WEIGHTS are scripts/seeker/weights/nn_tier/n-mono.onnx and that quad_v2 "must never be the default on hardware" (AP50 0.0003 / recall 1.1% / false-fire 88.5% on real held-out imagery). The n-mono model, the GPU training pipeline and the 15,391-image real-media corpus land only in the real_data stage changelog and one key_number — the detector stage's active-version field, the field a fresh session reads to know what flies, is wrong for the hardware lane. The stage also needs to record that n-mono is validated on PUBLIC held-out media only; the held-out-FLIGHT gate on our target/camera/site is still the tripod day.

### [open] forward-camera-boom constraint: 0 of its 5 mount checks have physically run; only check (3)'s software half is done
- **Priority:** P1 — check (1) is the declared HARD gate before any props-on flight and it caps the printable wedge angle
- **Evidence:** docs/project_state.json constraints[9] (forward-camera-boom, 5-check list) vs build_plan.phases[3] (P4 bench gates, all unrun) and docs/next.md P0.4 DONE (4272fdf)

The constraint is written as a 5-check pre-flight list. Status by check: (1) tilt/clearance corner-spin at LOADED throttle — not run, no airframe (build_plan P4 hard gate); (2) boom vibration / bending mode in the 40-150 Hz prop band — not measured, no design analysis in the repo; (3) lever-arm — software half DONE (flight.geometry.camera_to_cg_los + P0.4 model-swap guard, commit 4272fdf), but the mount constant is a TODO-BUILDER; (4) sacrificial camera/boom spares — a BOM line, no breakaway design recorded; (5) CSI-ribbon frame-arrival logging separate from detection rate — build_plan P4 task, unbuilt. The constraint text also still says the lever-arm flag "LINK the offset flag to the model swap" is to-do, which P0.4 closed.

### [open] Contradiction-ledger integrity is unenforced: a ledger entry can flip open→resolved with no resolution note and --check stays green
- **Priority:** P1 — ~15 minutes of work on the one mechanism that stops a false 'resolved', and the review already wrote the patch
- **Evidence:** grep -n 'verdict|resolution' scripts/render_dashboard.py → no hits; tests/test_dashboard_guard.py test names (6, none covering contradiction schema); docs/review2_silent_failure_findings.md (render_dashboard.py:70 vs project_state_readme.md:117-123 finding)

docs/project_state_readme.md documents `verdict` and `resolution` as part of the contradiction schema, but scripts/render_dashboard.py's CONTRA_KEYS omits both, so neither is validated. Review 2 measured this (a mutation flipping status to resolved with `resolution` deleted passed) and specified the two-line conditional fix plus a unit test; the Wave-1 prevention tranche landed the number-traceability guard, the --check --artifact loud failure and the artifact_url required key (tests/test_dashboard_guard.py, 6 tests) but NOT these two assertions. The ledger — the project's guard against a resolved-but-not-actually-fixed claim — is currently honest "only by discipline".

### [open] key_numbers leads with billboard-era headline numbers and omits the two most consequential current ones
- **Priority:** P1 — the tiles are the dashboard's most-read surface and currently flatter a superseded regime
- **Evidence:** docs/project_state.json key_numbers[0..6]; the 0.36 m / 4-of-96 / 0.71 m figures live only in stages[6].note at :616; 0.43 m lives only in stages[0].changelog 2026-07-24 (ADR-0083)

The 7 key_numbers tiles open with "Pk@2.8 m, weave 12 m/s = 95.0%" (billboard-era, ADR-0064) and "M5 median miss = 0.93 m" (AprilTag/billboard era). Absent entirely: the ~0.36 m 5-inch ram envelope that stages[6] calls "the single most consequential number in the project", the 4/96 @0.36 m best-measured batch score against it, and ADR-0083's 0.43 m aim-corrected open-loop dash floor (the current best real-world-relevant miss). Also stale in context: "In-flight approach recall 0.8% — THE WALL" now has a measured counterpart (8-12 m recall 35.4% under the Phase-A pointing lever) that appears nowhere in the tiles.

### [open] Handoff hardening is unbuilt, and the hardware executor is missing the BREAKOFF magnitude check its sim twin has
- **Priority:** P1 — an un-ported guard on the BREAKOFF path is a flight-safety divergence, and it is invisible in the contract
- **Evidence:** docs/project_state.json:515 (stages[4].active); flight/deploy/real_flight.py:409-415

stages[4].active says the planned streak range-consistency hardening is "(unbuilt) … untested — a phantom-only streak is self-consistent at ~1.5 m, so suspect", with the range-window variant already NULLed and retired. Separately and not in the contract at all: flight/deploy/real_flight.py:409-415 `update_recede_streak` mirrors scripts/m4_intercept.update_range_increase_streak "minus its streak_start_range magnitude check (TODO-BUILDER: port that check before a real terminal flies -- it is what stops a slow drift from firing breakoff)". That is a live safety divergence between the sim-validated logic and the code that will fly.

### [open] Terminal stage's own BUILD CONSEQUENCE (gate/shadow markerless ENGAGE) has no implementation and no decision entry
- **Priority:** P1 — it decides what the first real ENGAGE is allowed to trust, and a directive buried in prose is how decisions evaporate
- **Evidence:** docs/project_state.json stages[5].note (final "BUILD CONSEQUENCE" sentence); grep -rn 'shadow' flight/deploy/*.py → no hits; build_plan.phases[5].tasks[2] ("shadow-log the NN alongside the tag")

stages[5].note concludes: "ENGAGE on the markerless seeker is not justified by any sim evidence — gate it to the favourable aspect, run it in SHADOW until real data earns it, or hold it behind the tag baseline." None of the three options is implemented (grep for 'shadow' in flight/deploy/*.py returns nothing; there is no aspect gate on ENGAGE; and per item 3 the tag baseline does not exist in flight code either), and the choice is not recorded as a decision object with options/chosen — it sits as a sentence at the end of a very long note. Shadow-logging exists only as a field-day scoring task (build_plan P5) and as tripod curve (b).

### [builder-decision] Review-2 residue tracked only in the updated_by prose blob: 3 more builder decisions + 10 unverified findings
- **Priority:** P1 — #22 (post-GO OFFBOARD loss) and the desk-fps question both bear on flight safety and on the money gate's inputs
- **Evidence:** python3 walk of project_state.json: 'altitude-datum' → ['.updated_by'], 'OFFBOARD-loss' → ['.updated_by'], 'desk-session' → ['.updated_by']; docs/review2_silent_failure_findings.md:12; docs/next.md ⬜ REMAINING block

`updated_by` closes with "Remaining: … the 10 unverified findings, builder decisions (#16 vertical/altitude-datum, #17 ram-radius/Tier-2, #22 OFFBOARD-loss, desk-session-fps)". A walk of the JSON shows 'altitude-datum', 'OFFBOARD-loss' and 'desk-session' each appear in exactly ONE place — updated_by — so three pending builder rulings and a 10-item re-verification queue have no first-class representation (no decision object, no contradiction, no stage note) and will not render as open items on the dashboard. The 10 findings were left unverified because review 2 hit a session limit and the doc says they "must be re-verified before being acted on".

### [open] build_tab placard note still says P0.2 "has NOT run yet" three lines above the step that quotes its answer
- **Priority:** P1 — a wrong or deferred placard print silently invalidates curve (a), the ~$740 gate
- **Evidence:** docs/project_state.json:1241 (parts note) vs build_tab.subsystems[0].steps tgt-08; bom_tiers[0] placard row already says 0.35 m

build_tab.subsystems[target].parts places the AprilTag placard at "SIZE TBD BY P0.2 — the apriltag-world auto-labeler validation that sizes it has NOT run yet … fall back to the ~0.3 m BOM default only if P0.2 is still unrun by field day", while step tgt-08 in the same subsystem says "Placard SIZE = 0.35 m edge (P0.2, 2026-07-21 — docs/placard_sizing.md); print tag36h11 id 0 at that edge". A builder reading the parts card would print the wrong size or defer the print; 0.35 m was measured as the ONLY size clearing the t_go money gate under realistic OV9281 scaling.

### [improvement-idea] Physical build progress is recorded only in browser localStorage — nothing in the contract or git knows what has been built
- **Priority:** P2 — no current claim depends on it, but it is the highest-value schema addition for the phase the project is entering
- **Evidence:** docs/dashboard.html:4022-4024 (var LSP = "buildtab:"; lsGet/lsSet), :4052 (checkbox render), :4116 (lsSet on change); no 'done' key on any build_tab step in project_state.json

The build_tab ladder says "Boxes are checked in the subsystem cards below — these counts follow them", but the checkboxes persist via `localStorage.getItem('buildtab:'+id)` in dashboard.html. That state is per-browser, absent from project_state.json, absent from git, and absent from the hosted artifact on any other device. So the one thing the builder is physically doing right now (B0-B3: target build, seeker rig, brain bench) cannot be audited by a fresh session, and the ladder's "counts" are unverifiable. Adding a `done`/`done_date` field per step (and having the dashboard seed from it) would make bench progress part of the contract.

### [stale/stranded] The portfolio deliverable (T25 demo video / README results) is essentially unmodeled in the contract, and its assets predate the retractions
- **Priority:** P2 — nothing downstream blocks on it, but it is the project's stated deliverable and the assets are now actively misleading
- **Evidence:** python3 walk: 'T25' → ['.headline.next_probe'] only; ls -la demo_out/t25/ (all assets 2026-07-10/11); git ls-files demo_out | wc -l → 1

docs/goals.md makes the portfolio the point (M5: README with architecture diagram, results, one demo GIF), and the builder's standing note requires the T25 video to feature tilt + the best tracking + a great intercept clip. In the contract, "T25" appears exactly once — inside headline.next_probe as "NEXT sim work: T25 video / demo captures". There is no stage, no build_plan phase and no decision tracking it. Meanwhile demo_out/t25/ assets are dated 2026-07-10/11 (cue-era, pre-coded-dash-pivot) and are untracked by git (git ls-files demo_out → 1 file, a README), so they sell claims the project has since retracted (camera-guided sub-meter 3D-quad intercept, the cue/handoff stack, guessed +15° tilt).

### [open] headline block is dated 2026-07-17 and its next_probe still points at work that is done or superseded
- **Priority:** P2 — no decision hangs on it, but it is the top-of-page orientation text and it is 8 days behind
- **Evidence:** docs/project_state.json headline.date "2026-07-17" vs narrative.as_of "2026-07-24" vs updated "2026-07-25"; dashboard.html:756 renders the stale next_probe

headline.date is 2026-07-17 while narrative.as_of is 2026-07-24 and `updated` is 2026-07-25. Its next_probe closes with "NEXT sim work: T25 video / demo captures. Real work: tripod day." — written before the 2026-07-22/24 Gazebo A/B, the anti-mirage decomposition, ADR-0080/0083, the aim-error refutation and the two full reviews. The §0 headline is the first thing the orientation ritual and any outside reader see; it should carry the current binding wall (build/bench/tripod + real-data detection under an unratified 0.36 m bar), not the 2026-07-17 framing.

### [open] Known latent flight-path bug (crossing_sign float dust) exists in docs/next.md but nowhere in the contract
- **Priority:** P2 — sim-only today and low-probability geometry, but it sits under the one lever every current miss number depends on
- **Evidence:** grep 'crossing_sign|float-dust' docs/project_state.json → 0 hits; flight/guidance.py:377-407 (dead-band + "that flown code path is deliberately left untouched"); flight/deploy/real_flight.py:223 (manual latch_lead_correction_deg)

docs/next.md lists "crossing_sign float-dust latent bug (task 11, LOW urgency) — the flown inline --dash-crossing-bias-deg key returns ±1 on near-head-on off float dust (could apply full ±30° on head-on)". Verified: flight/guidance.py:377 has the fixed version with a relative dead-band and its docstring explicitly says the m4 inline path "is deliberately left untouched (byte-identity)", so the bug stands in the flown sim path. Searching the contract for 'crossing_sign' or 'float-dust' returns zero hits. Scope is honest — the hardware executor uses a hand-signed latch_lead_correction_deg, not the auto key — but the aim constant IS the project's dominant proven lever, so a sign bug in its sim derivation path belongs in the contract.

### [in-flight] Standing external-verdict constraints still open and correctly gated on the tripod day (no action, but they are the live unknowns)
- **Priority:** P2 — no contract edit needed; flagged so the enumeration is complete and these are not double-counted as drift
- **Evidence:** docs/project_state.json constraints[6] transfer-bet, constraints[1] pi5-emulation-gap, constraints[2] wide-fov; build_tab steps skr-06/skr-07; build_plan.phases[2] (P2 tripod)

Three constraints remain genuinely unresolved and all resolve in one session: `transfer-bet` (smoke read says NOT-DEAD but per-frame discrimination is ~zero; the deciding variable is the 5-streak handoff gate on real clutter, measurable only by curve (b)); `pi5-emulation-gap` / `pi5-compute` (real AprilTag and CPU-YOLO fps cannot be pre-measured — QEMU proved the stack loads, not the throughput; this is why the Pi 5 is a Tier-1 buy); `wide-fov` (118° paper-verified, but the delivered adjustable lens must be re-measured at calibration, build_tab skr-06). Listing them so they are not mistaken for slack: they are correctly sequenced, not neglected.

**Reader notes:** SCOPE / METHOD. Digested docs/project_state.json (259 KB, schema v2.4) with targeted python3 json walks — never read whole. Cross-checked every claim against code (flight/, scripts/), git log, and `python3 scripts/render_dashboard.py --check` (EXIT=0, "20 visible numbers traced ... 37 contradictions [1 open]"). Graveyard (18 entries) and the 36 resolved ledger entries were read first; nothing dead is resurrected here — notably NOT proposed: adaptive tilt, v3/rebal retrains, range-plausibility gates, subpixel bearing, APN/PIP, foveated crop, RTK, sim pivot, narrow lens, bank-as-accel.

STAGE STATUS CENSUS: 10 stages — 3 implemented (launch_aim, coded_dash, sim_harness), 7 half-done (detector, pointing, handoff, terminal, kill, real_data, hardware). ZERO stages carry status "idea" or "rejected", so slice item (a) reduces to the 7 half-done stages; each is covered above (detector→item 11, pointing→items 6+12, handoff→item 15, terminal→items 4/5/16, kill→items 2+7, real_data→item 7, hardware→items 8/9/12).

CONTRADICTION LEDGER: exactly 1 open (wedge-sizing-vs-accel-cap, item 6) out of 37. But I found four UNLOGGED internal contradictions that the ledger does not carry and the drift check cannot see, because --check validates structure and numbers only, not prose: kill-stage note vs its own changelog, real_data note vs its own changelog, FAA status across three blocks, and the must-add lists vs build_tab ownership (items 7, 8, 9). Plus one dangling cross-reference to a "wedge-vs-level ledger entry" that was never created.

THE STRONGEST SIGNAL. Three of the top four findings are the same shape and it is the shape CLAUDE.md's newest standing rule targets: the contract's PROSE layer has drifted behind its own changelogs and behind the code, while every mechanical gate stays green. The number-traceability guard added 2026-07-25 catches an orphan NUMBER; it structurally cannot catch "the camera defends imperfect aim" (no digits) or a stage note asserting a built tool is unbuilt. If one improvement is worth building from this slice, it is a prose-level assertion check — e.g. a small set of pinned claim strings the contract must NOT contain once a verdict flips, or a per-stage `note_as_of` date validated against max(changelog.date).

MOST CONSEQUENTIAL SINGLE ITEM: #3 (no AprilTag seeker in flight code). The tripod money gate, the Tier-2 order logic, constraint pi5-compute and the hardware stage all rest on a "staged first-kill baseline seeker" that exists only in offline tooling. A PASS on curve (a) would certify an envelope for software nobody has written — precisely the vacuous-verdict class the project just spent two reviews eliminating.


## Reader 2 — Work queue (docs/next.md / docs/progress.md)

### [open] BLOCKER-class, unfixed: the coded-dash camera terminal commands ZERO horizontal velocity — the sweep verdicts the contract publishes were measured on a terminal that never steered
- **Priority:** P0 — an unfixed measurement/control defect that invalidates the session's headline sim verdict, which is currently published on the dashboard/Artifact as settled
- **Evidence:** docs/review2_silent_failure_findings.md:449-462 (finding + per-arm counts); code unfixed at ~/interceptor-sim/scripts/m4_intercept.py:2529 (`vh0 = vh1 = 0.0`) and :3703-3709 (`frozen_vworld = (vh0, vh1)`); `git log -3 -- scripts/m4_intercept.py` -> f56f9dc (pre-review); `grep -c frozen_vworld docs/project_state.json docs/next.md` -> 0, 0

Review-2 finding (HIGH) verified that `frozen_vworld` latches the never-assigned module-scope `vh0 = vh1 = 0.0` whenever ENGAGE starts inside TERMINAL_FREEZE_RANGE_M (the markerless r_hat ~1.5-3.5 m at handoff guarantees this), so the whole ENGAGE phase re-issues cmd=(0,0,v_down,yaw): measured 7/7 engaged flights of mc_fp_armG20_line9_s123, 7/8 of s777, 6/8 of AE5, 4/7 of AE15. The code is UNCHANGED (m4_intercept.py last touched f56f9dc, before the review commit f0dcd64), and neither docs/project_state.json nor docs/next.md mentions it (grep 'frozen_vworld' = 0 hits in both) — so the contract still asserts "the camera terminal NEVER earns its handoff … REFUTED IN SIM" and "G20 1.02 vs 0.75 m" as settled. Fix is a port from flight/deploy/seeker_loop.py:311-315 (one code path, sim==hardware), plus a loud <0.5 m/s latch warning and an audit FAIL on identically-zero pre-CPA ENGAGE rows; then re-fly ONLY the camera arms (G20-s123, G20-s777, AE5-s123, AE15-s123, GL-s777, ~40 flights, dash-only twins reused so pairing survives). ADR-0083's +5 deg aim trim is measured on dash-only arms and is NOT affected.

### [builder-decision] Builder decision #17 (ram radius 0.36 m vs 0.5 m) is unratified and gates the ~$740 Tier-2 order
- **Priority:** P0 — one-way-door money decision sitting directly upstream of the Tier-2 purchase
- **Evidence:** docs/project_state.json stage `kill` note: "RAM-RADIUS CORRECTION (full review 2026-07-24, RAISED TO THE BUILDER, not yet ratified) … ~0.36 m … the single most consequential number in the project"; docs/next.md:17 lists it as builder decision #17

The contract's `kill` stage records the correction as "RAISED TO THE BUILDER, not yet ratified": the 0.5 m ram radius was derived for a 7-inch airframe, both ordered aircraft are 5-inch, so the true tip-to-tip contact envelope is ~0.36 m — ~10% tighter. Against 0.36 m the best measured batch scores 4/96 and the open-loop dash floor is ~0.71 m, so the ratification changes whether the camera terminal is decoration or a must-earn-a-metre requirement. The stage note itself says it "should be settled BEFORE the Tier-2 order", and the money gate that authorizes that order is the next physical milestone.

### [open] `--quad-decimate` is prescribed by ADR-0082 and the tripod protocol but implemented nowhere — the no-second-field-day reclaim lever cannot be run
- **Priority:** P0 — must land before the single field day or the gate's only reclaim lever is unavailable
- **Evidence:** docs/tripod_test_protocol.md:236-250 (⛔ NOT EXECUTABLE WITH THE SHIPPED TOOLS, verified 2026-07-25); docs/decisions.md ADR-0082 ("quad_decimate=1.0 … promoted … to the PLANNED tripod-day capture setting"); `grep -rn quad_decimate scripts/seeker/*.py` shows only prose/notes, no argument plumbing; escalated in commit f3a3bca's own message

ADR-0082 promoted quad_decimate=1.0 from "reclaim lever" to the PLANNED tripod-day capture setting (it is the only config clearing the money gate at 20 and 14 fps; qd=2.0 clears only at 30 fps), but every decode path in the repo constructs Detector(families="tag36h11") with no quad_decimate argument and no CLI flag, so all of them silently run the pupil-apriltags default 2.0 and meta.json does not record which value produced a session. The protocol already carries a ⛔ NOT EXECUTABLE banner saying to add it BEFORE the field day or strike the bullet. Desk-cheap: `--quad-decimate` on pi_capture + tripod_score (+ autolabel), recorded into meta.json.

### [open] range_truth_join's explicit `--clock-offset-s` path has ZERO integrity checks — and the wave-2 protocol now routes the field day down exactly that path
- **Priority:** P0 — a wrong-but-confident range truth silently decides the $740 gate, on the path the protocol now prescribes
- **Evidence:** docs/review2_silent_failure_findings.md:601-615; `git log -5 -- scripts/seeker/range_truth_join.py` -> only 129ed45; scripts/seeker/range_truth_join.py:172,565,570 unchanged; docs/tripod_test_protocol.md:463-531 ("--auto-sync is a CROSS-CHECK, not the plan")

Verified HIGH finding: every residual/bias/identifiability test lives inside `if do_auto_sync:`, so the explicit-offset path silently accepts a 1.1 km survey error, an AMSL-vs-AGL altitude datum blunder (med_up ~106 m slips under the 150 m warn-only guard), or a 3 s clock error — all reproduced through the module's own fixture, each exiting 0 with empty warnings. The file has NOT been touched since 129ed45 (pre-review): IMPLAUSIBLE_UP_M is still 150.0 at :172 and still only sets `warn_alt` at :565. Wave-2 made this sharper, not safer: the new protocol §7.0 makes range_truth_join a mandatory scoring step and §6.1/§7.0 tell the operator to use `--clock-offset-s` as the plan with `--auto-sync` only as a close-crossing cross-check.

### [stale/stranded] The "10 unverified review findings" cannot be acted on — the promised list was never written into the doc
- **Priority:** P1 — a queue item that reads as tracked work but has no recoverable content; leaving it implies coverage that does not exist
- **Evidence:** docs/review2_silent_failure_findings.md:12 ("10 unverified titles are listed at the end") vs `grep -n '^### ' ` -> only BLOCKER/HIGH/MEDIUM/LOW, and `tail -3` ends mid-LOW; `grep -c '^\*\*' ` = 73; docs/next.md:17

docs/next.md and the contract both carry "re-verify the 10 findings the review's session limit left UNVERIFIED (flagged in the findings doc)" as remaining work, and the doc's own header says "10 unverified titles are listed at the end" — but the file ends with the LOW section and contains exactly 73 bolded finding titles and no unverified list. The raw 111 findings were never persisted either (no review artifacts under logs/). As recorded, the item is unactionable: nobody can say which 10. Recovery is either to close the item honestly (state that the list evaporated with the session) or to re-run a targeted verification pass over the highest-risk untouched areas.

### [builder-decision] Builder decision #22 (post-GO OFFBOARD loss): the code points at an ADR-0081 addendum that does not exist, and the bench step that would prove a fix was never updated
- **Priority:** P1 — a safety-behaviour decision on the only code that will fly; cheap to implement once ruled, and it must not be confused with the deliberate no-datalink link asymmetry
- **Evidence:** flight/deploy/real_flight.py:1724-1731 (the FAULT print + "see the open ADR-0081 addendum"); `grep -n 'ADR-0081' docs/decisions.md` -> single entry at :2107, no addendum; docs/review2_silent_failure_findings.md:512-516, :659; docs/launch_mechanism_plan.md L3 row unchanged

Today the driver only PRINTS `FAULT offboard_lost_after_go` and keeps streaming setpoints — the state machine has failsafes 1-8 but nothing for mode loss, and the print itself says "Policy: post-GO loss is RECORDED, not aborted … see the open ADR-0081 addendum". docs/decisions.md has no such addendum. The review's recommendation was FAILSAFE 7 (SAFE with reason `offboard_lost` in CODED_DASH/ENGAGE/BREAKOFF, relying on SAFE being absorbing so a live mid-ramp dash command cannot execute when the pilot flips OFFBOARD back on), plus a producer/consumer test and a bench step; launch_mechanism_plan.md L3 still reads only "OFFBOARD accepted/rejected as expected", and build_tab brn-05 was not mirrored.

### [builder-decision] Desk-rehearsal fps decision is open AND the code still allows a laptop-webcam session to supply the money gate's divisor
- **Priority:** P1 — a whitelist is ~3 lines, and the wrong divisor flips the $740 verdict
- **Evidence:** scripts/seeker/tripod_score.py:1219-1266 (resolve_stream_fps; only FPS_SOURCE_REJECT_MARK="replay" at :144-147) vs pi_capture.py:552 writing `"source": source`; docs/review2_silent_failure_findings.md:128-141; docs/decisions.md ADR-0082 "Honest scope" unamended

The review's open builder question — may a `--local`/v4l2 desk-rehearsal session ever supply the gate's fps? (recommendation: NO, only the Pi 5 §7.3 bench at the flying quad_decimate) — is unanswered, and the implemented fix is weaker than the recommendation: resolve_stream_fps only REJECTS sources whose `stream_fps_source` contains "replay" and merely WARNS when tag decode was off. It never checks meta["source"], which pi_capture already writes, so a v4l2 desk session with decode on yields a `decode_loop_fps` the gate accepts as the modelled quantity. ADR-0082's "Honest scope" also still names "achieved Pi capture rate" as the hinge quantity where it should read "sustained onboard decode cadence"; the addendum was never logged.

### [open] Every `--coded-dash` arm ran its pointing levers and its abort window on WALL clock, not sim clock
- **Priority:** P1 — ~10 sim-free lines (fix + loud wall-clock warning + a dash_clock CSV column) and it re-scopes the loft/cap arm numbers
- **Evidence:** docs/review2_silent_failure_findings.md:376-384 with measured RTF fits on logs/mc_loftdive_armB_line9_s123.csv; scripts/m4_intercept.py:3151-3170 comment vs the `if args.handoff:` clock construction; `git log -3 -- scripts/m4_intercept.py`

Verified finding: `sim_clock` is constructed only under `if args.handoff:`, and --coded-dash is mutually exclusive with --handoff, so `coded_dash_start_sim` is always None and the sim branch can never be taken — despite the caller's own comment claiming "SIM-CLOCK elapsed drives BOTH pointing levers (never wall time…)". Measured RTF 0.80-0.84 on the flown ARM B logs means the 3.57 cap was effectively ~4.35 m/s^2 (23.9 deg not 20 deg) and a 2.5 s dive ran ~2.05 sim s; t_sim is empty on all 167 rows checked. m4_intercept.py is untouched since f56f9dc, so this is live. It directly violates the project's oldest standing rule (ADR-0009, sim time never wall time) and also governs the `--coded-dash-max-s` acquisition window on every coded-dash flight, not just the (now-rejected) cap arms.

### [open] The contract and Artifact do not record wave-2 (field-tools + protocol, task #23) — the mandated same-turn ritual was not completed
- **Priority:** P1 — the project's #1 housekeeping rule, and the dashboard is the builder's live reference
- **Evidence:** `ls -la docs/project_state.json docs/dashboard.html` -> both 02:49 Jul 25 vs commit f3a3bca at 03:11; `grep -c range_truth_join docs/project_state.json` -> 0; commit f3a3bca file list (11 files, none of them the contract)

f3a3bca landed the tripod-day range-truth chain (protocol §7.0), the 04_pull_logs exit contract, the new 05_pi_link_check.sh and the reconciled station list at 03:11-03:12; docs/project_state.json and docs/dashboard.html were last written at 02:49 (0a41fd9). The contract contains zero mentions of range_truth_join, 05_pi_link_check or PULL_FAILED, so the canonical state does not know the tripod day can now produce a verdict, and the hosted Artifact is a republish behind. CLAUDE.md's rule is explicit: an update that isn't in the contract didn't happen.

### [open] docs/next.md's live block is stale in three ways, including a REFUTED conclusion still stated as settled
- **Priority:** P1 — a reversed conclusion sitting on an always-visible surface; ~15 minutes of edits
- **Evidence:** docs/next.md:37 (stale claim) vs docs/project_state.json "⛔ AIM-ERROR SWEEP COMPLETE 2026-07-24 — 'the camera defends imperfect aim' is REFUTED IN SIM"; docs/next.md:19 vs commit f3a3bca; docs/next.md:43-44 vs commits 31b0ab9 and 1911ce9; `git log -- docs/next.md` head = 0a41fd9 (before all three)

(a) SETTLED CONCLUSION #2 still reads "Its real value = defending imperfect real-world aim" — refuted by the aim-error sweep (fdf818b) and already corrected in the contract with a ⛔ marker, so the flipped verdict was swept in the JSON but not in the prose a reader sees (the exact failure CLAUDE.md's drift rule names). (b) The ⏳ IN FLIGHT line still shows task #23 as a running worker although f3a3bca landed it. (c) Both remaining unchecked boxes are closed: EAL by 31b0ab9 and the crossing_sign float-dust bug by 1911ce9 (fixed by routing through flight.guidance.crossing_sign, byte-identical on all 4 canonical crossing geometries, so no SITL slot is owed).

### [open] docs/progress.md has been frozen since 2026-07-20 and is missing the entire 07-21..07-25 arc
- **Priority:** P1 — cheap, and docs/progress.md is one of the three files a post-/clear session reads first
- **Evidence:** `ls -la docs/progress.md` -> Jul 20 21:20; `grep -o '2026-07-2[0-9]' docs/progress.md | sort -u` -> only 2026-07-20; run_tests stage 3 checks only project_state.json <-> dashboard.html

The milestone roll-up's newest row is the 2026-07-20 Tier-1 build phase; it contains no row for the aim-lever decomposition (ADR-0079/0080/0082/0083, the ~0.71 m open-loop floor and the +5 deg trim), for the real_flight.py onboard state machine (ADR-0081, both SITL gates passing), for the in-view probe settlement, or for the two full reviews and their fixes. It is a subordinate view the contract requires to stay consistent, and nothing enforces it — the drift check is JSON-vs-HTML only, so run_tests stays green while the roll-up misleads a fresh session or a reader.

### [open] The headline "r2l 0.33 m = INSIDE the ~0.36 m ram envelope" is a seed-123-only number published as pooled
- **Priority:** P1 — a wrong number on the most-quoted result surface, and it is a pure text correction plus one recomputation
- **Evidence:** docs/review2_silent_failure_findings.md:819-821; docs/decisions.md ADR-0083 "Why it matters" ("the r2l cell to 0.33 m, inside the envelope"); `grep -c '0.355' docs/project_state.json` -> 0 while '0.33' appears in two contract notes

Verified finding: recomputed from the committed CSVs the r2l median is 0.3315 m on seed 123 alone, 0.355 m pooled across 123+777, and 0.395 m on the disjoint seed — i.e. outside the stated envelope on the replication seed and a coin flip pooled. ADR-0083's own replication paragraph warns the seed-123 magnitude was optimistic and then its "Why it matters" paragraph uses it anyway; the contract's coded_dash/launch_aim notes repeat it verbatim. Uncorrected, a reader concludes the open-loop dash is already in kill range on r2l and de-prioritises further aim work — and it interacts with the unratified 0.36 m envelope (#17).

### [open] The one OPEN contradiction (wedge-sizing-vs-accel-cap) is now resolvable on existing evidence, and its stated resolution path is dead
- **Priority:** P1 — an unadjudicated contradiction feeding a hardware print; re-adjudicate and close (or restate) in the ledger
- **Evidence:** docs/project_state.json contradictions -> id `wedge-sizing-vs-accel-cap`, status open, resolution names the accel-capped Gazebo confirmation; same file stage `coded_dash`: "CAP CONFOUND RESOLVED 2026-07-24 … ⇒ don't cap"; run_tests stage 3 output "37 contradictions [1 open]"

The ledger's only open entry says the fixed wedge should be 10-15 deg sized to an ACCEL-CAPPED ~20 deg dash rather than the committed "wedge = measured dash pitch (25-40 deg)", and directs that a serialized Gazebo confirmation of the accel-capped dash settles it. But the cap has since been rejected: EAL showed it costs ~0.34 m of closing speed even at correct aim, and the contract's coded_dash note now reads "⇒ don't cap". With no cap, claim_b's premise is gone and the wedge question reverts to the uncapped pitch (bounded by the ~11 deg flush-mount prop clearance, hence the boom). This is not academic: P3/L9 prints a physical bracket off that number.

### [open] audit_per_tick still prints "N/N flights passed (a)/(b)/(c)" when check (c) never gated
- **Priority:** P1 — measurement-layer honesty on the project's anti-mirage gate; ~10 lines
- **Evidence:** scripts/audit_per_tick.py:401-410 (missing columns -> WARN-skip) and :849-851 (`PASS: {n}/{n} audited flights passed (a)/(b)/(c)`); docs/review2_silent_failure_findings.md:473

The instrument's own no-vacuous-verdict work landed at the flight level (VACUOUS/exit-3 on zero flights scored), but the sub-check level was left: when the columns for (c) are missing the check WARN-skips, and the final summary still asserts (a)/(b)/(c) passed for every audited flight. Check (c) is the camera-LOS coupling test — the exact one the contract calls "WEAK" and the anti-mirage gate depends on — so a silent non-run reads as a clean camera-guided pass. The review's recommendation was a third status plus a separate roll-up line "N flights: (c) did not run", non-gating so the 2026-07-08 calibration is preserved.

### [open] Nine review-2 findings cite files that have not been touched since the review commit — an evidenced lower bound on what "code side CLOSED" left behind
- **Priority:** P1 — the honesty-spine one (AST naming gap) and the inert-compensation one are HIGH; the rest are cheap
- **Evidence:** scripted locator-vs-diff analysis over docs/review2_silent_failure_findings.md (73 locator lines) against `git diff --name-only f0dcd64^ HEAD`; the 9 finding titles at lines 212, 377, 450, 521, 602, 762, 930, 1032, 1171

Diffing every finding's locator against `git diff --name-only f0dcd64^..HEAD` (44 files changed) leaves 9 findings whose cited files were never edited. Besides the three called out separately above (m4 zero-velocity, m4 wall-clock, range_truth_join), they are: the AST gt-check naming gap (a read of the raw Gazebo pose dict `tracker.latest[...]` leaks ground truth into pro-nav and passes both honesty checks clean — m3_static_intercept.py/tests/test_honesty_static.py); `--terminal-bearing-bias-*`/`--terminal-los-lag-ms` silently inert under `--law pip` while logging "compensation ACTIVE"; dash_cpa_model.py `--validate` reporting the FIRST candidate acceleration as best-fit when any flight has miss_m=nan; autolabel's printed "tag decode ceiling" always 100% in negatives mode; placard_mount.md §5.5's dropped sign on the GPS shadow (blocked sky ~44% not ~17%); and the half-landed hardware default-weights fix (the ARM bench check still forces the blind sim model).

### [builder-decision] Builder decision #16 (vertical channel + altitude datum) exists only as a five-word label — the question itself was never written down
- **Priority:** P2 — write the question/options/recommendation into the contract as a decision record before it evaporates; not itself blocking
- **Evidence:** docs/next.md:17; docs/project_state.json updated_by ("builder decisions (#16 vertical/altitude-datum …)"); no other hit for the phrase repo-wide; related but unlinked: docs/design_review_sim_to_real_2026-07-10.md:130 (gap 15) and docs/audit_findings_tracker.md:174 (P-lowC, UNTRACKED)

docs/next.md:17 and the contract's updated_by both list "#16 vertical channel + altitude datum" as a pending builder decision, but no repo document states what is being asked, what the options are, or what the recommendation is. The two plausible referents are both real and both undocumented as decisions: there is still no vertical guidance channel (fixed ALT_REF P-loop, so any target altitude offset is an unguided miss component — design-review gap #15, tracker P-lowC), and the field range-truth chain has an unguarded AMSL-vs-AGL datum. As recorded, this is unactionable when the builder returns — the precise context-loss failure mode the project has flagged as its #1 frustration.

### [open] docs/next.md's BUILD QUEUE (lines 751-850) is pre-pivot residue with no SUPERSEDED banner and contradicts the contract
- **Priority:** P2 — collapse the queue into the DONE/SUPERSEDED history and leave the top block as the only live surface
- **Evidence:** docs/next.md:788-812 (#46 as "favored"/"open pointing lever") vs docs/project_state.json stage `pointing`: "FINAL (builder 2026-07-17): FIXED up-tilt mount — no servo, adaptive tilt REJECTED"; docs/next.md:826-828 vs `ls LICENSE .github/workflows/ci.yml` and `git remote -v`

The queue is presented as live work but item 3 promotes "#46 adaptive camera tilt" as the open pointing lever, while the contract's `pointing` stage records the builder's 2026-07-17 FINAL ruling: fixed up-tilt mount, no servo, adaptive tilt REJECTED for simplicity. Items 2/4/5/6/7 (#40 remainder, #44, #32, #43, G8 clutter batch) are cue-era sim tasks the real-build pivot mooted, and the "Sim-free lane" still lists LICENSE, CI and the GitHub remote as to-do although all three exist (LICENSE, .github/workflows/ci.yml, origin Swampyemerson/interceptor-sim). A fresh session orienting from docs/next.md can pick a dead item.

### [builder-decision] T25 demo video: four required elements are in a 30.7 s cut, but it has been idle 14 days and its story predates the pivot
- **Priority:** P2 — the single most visible portfolio artifact, but its content needs re-scoping before more render time is spent
- **Evidence:** `ls -la demo_out/t25/` -> t25_demo.mp4 927 KB, Jul 11 01:20; demo_out/t25/build/ has shot0..shot8 but no shot7; docs/next.md:752-786; docs/project_state.json headline "NEXT sim work: T25 video / demo captures"; .gitignore:63 `demo_out/*`

The build has shots 0/1/2/3/3b/4/5/6/8 assembled (t25_demo.mp4, last written 2026-07-11); what remains is shot 7 (the chase angle, which must be captured alone because a second rendered camera perturbs the flight), a final frame-honesty audit, and `assemble_t25.sh --require-all`. The contract still names it ("NEXT sim work: T25 video / demo captures") and the builder's stated must-haves include the tilt shot. The open question is whether to finish it at all: the stereo opener showcases the ground-cue stack the real build CUT, and the headline sim conclusions moved twice since the cut was made — so it needs a builder/PM call on finish-with-reframing vs park. demo_out is gitignored, so the footage has no off-machine backup.

### [open] curve (b)'s far band is unscoreable — log-derived truth boxes are not built, so the Hailo question is only answerable inside curve (a)'s decode ceiling
- **Priority:** P2 — gates only the Hailo door, not the $740 order, but it is cheap now and expensive after the one field day
- **Evidence:** docs/tripod_test_protocol.md:633-642 ("⚠️ The shipped scorer does NOT do this yet … Truthing the far band needs log-derived truth boxes, which is not built"); escalated as a tracked task in commit f3a3bca's message

tripod_score.curve_b builds its truth box from the TAG's recovered pose, so it can only score frames where the tag decoded; beyond that ceiling the NN read degenerates to a fire-rate (how often it boxes something), not recall. The protocol documents this honestly and the tool reports `curve_b_band_coverage` rather than dropping frames silently, but §8.2's 10-25 m band — the band that gates the deferred Hailo/markerless phase — is answerable only where the two overlap. Now that range_truth_join produces log-derived TRUE ranges, extending curve_b to project a truth box from the target log is the natural next build, and it is offline/sim-free.

### [open] Hardware-gated remainders with no checklist: skr-07's owed --stream-fps bench number and the 20 TODO-BUILDER constants in the flight executor
- **Priority:** P2 — blocked on parts, but the mapping table is sim-free and prevents a burned bench day
- **Evidence:** docs/project_state.json build_tab step skr-07 ("Bench the REAL Pi 5 … Emulation cannot measure this"); `grep -c TODO-BUILDER flight/deploy/real_flight.py` -> 20; docs/launch_mechanism_plan.md §7 L1-L12 (L1/L2 done, L3-L7 parts-blocked)

The money gate's divisor must come from a sustained Pi 5 AprilTag/CPU-YOLO thermal-soak bench (build_tab skr-07); until that number exists tripod_score correctly returns UNCERTAIN, so it is a hard prerequisite of the tripod day rather than an optional bench. Separately, flight/deploy/real_flight.py carries 20 TODO-BUILDER sites (standby alt, dash speed, terminal/breakoff, all failsafe thresholds, RC channel index, geofence, camera intrinsics) that the contract calls "NOT flight-ready", but nothing maps each constant to the bench step (L3/L5/L6/L9/L10) that measures it. A one-page mapping table now converts a vague warning into a runnable bring-up checklist.

### [improvement-idea] Low-severity review residue that the "code side CLOSED" claim does not cover
- **Priority:** P2 — minutes each; the renderer test is the one with real leverage since it generalizes to 20+ unenforced schema fields
- **Evidence:** docs/review2_silent_failure_findings.md LOW section (:1219+); `ls tests/ | grep -i render` -> empty; scripts/render_dashboard.py CONTRA_KEYS has no verdict/resolution; docs/placard_mount.md:878; verified-fixed counterparts at render_dashboard.py:102,:386-392 and tripod_score.py:1873

Three cheap items remain from the verified LOW set: (1) scripts/render_dashboard.py still has NO unit test at all and the contradiction ledger requires neither `verdict` nor `resolution`, so a flag can be flipped open->resolved with the resolution deleted and every check stays green (the review recommended two conditional asserts plus tests/test_render_dashboard.py, and explicitly recommended AGAINST the stale-quote scan, which measured a 100% false-positive rate). (2) docs/placard_mount.md:878 still reads "0.58 s at 9 m/s ≈ 17 frames", which only holds at the invented 30 fps ADR-0082 removed — 11.4 frames at the 20 Hz loop, against a 5-frame streak requirement — and the §13 assumption register has no row for the capture rate. Some sibling LOW findings ARE fixed (artifact_url now required, --check --artifact now fails loud, tripod_score --gate exit codes landed), so this is residue, not a systemic miss.

**Reader notes:** SCOPE: docs/next.md (878 lines; live content is the top ~50 lines plus the un-bannered BUILD QUEUE at 751-850) + docs/progress.md, cross-checked against git log, docs/project_state.json, docs/review2_*.md and the actual code. Everything under the 🗄 SUPERSEDED / ⛔ / [SUPERSEDED] markers was read but excluded (adaptive tilt #46, fixed-mount adoption, #40 Stage-2, foveated crop, phantom-removal, RTK, ADR-0059 recovery are all graveyarded or builder-rejected — I did not resurrect them).

CLOSED BY LATER COMMITS (reported so the parent can strike them, not as open work): task #23 field-tools/protocol landed f3a3bca + eb748e1; the EAL experiment closed 31b0ab9 (cap costs ~0.34 m at correct aim, decomposition complete); crossing_sign float-dust closed 1911ce9 with an offline byte-identity proof, so the "needs a SITL slot" note is void; LICENSE/CI/GitHub remote all exist. Only 2 unchecked checkboxes exist in all of docs/next.md and both are these closed items.

VERIFICATION RUN: `scripts/run_tests.sh` is ALL GREEN at HEAD c289086 — 428 passed/2 skipped (the 2 skips are the deliberate onnxruntime-in-.venv routing), 28 ONNX parity, dashboard drift OK ("20 visible numbers traced to the contract", 37 contradictions [1 open]), and 3 self-test entry points OK. So the queue's problems are NOT test-suite regressions; they are unfixed findings, un-swept docs, and unmade decisions.

THE ONE THING I'D SURFACE FIRST: item 1 (frozen_vworld). It is a measurement/control silent failure of exactly the class the project just built prevention for — the aim-error sweep's "the camera never earns its handoff, REFUTED IN SIM" verdict, currently the contract's and the Artifact's live headline, was measured on a terminal commanding (0,0) in most engaged flights. The dash-only twins are unaffected, so ADR-0083's +5 deg trim survives; the fix is a port from the deployed seeker_loop plus a ~40-flight camera-arm re-fly at idle load.

CROSS-SLICE NOTE: items 1, 4, 8, 13, 14 originate in docs/review2_silent_failure_findings.md, so they may also surface in a review-2-focused slice; I included them because docs/next.md's REMAINING block is what points at them and because I verified each against current code rather than trusting the finding text.


## Reader 3 — Review-2 silent-failure remainder

### [open] BLOCKER STILL OPEN: the sim coded-dash camera terminal commands ZERO horizontal velocity (m4_intercept.py frozen_vworld latch) — I reproduced it across 6 arms
- **Priority:** P0 — a published, contract-level verdict rests on a terminal that was provably not steering
- **Evidence:** scripts/m4_intercept.py:2529, :3703-3709 (unmodified since the review); logs/mc_fp_armG20_line9_s123_stdout.log 7x "Terminal coast: velocity vector frozen at r_hat=1.54 m ((0.00, 0.00) world)"; per-flight CSV scan (cmd_vn/cmd_ve<1e-6 on ENGAGE rows) across logs/mc_fp_arm{G20,AE5,AE15,GL,CG}*.csv = 29/44 engaged flights >75% zero-command

Review-2 finding at docs/review2_silent_failure_findings.md:449 was NEVER fixed: the flight-code stream explicitly scoped itself to flight/ and left scripts/m4 "for that owner" (commit 49e4281 body), and nobody picked it up. scripts/m4_intercept.py:2529 still initialises `vh0 = vh1 = 0.0`, :3703-3704 still latches `frozen_vworld = (vh0, vh1)`, and under --coded-dash --fpv the NN's short r_hat puts the very first ENGAGE tick inside TERMINAL_FREEZE_RANGE_M so the block that computes vh0/vh1 never runs. I re-measured it independently from the logs, not from the review's text: armG20_line9_s123 = 7/7 engaged flights zero-command, 661/668 ENGAGE ticks (99%); G20_s777 7/8 flights, 55% ticks; AE5_s123 5/8, 72%; AE15_s123 4/7, 73%; GL_s777 5/6, 50%; CG_s777 1/8, 32%. Every camera-arm conclusion the project currently publishes ("the camera terminal NEVER earns its handoff / REFUTED IN SIM", "G20 1.02 vs 0.75 m", "both terminal compensations are NULL") was measured on a terminal that was braking, not steering. Fix is a port from flight/deploy/seeker_loop.py:311-315 (one code path, sim == hardware), then a re-fly of ONLY the camera arms (dash-only twins must be reused or the pairing breaks).

### [open] The contract/dashboard still publish the zero-command-terminal verdicts as SETTLED; no contradiction-ledger entry, no PENDING RE-FLY downgrade
- **Priority:** P0 — a reversed-conclusion risk sitting on the always-visible layer, exactly the drift class CLAUDE.md pins
- **Evidence:** docs/project_state.json:6 (updated_by still asserts "REFUTED IN SIM" and "G20: 1.02 vs 0.75 m"); python scan of state['contradictions'] = 37 entries, 1 open, 0 matches for frozen_vworld/zero-command/PENDING RE-FLY

The review's decision item 5 for the frozen_vworld finding required the same-turn contract edits: downgrade "AIM-ERROR SWEEP COMPLETE … REFUTED IN SIM", "G20 1.02 vs 0.75", "GL/compensations are NULLs" to PENDING RE-FLY, ring-fence what still stands (ADR-0080/0083, the 0.71/0.43 m dash-only floor, the hardware seeker_loop path), and open a ledger entry "camera-terminal verdicts measured on a zero-command terminal". None of that happened. The contract has 37 contradictions with exactly ONE open (wedge-sizing-vs-accel-cap) and zero occurrences of frozen_vworld / zero-command / PENDING RE-FLY. This is precisely the "sweep the always-visible surfaces the same turn" rule in CLAUDE.md, and it is the surface outside readers see.

### [open] audit_per_tick check (c) still discards zero-command ticks, so the audit is anti-correlated with the defect above and prints PASS
- **Priority:** P0 — it is the detector for the item above; fixing the latch without it leaves the same blind spot
- **Evidence:** scripts/audit_per_tick.py: no `n_zerocmd`/UNVERIFIED/PASS_AB_C token anywhere (grep empty); :849-851 prints "PASS: {n}/{n} audited flights passed (a)/(b)/(c)"; contrast findings:465-479

Finding at findings:465 is unfixed. The (c) camera-LOS-coupling check skips rows where |cmd_horiz| ~ 0, so on exactly the flights above it drops nearly every row, falls under the n<10 WARN branch, stops gating, and the tool's final line still reads "PASS: N/N audited flights passed (a)/(b)/(c)". The review asked for three things: count and name the excluded rows (n_zerocmd) in the WARN text, add a third flight status so the summary cannot claim (c) ran when it did not, and add a NEW gating check (e) "the terminal actually actuated" (FAIL when >50% of detected pre-CPA ENGAGE ticks are zero-command). None are present. The 2026-07-25 vacuous-verdict work fixed the n=0 arm case (exit 3) but not this one — a per-flight silent non-gating.

### [open] Every --coded-dash arm runs the Phase-A pointing levers on WALL clock, violating the project's #1 methodology rule
- **Priority:** P1 — invalidates cap/dive SIZING and cross-arm comparability (not the ON/OFF direction), and it gates the open wedge-sizing contradiction
- **Evidence:** scripts/m4_intercept.py:4391-4395 (`if args.handoff:`), :3155-3159 (sim branch unreachable, wall fallback), :3190 (abort on wall); grep for `dash_clock` returns nothing

Finding at findings:376 is unfixed. sim_clock is still constructed only under `if args.handoff:` (m4_intercept.py:4393) while --coded-dash and --handoff are mutually exclusive, so coded_dash_start_sim never populates and _dash_elapsed falls back to wall monotonic at :3159 — driving --dash-accel-cap, --dash-loft-dive-s and the --coded-dash-max-s abort. flight/guidance.py documents those arguments as "SIM-clock seconds … never wall time (the RTF-sag rule)", and m4's own comment at :3151-3154 claims sim clock is in use. The review measured RTF 0.80-0.84 on the flown ARM B logs, i.e. a commanded 3.57 cap actually flew ~4.35 m/s2 (theta 23.9 not 20.0 deg). The cheap generalizable half — a loud one-shot WALL-clock warning plus a `dash_clock` (sim|wall) CSV column — is also absent, so future logs still cannot self-identify their clock.

### [stale/stranded] The ~10 UNVERIFIED review-2 findings are unrecoverable — the doc promises a list that does not exist
- **Priority:** P1 — decide explicitly: run a scoped re-verification sweep over the same code surfaces, or strike the item from docs/next.md + the contract so it stops reading as pending work
- **Evidence:** docs/review2_silent_failure_findings.md:10-12 vs. tail of file (line 1261) and `grep -c '^\*\*'` = 73; grep -o "unverified titles" across all session jsonl returns only the doc quote

docs/review2_silent_failure_findings.md:10-12 says the run hit a session limit, "10 of the verification agents did not complete" and "the 10 unverified titles are listed at the end and must be re-verified before being acted on". There is no such list: the file ends at the third LOW finding (line 1261) and contains exactly 73 finding headings, all in the verified BLOCKER/HIGH/MEDIUM/LOW sections. I searched the repo (docs/, logs/), the contract, and every Claude session transcript under ~/.claude/projects/-home-emerson-interceptor-sim (500 MB, all .jsonl + subagent transcripts) — the only hit for "unverified titles" is the doc's own broken promise. The 125-agent workflow's per-agent outputs are not stored anywhere reachable. So the item that docs/next.md and the contract both carry as REMAINING work cannot be executed as written.

### [open] Action-plan Wave-2 row never executed: the Fable gap-check review of each stream's diff
- **Priority:** P1 — the cheapest way to find the rest of the orphaned m4-side findings before more work is built on them
- **Evidence:** docs/review2_action_plan.md:41-48; commit 49e4281 body "#6 (bias/lag inert under pip) is in scripts/m4, not flight/ — left for that owner"; no subsequent commit touches scripts/m4_intercept.py

docs/review2_action_plan.md:42 lists "Review each stream's diff (Fable gap-check on the flight-code fixes before I trust them)" as a Wave-2 item. No commit, docs/next.md entry, or contract note records that it ran, and docs/next.md jumps straight from the three stream commits to "the code side of both reviews is closed". This is not a bookkeeping nit: the two biggest still-open defects (frozen_vworld and the wall-clock levers) both live in scripts/m4_intercept.py, were explicitly disclaimed by the flight-code worker as "not my file", and a gap-check pass over the diffs against the findings list is exactly what would have caught the orphaned ownership.

### [open] Contract, docs/next.md and dashboard are stale by two commits: Wave-2 landed but was never folded in (same-turn rule violated)
- **Priority:** P1 — one edit + render + republish; it also unblocks honest status reads for everything else in this list
- **Evidence:** `git log --oneline -- docs/project_state.json | head -1` = 0a41fd9 (also for docs/next.md and docs/dashboard.html), while HEAD is fad4c11 with f3a3bca/eb748e1 in between; project_state.json:6 "Remaining: field-tools+protocol (in flight)"; docs/next.md line "⏳ IN FLIGHT: field-tools + protocol …"

docs/project_state.json, docs/dashboard.html and docs/next.md were last touched at 0a41fd9. f3a3bca (the whole Wave-2 field-tools + protocol tranche) and eb748e1 (the --help crash fix) landed after and touched none of them. So the contract still says "Remaining: field-tools+protocol (in flight)" and docs/next.md still shows "IN FLIGHT: field-tools + protocol range-truth chain (task #23, worker)" for work that is finished, and the four items f3a3bca escalated (tripod_score --help [since fixed], quad_decimate unimplemented, curve-b far-band truth boxes, the §7.3 stream-fps bench) live only in a commit message plus two protocol call-outs — not in the contract's task lists. CLAUDE.md's #1 housekeeping rule is the same-turn contract update + artifact republish; the drift check cannot see this because it only compares JSON to HTML.

### [open] Wave-1 flight-code row #6 never landed: --terminal-bearing-bias-* / --terminal-los-lag-ms are still silently inert under --law pip
- **Priority:** P1 — ~4 lines, no flights, and it protects against manufacturing a fourth phantom NULL
- **Evidence:** scripts/m4_intercept.py:3640-3650 (ACTIVE print, shared), :3652-3677 (pip branch), :2481-2512 (flag resolution, no law guard); findings:520-544 for the 4-line fix

Listed in the action plan's Wave-1 flight-code stream (review2_action_plan.md:22) and deferred by the worker as an m4-side file. Current code is unchanged: lambda_cmd and the "[m4] Terminal LOS compensation ACTIVE" print sit on the shared path at :3640-3650, the `--law pip` branch at :3652-3673 builds its command from target_tracker.pos_hat and only consumes lambda_cmd in the tracker-not-ready fallback at :3676, and there is no parse-time guard rejecting the combination (nothing matching `args.law == "pip"` near the flag resolution at :2481-2512). Consequence if it bites: a legitimate PIP calibration arm logs "compensation ACTIVE", returns "no effect", and gets recorded as yet another terminal-compensation NULL when the knob was never in the loop.

### [open] quad_decimate is prescribed by the protocol but implemented nowhere — the no-second-field-day reclaim lever cannot be exercised
- **Priority:** P1 — desk-cheap, and it must land before the tripod session or the lever is lost with the data
- **Evidence:** docs/tripod_test_protocol.md:236-247 ("⛔ NOT EXECUTABLE WITH THE SHIPPED TOOLS (verified 2026-07-25)"); grep for quad_decimate in scripts/seeker/*.py finds only prose/comments, no Detector kwarg and no CLI flag

Escalated by f3a3bca and honestly flagged in the protocol, but the code work was not done. Every decode site constructs Detector(families="tag36h11") with no quad_decimate argument (pi_capture.py, tripod_score.py, autolabel_from_apriltag.py), so all run the pupil-apriltags default 2.0, no CLI flag exists, and meta.json does not record which value produced a session. §4.2b's "re-decode the same frames at qd in {2.0, 1.0}" — the first reclaim lever if the $740 gate comes back marginal, worth ~1.5x range and a +-48-56 deg cone — is therefore unrunnable, and after the field day the frames cannot be re-scored to recover it either (the value is unrecorded). The protocol says to add it BEFORE the field day or strike the bullet.

### [builder-decision] The $740 gate's divisor is still unmeasured: the §7.3 Pi-5 bench fps is owed, and the fps SOURCE check is a blacklist, not the recommended whitelist
- **Priority:** P1 — it is the single input that decides a ~$740 purchase, and the bench is a prerequisite for the field day
- **Evidence:** docs/project_state.json build_tab subsystem[1] step skr-07 (open); scripts/seeker/tripod_score.py:145-147 FPS_SOURCE_REJECT_MARK="replay", :1240-1266 resolve_stream_fps; docs/decisions.md newest entry is ADR-0083

Two halves of the same money-gate risk. (1) skr-07 ("Bench the REAL Pi 5: sustained AprilTag decode fps … through a thermal soak") is still an open build_tab step, so the number tripod_score wants via --stream-fps does not exist yet; the burn model scales as 1/fps and the verdict flips across ~24 fps. (2) resolve_stream_fps now prefers decode_loop_fps and warns loudly, but it still ACCEPTS a meta.json rate from any source except one containing the substring "replay" — the review asked for a whitelist (accept only meta["tag_decode"] True AND meta["source"]=="picamera2"). A --local/v4l2 desk rehearsal on the laptop webcam with decode on would still supply the gate's divisor with only a source string as the caveat. The builder ruling the review escalated ("may a desk-rehearsal capture ever feed the money-gate fps?", recommended NO) is unanswered and unlogged — there is no ADR-0084+.

### [open] Deployed-weights drift: flight default is n-mono, but the contract, the bench gate and the deploy README all still say quad_v2 — and no test pins it
- **Priority:** P1 — the bench gate currently proves the wrong model, and the contract asserts a superseded fact
- **Evidence:** flight/deploy/seeker_loop.py:1254-1263 vs scripts/check_deploy_bench.sh:86 and flight/deploy/README.md:109,119,129; docs/project_state.json detector stage `active` = "drone_finetuned_quad_v2.onnx @640…" and build_tab step skr-08

Four review findings (307/341/929/1123) share one root and none is closed. flight/deploy/seeker_loop.py:1254-1255 defaults to weights/nn_tier/n-mono.onnx (the real-data model, fixed in c3bd0d9), but: scripts/check_deploy_bench.sh:86 still defaults BENCH_WEIGHTS to drone_finetuned_quad_v2.onnx (so the hardware bench validates the blind sim model), flight/deploy/README.md:109/119/129 still documents and demonstrates quad_v2 as the on-vehicle weights, and the contract's detector stage `active` field plus the architecture DETECT step plus build_tab step skr-08 all still name quad_v2. No test asserts the default (grep for DEFAULT_WEIGHTS across tests/ and flight/tests/ is empty), so the fix can silently revert. The JSON-vs-HTML drift check cannot see any of this.

### [open] range_truth_join: the survey/altitude-datum integrity checks still live only on the --auto-sync path, which the field day's dominant geometry cannot use
- **Priority:** P1 — it fails silently on the exact path the field protocol now steers the operator onto
- **Evidence:** scripts/seeker/range_truth_join.py:1027-1028 (--max-bias-m documented as auto-sync-only), :421-533 (bias/survey Refuse logic inside auto_sync), :583-600 (shared-path checks are coverage-only); docs/tripod_test_protocol.md §4.6b

Finding at findings:601 is only partly closed. The shared path now refuses on zero in-span frames and on coverage below --min-coverage, and warns on an implausible median-up. But the constant-bias / survey-blunder ceiling is still --auto-sync-only (--max-bias-m's own help text says "--auto-sync constant-bias ceiling"), and the residual/bias diagnostics are computed inside auto_sync(). Wave-2 added protocol §4.6b precisely because --auto-sync refuses approach geometry by design — so on the day's main passes the operator runs the explicit --clock-offset-s path, i.e. the one WITHOUT the survey/datum check. A tripod survey given AGL instead of MSL would then propagate into true_range_m and into both curves silently.

### [builder-decision] Pending builder decisions, none logged as an ADR: #16 vertical/altitude datum, #17 ram radius vs the Tier-2 order, #22 post-GO OFFBOARD loss (FAILSAFE 7 absent)
- **Priority:** P1 — #17 gates a purchase; #22 gates a real-flight abort behaviour
- **Evidence:** docs/decisions.md last heading = "## ADR-0083"; docs/project_state.json:616 (ram-radius correction "not yet ratified"); flight/deploy/real_flight.py failsafe comments at :728,:736,:800,:810,:851(8),:900,:908 — no FAILSAFE 7

All three are carried in the contract's updated_by and in docs/next.md as REMAINING, and none has moved: docs/decisions.md still ends at ADR-0083. #17 is the most consequential number in the project — the contract's own kill-stage note says the 0.5 m ram radius was derived for a 7-inch airframe while both ordered aircraft are 5-inch (~0.36 m envelope), "RAISED TO THE BUILDER, not yet ratified", and it says this should be settled BEFORE the Tier-2 order. #22's recommended remedy (a FAILSAFE 7 for post-GO OFFBOARD loss) is unimplemented: real_flight.py defines failsafes 1-6 and 8, with 7 conspicuously unused. Note that #17's supporting evidence (the camera terminal "has to earn ~a metre and at correct aim it does not") is itself downstream of the zero-command-terminal defect above, so the two should be adjudicated together.

### [improvement-idea] Error-handling policy rules 1, 5 and 6 have no mechanical enforcement; rule 3 covers only two emitters
- **Priority:** P2 — the highest-value increment is a seam registry test for §6; §1/§5 are probably better left as review discipline
- **Evidence:** tests/test_no_vacuous_verdicts.py:11-15 ("Covers the verdict-bearing emitters this rule currently binds"); tests/ has exactly test_tripod_pipeline_join.py + test_flight_csv_contract.py as contract tests; scripts/run_tests.sh:40 (stage 1, no -rs/fail-on-skip); .github/workflows/ci.yml header "WHAT THIS DELIBERATELY SKIPS -- the .venv-seeker ONNX-parity stage"

docs/error_handling_policy.md says enforcement "lives in run_tests.sh + CI, not in discretion", but only parts of it do. §3 no-vacuous-verdicts is pinned for exactly two emitters (audit_per_tick.arm_verdict and tripod_score.gate_verdict, per the test's own docstring) — antimirage_verdict, coded_dash_summary, parse_ab, approach_recall, curve_b and the check_*.sh gates are unpinned. §6 producer→consumer contract tests exist for two seams (tripod recorder→joiner→scorer; m4/mc_batch CSV→3 auditors) with no registry or test that a NEW seam gets one. §1 (helpers raise / main catches once) and §5 (typed sentinels, count the Nones) are style rules with no lint or test at all. §7 is enforced in stages 2 and 4 but stage 1 tolerates skips silently (2 today, both legitimately covered by stage 2) and CI deliberately omits stage 2 entirely, so a green CI badge does not include ONNX parity.

### [improvement-idea] No --help smoke coverage for the python field-day tools — the exact class that shipped broken in f3a3bca
- **Priority:** P2 — cheap, and the field day is the one context where a crash costs a drive home
- **Evidence:** scripts/field/selftest.sh:100-103 (help sweep over field shell scripts); `grep -rln '\-\-help' tests/ flight/tests/` returns nothing; eb748e1 commit title

eb748e1 fixed a crash where `tripod_score.py --help` died on a bare %% in an argparse help string: the tool was committed in the Wave-2 tranche and its own entry point was never exercised, which is a direct instance of policy §8 ("a fix isn't done until its EFFECT is observed") slipping in the same tranche that codified it. scripts/field/selftest.sh:100-103 sweeps --help over scripts/field/*.sh only; nothing sweeps the python tools the tripod day depends on (tripod_score.py, range_truth_join.py, pi_capture.py, field_score.py, camera_session_summary.py). A ~10-line parametrised test (or a selftest leg) that runs --help on each and asserts exit 0 closes the class, and there is currently no regression test pinning eb748e1 itself.

### [open] Smaller review-2 items still open: detect-devices --require, antimirage nan dilution, placard 30-fps prose, contradiction-ledger closure evidence, failsafe accel constant
- **Priority:** P2 — each is minutes of work; (1) is the one with a field-day consequence
- **Evidence:** scripts/field/00_detect_devices.sh:188-190; scripts/experiments/loft_dive/antimirage_verdict.py:52-58 (no isnan); docs/placard_mount.md:878; scripts/render_dashboard.py:71 + no tests/test_render_dashboard.py; flight/deploy/real_flight.py:226-230

Five verified findings I checked individually and found unfixed. (1) findings:1107 — 00_detect_devices.sh:188-190 emits fld_warn for an unidentified serial port and leaves STATUS at OK, so the field-day hard gate --require still passes with no flight controller identified. (2) findings:1049 — antimirage_verdict.py load() keeps rows via `if r.get("miss_m")` with no isnan guard, so failed flights (miss_m=nan) still enter the comparison and dilute a real camera effect toward MIXED/MIRAGE. (3) findings:1231 — docs/placard_mount.md:878 still reads "0.58 s at 9 m/s ≈ 17 frames", re-introducing the invented 30 fps that ADR-0082 removed (11.4 frames at the 20 Hz the loop runs). (4) findings:1221 — render_dashboard.py:71 CONTRA_KEYS still omits verdict/resolution and there is no tests/test_render_dashboard.py, so a contradiction can be flipped to resolved with the resolution note deleted. (5) findings:988 — real_flight.py:230 dash_accel_ms2 is still 10.0 while ADR-0083 measured ~3.1, feeding both the accel-aware lead solve and the FAILSAFE 4 distance bound (it is TODO-BUILDER-flagged, which mitigates but does not close it).

### [open] Curve (b)'s far band still has no truth source — §8.2's 10-25 m question stays partly unanswerable after the field day
- **Priority:** P2 — decide (build vs re-plan) before the session; the tool now fails honestly, so it is not a correctness risk
- **Evidence:** docs/tripod_test_protocol.md:630-640 ("⚠️ The shipped scorer does NOT do this yet"); findings:879; commit f3a3bca body "Escalated as tracked tasks: … curve-b far-band truth boxes"

Escalated by f3a3bca and honestly documented, but not built. tripod_score.curve_b builds its truth box from the TAG's recovered pose, so it can only score frames where the tag decoded; beyond curve (a)'s decode ceiling curve (b) degrades to a fire-rate read (how often the NN boxes something), not recall. The 8cdbc77 work made this loud rather than silent (curve_b_band_coverage + section_8_2_answerable:false), which is the right first move, but the missing capability — log-derived truth boxes for frames past the tag ceiling — is what the six 15-20 m NN crossing passes actually need. Worth deciding before the field day whether to build it or to re-plan the crossing passes closer in, because the frames are cheap to capture and expensive to re-fly.

**Reader notes:** SCOPE + METHOD. I read docs/review2_action_plan.md and docs/error_handling_policy.md whole, indexed all 73 finding titles in docs/review2_silent_failure_findings.md by severity, then verified individual findings against live code rather than trusting the commit messages. I also ran the full suite and reproduced one finding from the logs.

VERIFIED GOOD (do not re-open):
- scripts/run_tests.sh is ALL GREEN at HEAD (fad4c11): stage 1 428 passed / 2 skipped, stage 2 ONNX parity 28 passed, stage 3 drift OK (20 visible numbers traced, 37 contradictions [1 open]), stage 4 three self-tests OK. Output at /tmp/claude-1000/-home-emerson-interceptor-sim/f68f5895-f3ec-4d04-aee6-345e425c9c9f/scratchpad/ (also the task output file). The 2 stage-1 skips are the two onnxruntime-gated tests in tests/test_ground_station.py, which stage 2 runs for real — legitimate.
- All FOUR field-tools/protocol items in my brief are CLOSED by f3a3bca and I confirmed each in code/doc, not from the commit message: 04_pull_logs.sh honors its 0/1 PULL_FAILED contract (:85,:241); tripod_test_protocol.md §7.0 "run these THREE, in this order" exists (:478) with the surveyed-position hard checkbox and the §4.6b close-crossing sync pass; scripts/field/05_pi_link_check.sh exists and is wired into bringup.sh:102, selftest.sh:98 and field_bringup.md §3.0; the station list is the contiguous 4/6/8/10/12/14/16/18/20 m grid (protocol :190).
- Also confirmed fixed: the curve-b geometry blocker, field_score analytic CPA + ULog coverage, R_decode90 hole-stopping walk, pi_capture crash-safe meta, antimirage geometry-match pairing, the flight/deploy coast-freeze latch + yaw-chase + offboard/RC clock, the widened AST honesty audit AND its scope-honesty caveat (real_flight.py:1326), test_injected_gt_reads_are_caught (test_real_flight.py:782-797), the artifact triple (artifact_url required at render_dashboard.py:102, --check --artifact now fails at :391, publish receipt at :428-433), and fc_link_check's widened arming audit.
- Working tree clean, pushed to origin/main.

TWO THINGS THE PARENT SHOULD ACT ON FIRST: (1) the frozen_vworld zero-command terminal is real, reproduced independently by me from the arm logs, and it undercuts the camera-arm conclusions the contract currently publishes as settled — including the evidence base for the pending ram-radius/Tier-2 builder decision; (2) the "10 unverified findings" work item cannot be executed because the titles were never written down anywhere that survives — it needs a decision (re-verify a scoped surface, or strike the item) rather than more searching. Both of the biggest open defects live in scripts/m4_intercept.py, which no Wave-1 stream owned; the un-run Wave-2 Fable gap-check is the process hole that let that happen.


## Reader 4 — Hardware / field arc

### [stale/stranded] The AUTO-mission generator still emits the PRE-CORRECTION tripod geometry that manufactures a FALSE NO-GO
- **Priority:** P0 — one stale default on the flown mission burns the single field day the $740 gate hangs on
- **Evidence:** configs/target_kakute/gen_tripod_mission.py:370-376 (defaults 8.0 / 17.0, help strings citing the superseded protocol) + configs/target_kakute/README.md:218; vs docs/tripod_test_protocol.md:158-212. `grep -rn gen_tripod_mission docs/ docs/next.md` = 0 hits; 'gen_tripod_mission' in project_state.json = False.

docs/tripod_test_protocol.md §4.1 was corrected 2026-07-24 (commit dbca8cf) to stations 4/6/8/10/12/14/16/18/20 m with a 5-6 m tag-envelope crossing standoff, because the old 8-30 m ladder sat entirely outside the predicted 7.10 m decode envelope and would have returned an all-zero curve (a) = NO-GO on the $740 order. The tool that generates the flown mission was never swept: its defaults are --near-range 8.0, --standoff 17.0, --leg-length 30.0, and its help text still cites 'protocol 4.1 max station is 30 m' / 'protocol 4.2 ~15-20'. The two money stations (4 and 6 m) are unreachable with defaults. It is also referenced NOWHERE in docs/, docs/next.md or project_state.json, so a builder following the protocol may not know it exists — and its --self-test passes because it checks mission structure and round-trip, not protocol conformance.

### [open] quad_decimate=1.0 is now the PLANNED tripod-day setting, but no decode path in the repo can be told to use it
- **Priority:** P0 — desk-cheap, and without it the money gate is run in a config that predicts FAIL
- **Evidence:** scripts/seeker/pi_capture.py:167, scripts/seeker/tripod_score.py:456, scripts/seeker/autolabel_from_apriltag.py:106 (all bare `Detector(families=...)`); docs/tripod_test_protocol.md:239-251 (the ⛔ NOT EXECUTABLE WITH THE SHIPPED TOOLS banner); docs/placard_sizing.md:235-290.

ADR-0082 (docs/placard_sizing.md §CORRECTION, 2026-07-24) promoted qd=1.0 from 'reclaim lever' to the planned capture setting because it is the ONLY configuration clearing t_go>=0.5 s at every credible frame rate — at the deployed qd=2.0 the adopted 0.35 m placard FAILS at 20 Hz (0.442 s) and 14 Hz (0.294 s). Every decode call in the field chain constructs Detector(families='tag36h11') with no quad_decimate argument, so all three run the pupil-apriltags default 2.0, no CLI flag exists, and meta.json does not record which value produced a session. The fix is small (add --quad-decimate to pi_capture + tripod_score, stamp it into meta.json) but must land before the field day, or the day is flown in the configuration that predicts FAIL and the offline re-score comparison is impossible.

### [open] Blocking parts still unordered — the Kakute microSD alone makes any pass unscoreable
- **Priority:** P0 — long-pole shipping items on the critical path; the microSD is a single point of total data loss
- **Evidence:** docs/project_state.json build_tab ground/seeker/target part notes ('STILL NEEDED: tripod + ball head ... PD power bank ... cones + tape measure'; 'microSD 32-64 GB for the Kakute' status=must-add); headline text 'Still missing before build: Kakute microSD, LiPo bag/glasses/blanket, Pi cooler, PD power bank, tripod+head, buzzer'; docs/launch_mechanism_plan.md:335-338.

Converged across bom_tiers, build_tab and launch_mechanism_plan §7, the still-missing list as of the last order update (2026-07-20b): 32-64 GB microSD for the Kakute (no card = no DataFlash .BIN = no target track = range_truth_join has nothing to join = every curve UNCERTAIN), fireproof LiPo bag + ANSI Z87 glasses + fire blanket, Pi 5 active cooler, >=27 W USB-C PD power bank (the wall PSU is useless at the field), tripod + ball head, cones + tape measure, lost-model buzzer, and 'verify the hex kit has an 8 mm prop socket'. Nothing records that any were ordered after 2026-07-20b, nor that the 2026-07-20 order has physically ARRIVED. The microSD and the tripod are hard blockers for rung B4 and for launch-mech bench L5.

### [open] No print-ready artifacts exist for the placard, the checkerboard, or the placard mount hardware
- **Priority:** P0 — three separate lead-time items (copy shop, board, print), each gating a B2/B4 step
- **Evidence:** `find` for *.stl/*.scad/*.step returns only Gazebo prop meshes; PIL reports models/apriltag_target/tag36h11_00000.png = 512x512 texture; docs/placard_mount.md §6 (sketch only) + §5.2 (parts list); docs/hardware_order_list.md:399 ('Region + printer? Assumed ... 3D-printer access').

The placard is sized (0.35 m tag black square on a 450x450 mm sheet) and its mount fully engineered, but nothing printable exists: the only tag asset is a 512x512 px Gazebo texture with no scaled PDF; there is no checkerboard file at all (bom_tiers says buy a rigid pre-printed board ~$8, still status 'print'); the PETG index disc + sacrificial frangible shoe exist only as an ASCII dimensioned sketch in placard_mount §6 — no STL/CAD anywhere in the repo. 3D-printer access is itself an unconfirmed ASSUMPTION in the BOM's own assumption register. Camera calibration (skr-06, the FIRST bench gate — every range/bearing number is wrong until it runs) is blocked on the checkerboard.

### [open] The capture matrix contradicts the adopted beam-facing placard mount: approach is the tag's NULL aspect and there is no nose-on block
- **Priority:** P0 — flying the wrong aspect mix wastes battery segments and can produce a false NO-GO read
- **Evidence:** docs/placard_mount.md §3.6 + §11 items 5-6 + §12 finding 7; docs/tripod_test_protocol.md:281-300 (matrix rows unchanged, no index-90 block); §4.2 got the 5-6 m crossing standoff but not the aspect re-brief.

placard_mount DECISION 1 committed a single vertical panel EDGE-ON to flight (tag facing sideways) because a nose-on panel makes 11.8 N of drag at 9 m/s vs 0.28 N edge-on. With that mount CROSSING is the tag's primary aspect and APPROACH is its 90-degree-incidence null — zero decodes by geometry. The protocol's §4.6 matrix still schedules 16 of ~28 passes as approach and labels them 'primary decode-envelope + recall data'. placard_mount §11 items 5 and 6 asked for the aspect re-brief plus a dedicated 90-degree-index nose-on block at <=4 m/s (also the ONLY source of approach-aspect frames for autolabel_from_apriltag.py, the NN label pipeline); neither was merged. As written a large fraction of the day's passes produce structural zeros that read as a tag failure.

### [open] Incidence-aware scoring — 'the single largest information gain available from the session' — is not implemented anywhere
- **Priority:** P0 — zero field-time cost to collect, but unimplemented it silently converts into an over-optimistic gate verdict
- **Evidence:** `grep -c incidence scripts/seeker/tripod_score.py` = 0; same for scripts/seeker/range_truth_join.py; docs/tripod_test_protocol.md:224-238 (mandate) vs :658-705 (§8.1 range-only); docs/placard_mount.md §13 ('cos θ decode law above 33° — HYPOTHESIS').

Protocol §4.2b now mandates recording the 15-degree-indexable mount angle per pass and scoring curve (a) as decode vs (range x INCIDENCE), and placard_mount §11 item 10 asks §8.1 to state the gate as t_go = (R_decode90(0)·cos θ_eng − R_burn)/V. Neither exists in code: 'incidence' appears zero times in tripod_score.py and zero times in range_truth_join.py, and §8.1 is still the range-only form. The cos-θ law is validated only to 32.6 degrees in sim and is a listed HYPOTHESIS above 33; the pessimistic corner leaves θ_max = 0, i.e. NO incidence budget. A GO measured at 0 degrees and applied to a 30-degree engagement is not a GO — so today the day can produce a number the gate cannot honestly consume.

### [open] docs/placard_mount.md is invisible to the contract — 9 bench gates, 10 contradictions and its BOM adds are all outside project_state.json
- **Priority:** P1 — a whole engineering doc's action items sit outside the compass a fresh session orients from
- **Evidence:** python check over docs/project_state.json: 'placard_mount' = False, 'frangible' = False, 'anemometer' = False; docs/placard_mount.md §9 (nine gates) and §12 (ten contradictions, none in the ledger, which has exactly one open entry).

The 67 KB mechanical design of the tag carrier (2026-07-24, three ADR-lite decisions) is referenced NOWHERE in docs/project_state.json — the strings 'placard_mount', 'frangible', 'anemometer', 'autotune', 'VIBE', 'index disc' all return False against the whole contract. So build_tab's target ladder (tgt-01..tgt-08) omits all nine of its $0 pre-field bench gates: mass check, CG hand-balance, the GPS-transparency test (the doc's own highest-value check), ELRS link with/without panel, compass cal in the flight index, hover VIBE check, the 60-80 N frangible pull test, target-side prop clearance (a 450 mm panel passes ~19 mm from two prop discs), and autotune with the placard fitted. Per CLAUDE.md, an update that isn't in the contract didn't happen.

### [stale/stranded] build_tab and bom_tiers are stale on the placard: they still say the sizing validation has NOT run and to hold the print
- **Priority:** P1 — an internally contradictory contract on the single number you cannot un-print
- **Evidence:** docs/project_state.json build_tab.subsystems[target].parts (placard notes verbatim) and build_tab step gnd-02 text; contradicted by docs/placard_sizing.md TL;DR, docs/placard_mount.md §2, and the contract headline's own '[2026-07-21] PLACARD SIZED 0.35 m (P0.2 — print it)'.

P0.2 landed 2026-07-21 (auto-labeler validated, IoU 0.965; placard ADOPTED at 0.35 m tag / 0.45 m sheet; print UNBLOCKED), but the Build tab was never swept. The target subsystem's placard part still reads 'SIZE TBD BY P0.2 — the apriltag-world auto-labeler validation that sizes it has NOT run yet ... fall back to the ~0.3 m BOM default'; step gnd-02 still says 'hold the placard print for P0.2 sizing'; the bom_tiers item title is still 'AprilTag tag36h11 placard (~0.3 m print, rigid)'. A builder working the Build sheet would defer the print, or print the ~0.3 m fallback — which placard_sizing.md says does not clear the gate. The sheet-vs-tag distinction (450 mm object, 350 mm black square) is absent from the contract entirely.

### [open] Four placard_mount §11 protocol edits never merged: anemometer/spares, pack budget, wind-and-index limits, target mass
- **Priority:** P1 — each is a day-of failure mode (unenforceable validity limits, running out of packs mid-matrix)
- **Evidence:** `grep -n 'anemometer|wind|packs|700–900' docs/tripod_test_protocol.md` → only :298 ('With 3× 6S 1500 mAh packs') and :727 ('~700–900 g class'); no anemometer, no wind-limit section; docs/placard_mount.md §11 items 2/7/8/12 and §8.

The 2026-07-24 protocol fix merged items 1/3/4/9(partial)/11 but left four. §2 'What to bring' has no handheld anemometer (~$20, in NO BOM tier — without it §8's <=3 m/s crosswind / <=5 m/s gust validity limits are unenforceable and crosswind becomes an uncontrolled variable in curve (a)), no spare shoes, no spare panel, no M3 nylon hardware. §4.6 still plans 3 packs against a derived 25-35% endurance loss with the placard fitted (doc says 4-5). There is no §4.7 importing the operating limits (<=4 m/s nose-on hard cap; <=3 m/s ground-handling wind with the panel fitted — tip-over calculated at 4.3 m/s; brief the leg within ±30° of the wind line). §9 still cites '~700-900 g class' against a ~610 g bare / ~805 g placard-fitted roll-up.

### [builder-decision] RAM RADIUS 0.36 m vs 0.5 m — raised to the builder, still unratified, and the code default is still 0.5
- **Priority:** P1 — a one-way-door input to the Tier-2 airframe choice; only the builder can ratify
- **Evidence:** docs/project_state.json stages[kill].note ('RAISED TO THE BUILDER, not yet ratified') + changelog 2026-07-24; scripts/field_score.py:120 with the caveat text at :748-752.

The 2026-07-24 full review found the 0.5 m ram radius quoted throughout was derived for a 7-INCH airframe; both ordered aircraft are 5-inch, giving a ~0.36 m maximum tip-to-tip contact envelope — a ~10% tighter bar. Against 0.36 m the best measured batch scores 4/96, and 0/96 at 0.25 m, while the open-loop dash floor is ~0.71 m. The contract itself calls this 'the single most consequential number in the project' and says it should be settled BEFORE the Tier-2 order; the only physical lever that widens the envelope is a bigger interceptor, which is a Tier-2 BOM decision. field_score.py still ships DEFAULT_LETHAL_RADIUS_M = 0.5 (it warns about the 7-inch provenance in its output but does not change the default).

### [builder-decision] Regulatory regime and site are 'confirmed' by the builder but the specifics are captured nowhere, and the contract disagrees with itself
- **Priority:** P1 — legal blocker on every outdoor step; the write-down is 10 minutes and only the builder has the facts
- **Evidence:** docs/project_state.json stages[hardware].note ('Specifics ... are not yet captured here') vs build_tab.subsystems[ground].parts 'FAA registration + Remote ID decision' status=must-add / notes 'STILL OPEN'; docs/tripod_test_protocol.md:725-740.

stages[hardware].note records that on 2026-07-24 the builder confirmed he HAS the regulatory basis AND a site, and explicitly notes the specifics — which regime, which site, and whether a deliberate two-aircraft mid-air is permitted IN WRITING — are 'not yet captured here'. Meanwhile build_tab's FAA part still reads 'STILL OPEN: the field choice — FRIA field ($0) vs a broadcast Remote-ID module; decide before the first outdoor flight', and gnd-05 is still a hard gate. Protocol §9's checklist is unchecked and cites the wrong airframe mass. The contract simultaneously says cleared and open, and a fresh session cannot tell which venue rules bind the tripod day vs the kill day (the note itself flags these as separate venue questions).

### [builder-decision] Two aircraft, one TX — the open S2 audit decision was never made and only one Pocket M2 was ordered
- **Priority:** P1 — safety-relevant builder call with hardware lead time; deferrable past the tripod day, not past the first joint flight
- **Evidence:** docs/hardware_order_list.md §0b '⚠️ OPEN DECISION (yours — audit S2)'; configs/target_kakute/target.param:210 ('FAILSAFE / GEOFENCE -- OPEN DECISION: "two aircraft, one TX"'); docs/project_state.json build_tab ground TX part notes.

The pre-purchase audit flagged that one TX binding both aircraft requires ELRS Model Match, which leaves the UNSELECTED aircraft in RC failsafe — so while the operator holds the interceptor's kill switch, the target has NO independent RC kill. §0c's own recommendation was 2x Pocket ($130, 'also fixes the §0b S2 two-aircraft-kill hole'); the placed order has one. The alternative (target continues in AUTO on RC failsafe behind a tight geofence) is already pre-baked into the target params (FS_THR_ENABLE=1, FS_OPTIONS=1, FENCE_ENABLE=1, FENCE_RADIUS=80) under a comment that still reads 'OPEN DECISION'. This bites at the first two-aircraft session, not the tripod day (which flies the target alone), but a second TX is a shipping item.

### [open] skr-07 (Pi 5 fps bench) has become a hard GATE INPUT, not a ride-along — and it is parts-blocked
- **Priority:** P1 — sequencing hazard: doing it after the field day cannot rescue a session captured at the wrong rate
- **Evidence:** scripts/seeker/tripod_score.py:631-780 (gate_verdict, 'STREAM FPS IS REQUIRED, NEVER INVENTED (2026-07-24)') + constants at :135-150; docs/tripod_test_protocol.md:658-695; build_tab step skr-07.

tripod_score.py deliberately has no stream_fps default: with neither meta.json stream_fps nor --stream-fps it returns UNCERTAIN naming the missing measurement (correct fail-closed behaviour after the invented-30-fps defect). The gate flips across ~24 fps at the predicted R_decode90, the deployed loop runs 20 Hz, and the only measured AprilTag cadence in the repo is ~14 Hz. So the $740 verdict now literally depends on a bench number obtainable only on the real Pi 5, which must be a sustained thermal-soak decode cadence at the FLYING quad_decimate (itself unsettable — see that item), and must not be confused with the recorder's capture rate (the recorder pays a PNG write the flight loop does not). Sequencing risk: measure before or at the field day, never after.

### [open] The brn-05 bench gate exercises seeker_loop.py, not real_flight.py — the actual flight executor has no hardware bench check
- **Priority:** P1 — the bench day is the only chance to retire these before props spin near a second aircraft
- **Evidence:** `grep -rln real_flight scripts/*.sh` → run_tests.sh, check_real_flight_sitl.sh only; scripts/check_deploy_bench.sh header ('drives flight/deploy/seeker_loop.py ... build_tab gate step brn-05'); docs/review2_silent_failure_findings.md:555; `grep -c TODO-BUILDER flight/deploy/real_flight.py` = 20.

ADR-0081 made flight/deploy/real_flight.py the onboard state machine (STANDBY→DASH→ENGAGE→SAFE) and it is SITL-verified, but the contract's props-off bench gate (brn-05) and scripts/check_deploy_bench.sh both drive the older flight/deploy/seeker_loop.py OFFBOARD path. No script drives real_flight against a real FC. Review 2 additionally found that neither bench item L3 nor the link-denied flight arm L11 exercises the STANDBY link-loss failsafe (L11 kills the TX post-GO, ignored by design) and recommended a dedicated bench check: power the TX down while in STANDBY, expect SAFE within ~1 s. real_flight.py also still carries 20 TODO-BUILDER constants (RC channel index/PWM endpoints/link timeout, standby alt/tol, dash speed, geofence) that only a bench can retire.

### [open] Parked-until-parts bench queue (skr-05, skr-06, brn-05) — tooling exists, dependencies differ and one is unwritten
- **Priority:** P2 — genuinely parts-blocked; the actionable slice now is writing the GPS-fix precondition into brn-05 and buying the board
- **Evidence:** `bash scripts/field/selftest.sh` → 'PASS, 0 failure(s)' (run 2026-07-25); docs/project_state.json build_tab steps skr-05/skr-06/brn-05; scripts/check_deploy_bench.sh:22-31 (GPS-fix requirement); docs/tripod_test_protocol.md:73-78 (networking dry run, unchecked).

Three bench gates are correctly parked but their preconditions differ. skr-05 (pi_capture on the real Pi: picamera2 backend, confirm the sensor actually applies <=1 ms exposure into meta.json) is purely Pi-blocked; the recorder is built and dir-replay verified, and scripts/field/selftest.sh is green at the desk (0 failures, run this session). skr-06 (>=15 checkerboard views, RMS <= 1.0 px, plus re-measuring the delivered adjustable lens FoV) is blocked on a checkerboard that does not exist. brn-05 needs the 6C Mini plus a GPS fix indoors-by-a-window — PX4 will not accept OFFBOARD velocity-NED without an EKF horizontal position and bench.params sets COM_ARM_WO_GPS=0 — a practical constraint written in the script header but NOT in the build_tab step. The laptop↔Pi networking dry run (ssh key, hotspot profile, IP-not-.local) is a protocol P0 checkbox that also cannot be ticked until the Pi boots.

### [open] Launch-mechanism bench items L5/L6/L7 are in no build ladder and one is tripod-blocked
- **Priority:** P2 — cheap, parallel to the tripod campaign, and currently invisible to the work ladder
- **Evidence:** docs/launch_mechanism_plan.md:319-338 (L1-L7 table + 'Still missing to complete the now column'); build_tab rungs B0-B4 refs contain no launch-mechanism steps; the contract mentions launch_mechanism_plan only in two changelog notes.

docs/launch_mechanism_plan.md §7 lists a 'now, with parts in hand' column: L1/L2 are done (real_flight.py + the latch-once honesty assert), L3/L4 map roughly onto brn-03/04 and skr-06, but L5 (bearing-latch accuracy bench — tripod the camera, survey placard bearings, measure latched-bearing 1σ against the <<3 degree budget), L6 (trigger-latency measurement, which decides whether the bearing latch is mandatory) and L7 (print the launch plate: skid pocket, scribed north line, protractor arc, stake holes) appear in no build_tab rung and in no contract step. L5 needs the same unbought tripod as the field day so it could ride that purchase; L6 needs only the TX, FC and Pi. The plan's §3 aim-error budget — the thing that decides whether the launch mechanism works at all — stays unmeasured until L5/L6 run.

### [stale/stranded] Stale build-facing rows: ordered frame, tag size, tools-owned, and the target-FC constraint
- **Priority:** P2 — cheap sweep, but (1) feeds a HARD clearance gate and (3) risks a duplicate order
- **Evidence:** docs/hardware_order_list.md:264 (GEPRC Mark4) and the tag row in §E; docs/project_state.json bom_tiers[0] must-add build-layer note vs build_tab.subsystems[ground] tool/solder part notes; constraints[target-is-ardupilot].text.

Four drift spots a builder shops or builds from. (1) BOM §E still lists 'Budget 5" carbon frame (GEPRC Mark4 or equiv ~220mm)' while the ordered frame is the TBS Source One V6 at 240 mm — the 20 mm changes the prop-clearance corridor to 21.4 mm and every clearance number in placard_mount §5.5 keys off it. (2) The same §E row prints the tag as '~0.25-0.35 m' (names the tag, not the 450 mm sheet). (3) bom_tiers' must-add row still says 'STILL MISSING: the soldering IRON itself ... zip ties, multimeter if unowned' while build_tab and the headline record iron/zip ties/prop wrench/multimeter as OWNED (builder confirmed 2026-07-20) — a shopping list that would double-buy. (4) The constraint target-is-ardupilot still names 'SpeedyBee F405, BOM §E' as the target FC though the ordered board is the Kakute H7 (the ledger entry target-fc-matek-vs-kakute is resolved, but the constraint text was never swept).

### [stale/stranded] docs/real_build_coded_dash.md still says no fixed tilt was adopted and adaptive tilt is the open lever — the opposite of the committed decision
- **Priority:** P2 — doc-only, but it is a reversed conclusion sitting in prose a reader actually reads (the same-turn drift-sweep rule)
- **Evidence:** docs/real_build_coded_dash.md:129 vs docs/project_state.json stages[pointing].active ('FINAL (builder 2026-07-17): FIXED up-tilt mount — no servo'); docs/goals.md STATUS banner naming real_build_coded_dash.md as current truth.

The build reference's hardware-mapping table describes the up-tilt bracket as 'a FIXED tilt does NOT close the dash-pitch gap — it only relocates the in-view window (add #18j-fix; no fixed tilt adopted, ADR-0067/0068 — adaptive tilt #46 is the open lever)'. The contract's pointing stage records the opposite as FINAL: the builder chose a FIXED up-tilt mount on 2026-07-17, no servo, adaptive REJECTED. This doc is named in docs/goals.md's STATUS banner as one of three sources of 'current truth' for the real build, so the stale row sits on a surface readers are explicitly sent to, and it is not in the contradiction ledger. It also matters mechanically: it is the row that tells you what bracket to print.

### [open] The open wedge-sizing contradiction bites at print time and the pointing stage still asserts the un-hedged number
- **Priority:** P2 — does not bind until Tier-2 P3, but the bracket range is a print decision made then
- **Evidence:** docs/project_state.json contradictions[wedge-sizing-vs-accel-cap] (status open; resolution 'Do NOT freeze the wedge at the full dash pitch before then'); build_plan P3 task 3 and docs/hardware_order_list.md:229 (adjustable 10-30 deg bracket).

wedge-sizing-vs-accel-cap is the contract's ONLY open contradiction (verdict CONFIRMED, severity medium): the committed 'wedge angle = measured dash pitch (~25-40 deg)' was premised on an uncapped flat dash, while the Phase-A in-frame A/B shows that once the dash is accel-capped to ~20 deg pitch a wedge >=20 deg DUMPS the <5 m terminal target out the frame bottom, so the wedge must be ~10-15 deg and co-sized with the cap. Its resolution path is a serialized Gazebo confirmation of the accel-capped dash, and it explicitly says do NOT freeze the wedge at full dash pitch. The build-side hedge is already right (print an ADJUSTABLE 10-30 deg bracket, lock after the first dash ULog), but stages[pointing].active still reads FINAL at dash pitch — and a 25-40 deg lock would fall outside a 10-30 deg bracket's range anyway.

### [improvement-idea] The target config pack has no CI gate — which is how the stale mission geometry survived
- **Priority:** P2 — the mechanical fix that prevents the P0 mission-geometry item from recurring
- **Evidence:** `grep -n target_kakute scripts/run_tests.sh` = no hits (only scripts/field/selftest.sh at :106-107); `bash configs/target_kakute/selftest.sh` → 'ALL TARGET-CONFIG-PACK CHECKS PASSED' while shipping 8 m / 17 m defaults.

configs/target_kakute/selftest.sh runs green (verified this session: gen_tripod_mission --self-test PASS, lint_param 44 params PASS) but is not invoked by scripts/run_tests.sh, which wires only scripts/field/selftest.sh. So target.param and the mission generator can drift from the protocol indefinitely with every check green — exactly what happened. Two improvements: (a) wire configs/target_kakute/selftest.sh into run_tests.sh; (b) give the generator's self-test teeth by asserting protocol conformance, not just structure — near-range <= 4 m, tag-envelope crossing standoff in 5-6 m, and station spacing contiguous on the 2 m bin the scorer's r90 walk requires. Same pattern as the review-2 producer→consumer contract tests: the protocol is the producer, the mission file the consumer.

### [improvement-idea] Physical build progress is not durable: build_tab steps carry no status and nothing records which parts arrived
- **Priority:** P2 — improvement, but it is the difference between the build phase being contract-tracked and chat-tracked
- **Evidence:** python dump of build_tab.subsystems[*].steps shows keys {id, text, why, gate} only, no status; scripts/render_dashboard.py:29,182 ('localStorage checkboxes'); `grep -rn 'arrived|delivered|in hand' docs/next.md docs/project_state.json` returns no delivery record.

All 30 build_tab steps (tgt-01..08, skr-01..08, brn-01..06, gnd-01..08) have only id/text/why/gate keys — no status field — and the dashboard renders them as browser-localStorage checkboxes. So 'what is physically done on the bench' lives in one browser profile, survives no /clear, appears in no git diff, and cannot be audited by the drift check. Nothing records delivery either: the order was placed 2026-07-20 and there is no arrival/inventory state anywhere, so a fresh session cannot tell whether the boxes are on the table. A per-step status (like every other contract object) plus a received/not-received flag per ordered part would make the build phase as auditable as the sim phase already is.

### [improvement-idea] No decision-grade protocol exists for the kill day, only for the tripod day
- **Priority:** P2 — not on the tripod-day critical path, but it is the next protocol-shaped hole and it is sim-free work
- **Evidence:** docs/tripod_test_protocol.md (46 KB, 12 sections) vs docs/project_state.json build_plan phase P5 (4 task bullets, no gate criteria); scripts/field_score.py exists but no protocol drives it; docs/launch_mechanism_plan.md §4 is narrative, not a checklist.

The tripod afternoon has a 46 KB decision-grade protocol with a pre-session checklist, survey inputs, a capture matrix, a three-step scoring sequence, pass/no-go thresholds and a done-definition. The first-kill day (build_plan P5: static intercept → moving straight-line → the speed ladder to 9 m/s) has only five bullets in the contract plus launch_mechanism_plan §4's pointing-and-firing narrative, and its evidence chain (seeker video + phone slow-mo + both ULogs, scored by field_score.py) has no pre-registered success/abort criteria, no safety geometry, no pass card, and no clock-sync procedure equivalent to §4.6b. Since binary-kill evidence IS the project's success criterion, the kill day deserves the same treatment — and it should be written while the tripod protocol's lessons (survey first, sync pass per battery segment, no vacuous verdicts) are fresh.

**Reader notes:** METHOD: digested docs/project_state.json via targeted python (headline, bom_tiers, build_tab.scope/ladder/subsystems/parts/steps, build_plan P0-P5, constraints, contradictions, stages[hardware]/[kill]); read docs/hardware_order_list.md §0b/§0c/§0d/§E/§F, docs/tripod_test_protocol.md (§0-§12), docs/placard_sizing.md (incl. the ADR-0082 correction), docs/placard_mount.md (§8/§9/§10/§11/§12/§13), docs/launch_mechanism_plan.md §7-§9, docs/field_bringup.md §7, docs/real_build_coded_dash.md, docs/review2_action_plan.md; cross-checked every claim against code with grep and two live runs. All paths above are relative to ~/interceptor-sim.

RUNS EXECUTED (this session, idle load, no sim touched): `bash ~/interceptor-sim/scripts/field/selftest.sh` -> PASS, 0 failures (all six field scripts honor their exit contracts; the desk-side field pack is genuinely green). `bash ~/interceptor-sim/configs/target_kakute/selftest.sh` -> PASS (mission generator self-test + param lint) - but that pass is exactly the point of the CI item: it is structural, not protocol-conformant.

THE HEADLINE: the most dangerous item in this slice is #1 - the protocol's false-NO-GO geometry was fixed in prose on 2026-07-24 (dbca8cf) but ~/interceptor-sim/configs/target_kakute/gen_tripod_mission.py still ships the pre-correction defaults and is referenced by nothing. Combined with #2 (qd=1.0 unsettable) and #6 (no incidence scoring), the tripod day as currently equipped can be flown correctly per the protocol and still return a verdict the money gate cannot honestly consume. All three are desk-cheap and should land before any parts are unboxed.

VERIFIED-MISSING vs the task prompt's list: Kakute microSD, LiPo bag/glasses/blanket, Pi 5 cooler, >=27 W PD power bank, tripod+head, buzzer are ALL still must-add in the contract as of the last order update (2026-07-20b) - the prompt's list is accurate and current. Newly surfaced unordered items NOT in that list: handheld anemometer (in no BOM tier at all), 2 carbon spars + PETG filament, M3 nylon hardware + rubber bands, 3 spare frangible shoes + 1 spare panel, cones/tape measure, and an unverified 8 mm prop socket in the ordered hex kit.

SCOPE NOTES: I did not re-litigate anything in the graveyard (foveated crop, adaptive tilt, RTK, AR0234 reorder, auto-fire) or any superseded/rejected stage. I did not audit the sim/guidance, detector/NN, or remaining review-2 code slices except where they cross into the field chain. I made no edits to any file - this is a read-only enumeration. Three items (#10 ram radius, #11 regulatory specifics, #12 two-TX) are builder-decision and are flagged as such rather than proposed as work.


## Reader 5 — Repo & evidence health

### [open] CI has failed on 40/40 pushes — the remote gate is dead and nobody is watching it
- **Priority:** P0 — the only automated gate that runs outside the builder's machine is 100% red, so it can no longer detect any regression, and the repo is slated to go public with an all-red Actions tab
- **Evidence:** `gh run list --limit 40` → Counter({'failure': 39, '': 1}), successes=0; job 89665334423 step "Run the offline suite (run_tests.sh stage 1, .venv)" = failure; CI log: `FAILED tests/test_ground_station.py::test_detection_miss_yields_status_miss_and_no_wire_payload - FileNotFoundError: [Errno 2] No such file or directory: '/home/runner/work/interceptor-sim/interceptor-sim/logs/rig_captures/full_sweep_20260709T015530Z/capture_meta.json'`; `git ls-files logs/rig_captures/ | wc -l` = 0

Local `scripts/run_tests.sh` is ALL GREEN (428 passed, 2 skipped, exit 0), but GitHub Actions has failed on every single run going back to at least 2026-07-24T21:16Z — 39 failures + 1 in-flight that also failed, zero successes in the last 40. Root cause: CI stage 1 (`pytest tests/ flight/tests/`) collects `tests/test_ground_station.py`, whose module-level fixtures read `logs/rig_captures/full_sweep_20260709T015530Z/capture_meta.json` (tests/test_ground_station.py:87,95,100) with NO existence guard. That directory is gitignored (`.gitignore:11 logs/*`, not re-included; `git ls-files logs/rig_captures/ | wc -l` = 0), so a clean runner gets `1 failed, 415 passed, 4 skipped, 10 errors`. It passes locally only because this machine happens to hold the capture. The ci.yml header explicitly anticipated the gitignored-data problem for stage 2 (the ONNX venv) but missed that the SAME data dependency sits in stage 1's non-ONNX cases — it excluded the venv, not the test file. Fix: skip-guard the capture dir (module-level `pytest.skip(allow_module_level=True)` on absence) or deselect the file in CI, matching the stage-2 rationale.

### [open] CI stages 3 and 4 (dashboard drift + self-test coverage) have NEVER executed on the remote
- **Priority:** P0 — a fix is not done until its effect is observed end-to-end (CLAUDE.md standing rule); this one has never run
- **Evidence:** `gh run view 30152580166 --json jobs`: `failure | Run the offline suite (run_tests.sh stage 1, .venv)`, then `skipped | Project-state dashboard drift check (run_tests.sh stage 3)`, `skipped | Uncovered --self-test entry points (run_tests.sh stage 4)`; .github/workflows/ci.yml header lines describing stage 4 as the fix for "before 2026-07-25 only 1 of 29 self-tests was reachable from CI"

Because stage 1 fails, the two later CI steps are `skipped`, not run. That means the entire review-2 "green != ran" hardening committed 2026-07-25 (8e10b12, 8cdbc77, 0a41fd9) — the project-state drift check and the newly-added `--self-test` entry-point coverage, added precisely to close the "only 1 of 29 self-tests was reachable from CI" finding — has never actually executed in CI even once. This is the project's own silent-failure pattern inverted: the prevention infrastructure was committed, documented as landed, and is inert remotely. Fix is independent of finding #1: add `if: always()` (or split into separate jobs) so a stage-1 failure cannot mask the drift check and the self-test gates.

### [open] COAST_STALE_S numeric value is still unpinned, and the test's own comment falsely claims it is protected
- **Priority:** P1 — a comment that overstates its own coverage is the project's named defect class, and it sits inside a test file (an instrument)
- **Evidence:** `grep -rn "COAST_STALE_S" tests/ flight/tests/ | grep -i assert` → 0 hits; tests/test_cue_staleness.py:49-51 (claim) vs :236-241 (AST-name-only assertion); scripts/m4_intercept.py:544; docs/audit_findings_tracker.md:81 DEEP-T1 "**Remaining:** the COAST_STALE_S value pin…"

`tests/test_cue_staleness.py:49-51` states the horizon literal is "kept as a literal here so a silent change to that default trips test_m4_exposes_cue_stale_horizon_flag." That is not true: `test_m4_exposes_cue_stale_horizon_flag` (lines 226-241) is a pure AST check that the argparse default is the *Name* `COAST_STALE_S` — it never reads the value. No test anywhere asserts the number. So `scripts/m4_intercept.py:544 COAST_STALE_S = 1.0` could drift to 10.0 and the whole suite stays green — exactly the DEEP-T1 complaint, still listed as Remaining in the tracker. The overclaiming comment is the worse half: it will stop a future reader from adding the real pin. Fix: one `assert COAST_STALE_S == 1.0` with a provenance comment, and correct the misleading comment.

### [open] solve_intercept_time (PIP lead-time math) has zero test coverage on a live guidance path
- **Priority:** P1 — untested pure math on the flight guidance path, and cheap to close
- **Evidence:** `for h in …; do grep -rl "$h" tests/ flight/tests/; done` → `solve_intercept_time: 0 test file(s)` (wrap_pi 4, kalata_alpha_beta 1, compute_v_close 2); defs at scripts/m4_intercept.py:1127 and scripts/guidance_lab.py:1490; docs/audit_findings_tracker.md:81

Named in DEEP-T1 as an untested pure helper and still untested. It is defined at scripts/m4_intercept.py:1127 and called on two live guidance branches (:3330, :3665), plus a parallel definition in scripts/guidance_lab.py:1490 with three call sites. It computes predicted-intercept-point lead time — a quantity that directly moves miss distance — and it is a pure function, so a test is cheap. Note the duplicated definition across m4_intercept and guidance_lab is itself a drift hazard (the lab is the design-time surrogate that ranks options; if the two diverge, lab rankings stop predicting Gazebo).

### [improvement-idea] run_tests.sh stage 4 covers 3 of 40 --self-test entry points, with an unenforced prose skip-list
- **Priority:** P2 — real coverage-drift hazard, but the three highest-stakes tools (kill scorer, deployed loop, field pack) are already in
- **Evidence:** scripts/run_tests.sh:82-107 (three `run_selftest` calls + the "DELIBERATE SKIP SET" comment at :85-88); `grep -rl -- "--self-test" scripts/ flight/ | wc -l` = 40

Stage 4 hardcodes three self-tests (field_score, seeker_loop, field/selftest.sh). The other ~37 are excluded by a comment block declaring a "DELIBERATE SKIP SET" that claims they either duplicate stage-1 pytest coverage or need weights/GPU/hardware. Nothing enforces that claim: the mapping is never verified, and a self-test added tomorrow is silently uncovered with no signal. This is a weaker version of the gap stage 4 was built to close. Fix: enumerate `--self-test` entry points at runtime and fail if any is neither in stage 4 nor in an explicit, named allow-list file — so the skip set becomes data that must be maintained, not prose that rots.

### [open] 02_apriltag_desk_check.sh throws away a valid real capture when a newer self-test replay exists
- **Priority:** P2 — cannot cause a wrong verdict, but wastes scarce tripod-day time on a tool the field protocol depends on
- **Evidence:** scripts/field/02_apriltag_desk_check.sh:111 `LATEST="$(ls -1dt "$FLD_LOGS"/01_camera_*/session … | head -1 …)"`, :112-115 replay → `LATEST=""` → :120 SIM fallback; logs/field/01_camera_20260725T180750Z/session/meta.json `"session": "fieldcheck-replay"` created by my run_tests.sh invocation

The no-arg path takes only `head -1` of `logs/field/01_camera_*/session` (line 111). If that single newest session is a replay it sets `LATEST=""` (line 114) and falls through to the repo's SIM fixture as a dry run — it never scans further back for the newest NON-replay session. Running `scripts/run_tests.sh` creates replay sessions in exactly that glob (my one invocation created `01_camera_20260725T180748Z` and `...180750Z`, meta.json `"session": "fieldcheck-replay"`). So on the tripod day: capture real frames, run the suite as the methodology encourages, then run the desk check with no args → it ignores the real capture and grades sim frames. It is loud (`fld_miss` + DRY=1), so it cannot produce a false PASS — but it burns field time and discards good evidence. Fix: iterate the sorted list to the first non-replay session instead of blanking on the first.

### [improvement-idea] run_tests.sh pollutes logs/field/ with ~8 real-looking capture dirs per invocation
- **Priority:** P2 — evidence-directory hygiene ahead of the tripod day
- **Evidence:** `ls -d logs/field/*/ | wc -l` = 34; 8 dirs stamped 20260725T18074[89]Z–180750Z created by my single `scripts/run_tests.sh` run at 12:07 MDT; scripts/field/selftest.sh:30

Stage 4's `scripts/field/selftest.sh` writes timestamped directories into `logs/field/` that are named identically to genuine field captures (`00_detect_devices_*`, `01_camera_*`, `02_apriltag_*`, `03_fc_*`, `05_pilink_*`). My single test run added 8 dirs; the directory now holds 34. Only two are self-identifying by name (`selftest_empty`, `selftest_copyfail`) — the rest are distinguishable only by opening meta.json. This is the mechanism behind finding #6 and it makes the field evidence directory harder to audit after the fact, which matters given the project's "instruments are evidence" rule. Fix: point the self-test at a temp dir (it already does `mktemp -d` at selftest.sh:30 for part of its work) or prefix the dirs `selftest_`.

### [improvement-idea] 12 dead experiment scripts with zero references anywhere in docs or code
- **Priority:** P2 — pure tidiness, but it is portfolio-visible surface area
- **Evidence:** Orphan scan output (script at /tmp/claude-1000/-home-emerson-interceptor-sim/f68f5895-f3ec-4d04-aee6-345e425c9c9f/scratchpad/deadscan.py): scripts/bird_mc_harness.py (07-08), gen_demo_ground_texture.py (07-07), plot_t21_maneuver.py (07-09), wait_batch_string.sh (07-09), seeker/analyze_v2_ab.py (07-08), seeker/plot_seeker_ab.py (07-08), seeker/phantom_forensics_v3.py (07-09), seeker/verify_grid_labels_v3.py (07-09), seeker/probe_v2.sh (07-11), seeker/verify_autocrop.py (07-12), seeker/prop_mask.py (07-20), seeker/nn_tier/onnx_smoke_test_aug.py (07-21)

A full scan of 218 scripts against docs/, tests/, flight/, scripts/ and .github/ found 12 with no reference from any document or any other file (self-references excluded). All date from 2026-07-07 to 2026-07-21 and appear to be one-off experiment/plot/probe tooling that outlived its analysis. They are harmless but they inflate the tree a portfolio reviewer reads, and a few (verify_autocrop.py, verify_grid_labels_v3.py, phantom_forensics_v3.py) have names suggesting verification tooling that is no longer wired to anything — the kind of thing someone re-runs and trusts. Suggest moving to `scripts/experiments/attic/` with a one-line README, rather than deleting, so the analyses stay reproducible.

### [builder-decision] Sole open contradiction (wedge-sizing-vs-accel-cap) is gated on a serialized Gazebo run that has not happened
- **Priority:** P1 — a hardware-shaping decision (wedge angle) is committed at a value its own evidence says is wrong; it should not be cut before the Gazebo run
- **Evidence:** docs/project_state.json contradiction id `wedge-sizing-vs-accel-cap`, verdict CONFIRMED, severity medium, status open; `render_dashboard.py --check` → "37 contradictions [1 open]"; loc_b: scripts/experiments/loft_dive/inframe_ab.py + inframe_ab_results.csv

The contract carries exactly one open ledger entry: the committed pointing decision sizes the fixed up-tilt wedge to the full dash pitch (~25-40 deg), while the Phase A in-frame A/B found that under an accel-capped (~20 deg) dash a wedge >=20 deg dumps the <5 m terminal target out the frame bottom, so the wedge must be ~10-15 deg. Its own resolution field says the serialized Gazebo confirmation of the accel-capped dash settles the joint sizing and instructs "Do NOT freeze the wedge at the full dash pitch before then." Flagging from the evidence-health angle: this is a known-wrong committed decision sitting in the contract, and the gating run is a sim task that can be queued now since no sim is currently running. May overlap another slice's stage review.

**Reader notes:** SLICE 5 raw check results (all commands run from ~/interceptor-sim).

(a) scripts/run_tests.sh — EXIT 0, ALL GREEN, ~16 s wall. Stages: [1/4] 428 passed 2 skipped in 7.84s; [2/4] ONNX parity 28 passed in 3.77s; [3/4] drift OK; [4/4] 3 self-tests OK. Full log at /tmp/claude-1000/-home-emerson-interceptor-sim/f68f5895-f3ec-4d04-aee6-345e425c9c9f/scratchpad/run_tests.log.

(b) Git — CLEAN otherwise. At session start: ` M .claude/settings.json` + `?? scripts/hooks/` (an Opus-4.8 subagent block hook, mtime 12:05, i.e. created while I was starting). That work was committed AND pushed by another agent during my run (run 30169037375 "Hard-block Opus 4.8 for all subagent spawns"), so it is in-flight, not stranded — no action. `git log origin/main..main` = EMPTY (nothing unpushed). `git worktree list` = main only; `.git/worktrees` does not exist; no stray `interceptor-sim-*` dirs. Branches: `main` + `remotes/origin/main` only — no stranded branches. Remote = https://github.com/Swampyemerson/interceptor-sim.git.

(c) `python3 scripts/render_dashboard.py --check` → exit 0, no drift; 10 stages, 16 decisions, 37 contradictions [1 open]. NOTE the tool itself prints "prose wording NOT checked" — the number-traceability guard covers 20 visible numbers but not narrative claims, consistent with the known JSON-only drift-check limitation in CLAUDE.md.

(d) logs/ — 10,534 files, 377 MB, all gitignored. Newest writes are 02:30-03:14 today plus my own 12:07 test artifacts; nothing since. Process scan via a script FILE (never an inline literal pattern, per the exit-144 rule): ZERO px4/gz/mc_batch/seeker/setsid processes running. No .pid/.lock/RUNNING markers under logs/. Conclusion: no stale in-flight daemons, sim is free — a Gazebo run can be queued now.

(e) TODO/FIXME scan of scripts/ — 15 markers across 6 files, all benign and self-documenting (mostly `TODO(sim)` verification tags in scripts/seeker_v3_capture.py, 8 of them, tracking constants to confirm live). Not worth an item on its own; folded into context.

HEADLINE: the one serious finding is that CI is red on 40/40 runs from a single fixable cause (gitignored test fixture), and that redness has silently disabled the drift check and the brand-new self-test coverage on the remote. Everything else is P1/P2 hygiene. Local repo state is genuinely clean — nothing uncommitted, nothing unpushed, no stranded worktrees or branches, no orphaned daemons.


## Reader 6 — Portfolio readiness

### [open] CI has NEVER been green — 72/72 runs failed — while the contract headline asserts "first CI GREEN"
- **Priority:** P0 — a permanently red CI badge plus a falsified headline claim is the single worst first impression on a public repo, and it is a ~1-commit fix
- **Evidence:** gh run list (72 runs, 71 failure / 1 blank); job 89665334423 step 6 FAIL; CI log: `FileNotFoundError: .../logs/rig_captures/full_sweep_20260709T015530Z/capture_meta.json` at scripts/ground_station/triangulate.py:260; local `.venv/bin/python -m pytest tests/test_ground_station.py -q` -> 26 passed; docs/project_state.json headline text "first CI GREEN"

`gh run list --limit 200` returns 72 runs, conclusion counter `{'failure': 71, '': 1}`; the oldest run (2026-07-21T03:35:19Z, the very first push) already failed. Root cause is a single class: `tests/test_ground_station.py` resolves `DEFAULT_CAPTURE_DIR` to `logs/rig_captures/full_sweep_20260709T015530Z/`, which is gitignored (`logs/*` with no `!` re-include; `git ls-files logs/rig_captures` returns nothing). CI (a clean clone) reads `1 failed, 415 passed, 4 skipped, 10 errors`; the same file run locally reads `26 passed, 2 skipped`. This is simultaneously (a) a portfolio blocker — a red X sits on every commit and a recruiter who clones and runs `scripts/run_tests.sh` gets failures, and (b) a contract-honesty defect of exactly the class CLAUDE.md names: an always-visible claim (`project_state.json` headline: "repo PUSHED to private GitHub …, first CI GREEN") that the evidence refutes and that no drift check catches because the drift check is JSON-internal. The fixture is 3.1 MB total but the failing entry point needs only `capture_meta.json` (803 B) plus `index.csv`/`centroids.csv`.

### [open] README is 5 days stale and now contradicts the contract's own current headline in both directions
- **Priority:** P0 — the README is the recruiter's first and often only surface, and it currently states as "open" two questions the project closed and omits the result that reframes the whole guidance story
- **Evidence:** git log -1 README.md = bf18db4 2026-07-20; README.md:81-83, 91-93, 115; docs/project_state.json narrative.big_lever + narrative.reframe (as_of 2026-07-24); stages detector/terminal notes; next_probe "RESOLVED 2026-07-21"

README.md was last touched in `bf18db4` (2026-07-20). Since then the contract moved substantially and the README was never swept. Concretely: (1) README:92-93 says the two perception mechanisms are "ground-clutter background … and phantom competition — both sim-testable, both open" — both were SETTLED 2026-07-21 (ground-clutter REFUTED, phantom CONFIRMED-but-modest ~12% and mount-removable) and the wall was re-decomposed to POINTING (~75% of 8-12 m terminal ticks have the target out of frame). (2) README:81-83 says "No camera-guided intercept of the realistic 3D quad target exists in the dataset" — superseded 2026-07-24 by the Arm-A dash-only control (camera 0.68 vs dash-only 2.05 m, 4/4 paired, scoped to line@9 +30bias l2r), i.e. the README now UNDER-claims a genuine win. (3) The README carries no trace of the biggest current finding, the contract's own `narrative.big_lever` headline: at correct aim the camera terminal does NOT beat the open-loop dash (combined camera median 1.02 m vs dash-only 0.75 m, tighter on only 2/8), and correct aim is the dominant free lever (ADR-0083, 0.71 -> 0.43 m). Nor ADR-0078 (mono camera decided), nor the real-data NN key number (AP50 0.44 vs 0.0003 held-out). This is the exact drift CLAUDE.md's "sweep the always-visible surfaces the SAME TURN" rule exists to prevent.

### [builder-decision] The ram-radius correction (0.5 m -> ~0.36 m) is raised-but-unratified and it moves the project's headline bar
- **Priority:** P0 — it is a one-way-door input to the ~$740 Tier-2 spend and it invalidates the radius attached to every Pk claim on every visible surface
- **Evidence:** docs/project_state.json stage `kill` note: "RAM-RADIUS CORRECTION (full review 2026-07-24, RAISED TO THE BUILDER, not yet ratified) … both ordered aircraft are 5-INCH … ~0.36 m"

The `kill` stage carries a flagged correction from the 2026-07-24 full review: the 0.5 m ram radius quoted throughout was derived for a 7-INCH airframe, but both ordered aircraft are 5-INCH, so the tip-to-tip contact envelope is ~0.36 m — the success bar is ~10% tighter than every prior claim assumed. Against that bar the best measured batch scores 4/96 @0.36 m and 0/96 @0.25 m, while the open-loop dash floor is ~0.71 m. The contract itself calls this "the single most consequential number in the project" and says it should be settled BEFORE the Tier-2 order. Until it is ratified, every kill-probability number on a portfolio surface is quoted against a radius the hardware cannot deliver, and the "camera terminal is optional decoration" framing inverts (the terminal now has to earn ~a metre, which at correct aim it does not).

### [in-flight] The T25 demo video exists only on this machine, is 62% static cards, and silently drops the handoff beat
- **Priority:** P1 — the demo is the one artifact a recruiter watches, and as composed it is a slideshow with a 0.77 s intercept
- **Evidence:** ffprobe demo_out/t25/t25_demo.mp4 duration=30.4; scripts/video/t25_shots.conf (no 4/5, has absent 7); shot4=0.300 s, shot5=0.233 s, shot6=0.767 s per ffprobe; .gitignore:63 `demo_out/*`; docs/next.md:770

`demo_out/t25/t25_demo.mp4` is 927 KB / 30.4 s and `demo_out/*` is gitignored (.gitignore:63-64), so nothing about the video reaches a cloner. Composition audit: `scripts/video/t25_shots.conf` lists shots 0,1,2,3,3b,6,7,8 — and the durations sum exactly to the file (4 + 4 + 4 + 1.267 + 5 + 0.767*8 + 6 = 30.4 s). That means ~19 s of the 30.4 s are STATIC CARDS (title 4 s, shot2 card 4 s, tilt A/B card 5 s, endcard 6 s) and only ~11.4 s is moving footage. shot7_chase.mp4 does not exist so it is silently skipped by the assembler; shot4_terminal_acquire.mp4 (0.30 s) and shot5_handoff.mp4 (0.23 s) exist as files but are NOT in the conf, so the honesty-handoff beat is absent from the assembled cut. Against the builder's four mandated elements: (1) THE TILTING is a static A/B PNG, not footage; (4) "a great interception clip" is 0.77 s of raw footage retimed 8x. docs/next.md:770 describes the cut as "shots 0/1/2/3/3b/4/5/6/8", which the conf and the arithmetic both contradict.

### [open] README embeds zero images and never mentions the demo video — the M5 gate's own deliverable
- **Priority:** P1 — highest-leverage/lowest-cost portfolio fix in the slice: embed 3-4 existing tracked PNGs plus a GIF, no new renders needed
- **Evidence:** grep for `![` in README.md -> no matches; README.md:168-172 (only textual reference to docs/images); ls docs/images/ = 29 files; docs/goals.md M5 success criterion

`grep -n '!\[' README.md` returns nothing; `grep -in 'video|demo|gif' README.md` returns only three unrelated prose hits (kill-confirmation video, not the reel). docs/goals.md M5 explicitly requires "README with architecture diagram, results, and one GUI demo GIF". The repo already has 29 tracked assets in `docs/images/` (architecture_block_diagram.png/.svg, process_flow_diagram.png, m5_miss_hist_cdf.png, m5_pk_vs_radius_by_arm.png, m5_traj_overlay.png, demo_onboard_final.png, probe1/probe2 figures, transfer_bet_contact_sheet.png) and the README only references two of them in body text. A recruiter opening the repo currently sees 318 lines of unbroken prose and tables with no visual anchor and no way to know a demo reel exists.

### [builder-decision] The rendered video's architecture is cue-era and was CUT by the 2026-07-15 pivot — including the comms-denied beat the claims-scrub HOLDs
- **Priority:** P1 — a video is the least-scopeable surface there is; a caption decision must precede any public release
- **Evidence:** docs/t25_storyboard.md (stereo opener + honesty-handoff beat); demo_out/README.md beat 4 "DATALINK DENIED -> CAMERA-ONLY TERMINAL"; docs/next.md:362-363 CUT list; docs/publish_runbook.md:52-56 (comms-denied HELD)

The T25 cut's opener (shots 1-2) is the ground-stereo rig -> triangulated cue -> handoff, and the older ADR-0032 reel's beat 4 is captioned "DATALINK DENIED -> CAMERA-ONLY TERMINAL" with a CUE_WAIT phase HUD. docs/next.md:362-363 lists CUE_WAIT/S2-cue-mock, `--fuse-midcourse`, handoff-cue-gate and coast-search as explicitly "CUT under coded-dash". Meanwhile the binding claims-scrub says "works comms-denied" is HELD everywhere and the coded-dash no-datalink story is "architectural and unflown — claim the design, never the result". So the reel's headline beat is the single scope the runbook most tightly guards. Decision needed: ship it explicitly framed as retired sim-phase history (the README already uses that framing for the block diagrams at :168-172), or re-render against the coded-dash architecture. Either is defensible; shipping it unlabelled is not.

### [open] ADR-0066 claims-scrub debt: no regenerated portfolio material exists, and a naive regeneration would resurrect cut subsystems
- **Priority:** P1 — this is the pre-declared final-mile task and the constraint on it is now under-specified; it should be re-scoped before anyone runs the regeneration
- **Evidence:** docs/decisions.md ADR-0066 (lines 1492-1516, recovery hash e6f06d3); docs/audit_findings_tracker.md:62,63,86,115,116,118,119 (all FIXED, all citing retired files); docs/next.md:118-124 (the binding constraint); docs/next.md:362-363 (fusion CUT)

ADR-0066 (2026-07-10) git-rm'd the four portfolio-facing docs (`interviewer_prep.md`, `portfolio_bullets.md`, `portfolio_visuals.md`, `WRITEUP.md`) on the premise that a dedicated agent regenerates them at project end. Those files are where the audit tracker's claim-scoping fixes actually LANDED — rows FWD-A1/A2/A5/A6 and DEEP-P4/H3/H4 are all marked FIXED with the fix living in the now-deleted text (recoverable at `git show e6f06d3:<path>`). Nothing has been regenerated yet, so the debt is entirely unpaid. Extra hazard the constraint does not state: FWD-A6's FIXED content is the fusion-capstone bullet, and mid-course cue fusion is itself CUT under the coded-dash pivot — so mechanically re-applying the FIXED rows would headline a dead subsystem. The regeneration must apply BOTH the ADR-0066 scopes AND the 2026-07-15 pivot's cut list.

### [open] docs/publish_runbook.md's claims-scrub gate is itself 5 days stale and under-specifies the public-flip bar
- **Priority:** P1 — it is the designated pre-public gate; an incomplete gate passes material it should stop
- **Evidence:** docs/publish_runbook.md:45-73 (scrub list), :3 ("Written 2026-07-20"); docs/project_state.json narrative.big_lever + stage `kill` note + next_probe (all 07-21..07-24)

The runbook (written 2026-07-20) declares its "Claims scrub — the gate before 'public' (BINDING)" section at :45-73. That list covers the pre-07-20 scopes (comms-denied HELD, no sub-meter 3D-quad camera claims, Pk 72/72 is billboard, r-squared is variance-explained, superseded numbers, jink caveat). It does NOT carry any of the four post-07-20 binding scopes: (a) G20 — at correct aim the camera terminal does not beat the open-loop dash (1.02 vs 0.75 m, 2/8); (b) the ram-radius correction to ~0.36 m; (c) the pointing reframe (ground-clutter refuted, phantom modest, 75% out-of-frame); (d) the newly-scoped Arm-A camera-driven result which must be quoted with its narrow scope (line@9 +30bias flat-dash l2r, 4/4) rather than generalised. Since this section is the literal gate the builder is told to re-read before flipping public, a stale gate is a silent-failure surface. Separately :94-98 flags `.claude/settings.local.json` as tracked — that was fixed 2026-07-21 and the runbook was never updated (see the stale item).

### [open] The honest resume line welds two different architectures into one claim
- **Priority:** P1 — it is the one sentence most likely to be copied verbatim into a resume and defended in an interview
- **Evidence:** README.md:17-20; docs/project_state.json key_numbers (Pk 72/72 "billboard-era adopted config"; M5 "running-start profile"); stage `terminal` note G20 2026-07-24 ("combined camera median 1.02 m vs dash-only 0.75 m, tighter on only 2/8")

README.md:17-20 reads: "Proportional-navigation terminal guidance handed off from a no-datalink coded dash, validated by Monte-Carlo miss statistics in PX4/Gazebo SITL; quantified the perception acquisition envelope and identified the flight-dynamic detector-recall limit." Both halves are individually true but the join is not: the Monte-Carlo miss statistics (M5 n=96 median 0.93 m; Pk@2.8 72/72 = 95.0% CP-LB) were flown on the CUE-era running-start profile against an AprilTag/flat-billboard target, not on the coded dash; the coded-dash + camera-terminal pairing on the 3D quad reads camera 1.02 m vs dash-only 0.75 m at correct aim. A reader takes the sentence to mean the no-datalink coded-dash architecture was MC-validated, which no batch in the dataset supports. The second clause is also now one refinement behind — the limit has been re-attributed from detector recall to pointing (75% out-of-frame).

### [builder-decision] Public flip, repo metadata, and the dashboard's reachability are a coupled pending builder decision
- **Priority:** P1 — 'a recruiter can read this repo' is literally gated on the visibility flip; description/topics/Pages are cheap once it lands
- **Evidence:** gh repo view --json visibility,description,repositoryTopics,homepageUrl; gh api repos/Swampyemerson/interceptor-sim/pages -> 404; grep external refs in docs/dashboard.html -> 0; docs/publish_runbook.md:31-33; docs/license_notice_weights.md:37

`gh repo view` reports visibility PRIVATE, description "", repositoryTopics null, homepageUrl "". `gh api repos/.../pages` returns 404 (Pages not enabled). The project's single best visual asset — `docs/dashboard.html`, 344 KB and fully self-contained (grep for external `src="http` / `href="http` returns 0) — is therefore unreachable to any recruiter: GitHub serves raw HTML as source, and the alternative hosted view is a `claude.ai/code/artifact/...` URL that requires a login. Enabling Pages on `docs/` would turn the dashboard into a live shareable URL, but Pages on a private repo needs a paid plan, so it is coupled to the public flip. Also unresolved on the same path: publish_runbook:31-33 states MIT is "the recommendation, not a settled decision — builder confirms MIT is what he wants his name on before the repo goes public", and docs/license_notice_weights.md:37 carries an unresolved "BUILDER DECISION — pick before going public with any weights file" (a no-op today, since `git ls-files` confirms zero weights tracked).

### [open] docs/progress.md is 5 days stale — the milestone roll-up stops at 2026-07-20
- **Priority:** P2 — real drift on a linked surface, but lower blast radius than the README since it is a history document by nature
- **Evidence:** git log -3 docs/progress.md -> bf18db4 2026-07-20; grep '2026-07-2' docs/progress.md -> only line 28 (07-20); git log --oneline -15 shows 07-21..07-25 commits; README.md:28-29 links it

docs/progress.md's last commit is `bf18db4` (2026-07-20) and its final row ends at the Tier-1 hardware order. It therefore contains no row for the 2026-07-21..07-25 arc: the in-view probes settling the binding wall, ADR-0078 (mono camera decided, color 3x worse on desert clutter), ADR-0079/0080/0081/0082/0083, the aim-error sweep refutation, the review-2 silent-failure audit (73 verified gaps, three fix streams committed), and the Wave-2 field tools. README:28-29 links docs/progress.md as the "milestone roll-up", so it is a directly recruiter-visible surface that stops one arc short of the truth. It is a subordinate view of the contract and the drift check does not police it.

### [improvement-idea] README prominently links docs/next.md — an 81 KB working document full of superseded blocks — as a reader entry point
- **Priority:** P2 — presentation only; no claim is wrong, but it dilutes an otherwise disciplined repo
- **Evidence:** wc -c docs/next.md = 81074; README.md:28-29; docs/next.md:368 '🗄 SUPERSEDED (2026-07-12 session — history)'

README.md:29 sends the reader to docs/next.md as "working front". docs/next.md is 81,074 bytes and its structure is a live scratchpad: a CURRENT block, a BUILD QUEUE, a large 'SUPERSEDED (2026-07-12 session — history)' region, inline supersession brackets, and correction banners. It is exactly right as an agent-facing operating file and exactly wrong as a recruiter entry point — a reader who follows the link lands in retracted claims annotated with correction banners and has no way to tell the live text from the archived text at a glance. Options: drop the link, gate it behind a 'working notes (internal)' label, or add a 40-line 'start here' index at the top of docs/next.md. Not a correctness defect — a navigation one.

### [improvement-idea] Add a CI badge and a one-command reproducibility path to the README once CI is green
- **Priority:** P2 — high signal-per-byte, but strictly downstream of the P0 CI fix
- **Evidence:** README.md has no badge markup (grep `![` -> no matches); README.md:258-263 (tests section placement); CI run 30152580166 annotation "Node.js 20 is deprecated … actions/checkout@v4"

The README has no badges at all. Once the fixture problem is fixed, a green Actions badge is the cheapest possible proof of the project's central pitch ("the claim discipline is the point") — it converts an assertion about rigour into a live signal a recruiter can verify in one glance. Pair it with an explicit 'clone -> `scripts/run_tests.sh` -> exit 0' line near the top rather than buried at :258-263. Strictly sequenced AFTER the CI fix: a badge added today would advertise 72 consecutive failures. Same lane: the `.github/workflows/ci.yml` annotation shows actions/checkout@v4 is being force-migrated off deprecated Node 20 — a trivial bump to v5 removes a warning banner from every run.

### [stale/stranded] STALE: publish_runbook still flags .claude/settings.local.json as tracked — already fixed 2026-07-21
- **Priority:** P2 — no work remains except deleting the stale paragraph; listed to prevent a duplicate fix
- **Evidence:** git check-ignore -v .claude/settings.local.json -> .gitignore:98; git ls-files .claude/ (13 files, settings.local.json absent); gh run list shows the 2026-07-21T03:35:19Z untrack commit; docs/publish_runbook.md:94-98 still asserts it is tracked

docs/publish_runbook.md:94-98 flags `.claude/settings.local.json` as tracked, calls it "a machine-local permissions file [that] will read as noise in a public repo", and lists it as a candidate for a future housekeeping pass. That work was done: commit `Untrack .claude/settings.local.json (machine-local, pre-publ…)` landed 2026-07-21, `git check-ignore -v` resolves it to `.gitignore:98`, and `git ls-files .claude/` returns only agents/commands/settings.json/skills. Flagging this so the item is not re-done; the residual action is a one-line runbook correction, folded into the runbook-staleness item above.

**Reader notes:** SCOPE COVERED: README.md, docs/publish_runbook.md, T25 video state (docs/next.md build queue + docs/t25_storyboard.md + scripts/video/ + ffprobe on the actual artifacts), ADR-0066 claims-scrub constraint (docs/decisions.md:1492-1516 + audit tracker rows), GitHub state (live gh API), CI state (live gh API + job logs), LICENSE, resume line. Contract digested via targeted json loads; drift check run (`scripts/render_dashboard.py --check` exit 0, 1 open contradiction: wedge-sizing-vs-accel-cap, which is a pointing/build item outside this slice).

METHOD NOTES:
- No sim was run; no files were modified. Everything above is read-only observation plus two zero-cost verifications: one local pytest invocation on tests/test_ground_station.py (1.5 s, sim-free) to establish the local-green/CI-red asymmetry, and ffprobe on existing demo_out artifacts.
- Two findings are measurement-layer, i.e. the class CLAUDE.md's silent-failure rule targets: (1) the "first CI GREEN" claim in the contract headline is refuted by 72/72 red runs, and (2) docs/next.md:770's description of the T25 cut's shot list is contradicted by the conf file and by exact duration arithmetic (4+4+4+1.267+5+6.133+6 = 30.4 s = the file's measured duration). Both were asserted in prose and never observed end-to-end.
- Ignored as instructed: everything in the graveyard (foveated crop, v3/rebal retrains, fixed-tilt adoption, RTK metrology, sim pivot, narrow lens, onboard acoustics, bank-as-accel cue [MEASURED DEAD 2026-07-21], range-plausibility gates, APN/Kalman-CA-PIP), plus the SUPERSEDED Stage-2 tilted recovery re-test in the docs/next.md build queue.
- Deliberately NOT raised as portfolio work: the retired ground-stereo spine and the cue-era ADRs. They are correctly framed as history in README:168-172 and that framing is sound; the only live risk from that era is the demo video's uncaptioned use of it (item 6).
- Sequencing suggestion for the parent: the CI fix is the only P0 that is purely mechanical and unblocks the badge item; the README sweep and the runbook scrub-list refresh should be done in ONE turn together (they share the same source-of-truth deltas), which also satisfies the same-turn drift-sweep rule. The ram-radius ratification and the public flip are builder-gated and should be surfaced, not worked around.


## Completeness critic (what the map missed / got wrong)

### [high] The map's CI fix is incomplete: a SECOND independent cause keeps CI red, and it sits in the stage added yesterday to close 'green != ran'

Readers 5 and 6 both root-cause CI to tests/test_ground_station.py's gitignored capture fixture and frame the fix as ~1 commit. I reproduced CI properly (git clone of the repo into the scratchpad -> ~/interceptor-sim/.venv/bin/python -m pytest tests/ flight/tests/ -q) and confirm stage 1 has exactly ONE cause (1 failed + 10 errors, all in tests/test_ground_station.py -> FileNotFoundError on logs/rig_captures/full_sweep_20260709T015530Z/capture_meta.json). But I then ran CI stages 3 and 4 on that same clean clone, which no reader did: stage 3 (render_dashboard.py --check) passes, and stage 4 FAILS. `bash scripts/field/selftest.sh` in a clean checkout exits 1 with '6 failure(s)', including 'no fixture PNG under scripts/seeker/data -- cannot test the short-session branch' (scripts/field/selftest.sh:140 `FIXTURE=$(find $REPO/scripts/seeker/data -name '*.png' -print -quit)`). `git ls-files scripts/seeker/data` returns exactly ONE file (drone_sim.yaml) — every image dir under it is untracked. That FAIL is venv-independent, so it will persist in CI after the test_ground_station fix, in the very stage (4c) added 2026-07-25 to close the review-2 'only 1 of 29 self-tests was reachable from CI' finding. Net: CI needs a tracked ~KB fixture PNG (or a skip-guard) as well as the capture-dir guard, plus `if: always()` on stages 3/4 so a stage-1 failure stops masking them. Log: /tmp/claude-1000/-home-emerson-interceptor-sim/f68f5895-f3ec-4d04-aee6-345e425c9c9f/scratchpad/selftest_clone.log

### [high] Uncovered category: off-machine backup — the repo's own claim that the GitHub remote backs up 'the whole evidence base' is materially false, and the deployed weights have no recorded checksum

No reader covered backup/disaster discipline beyond demo_out. Measured on disk: scripts/seeker/data 17 GB (the 15,391-image real-media corpus + all sim datasets), scripts/seeker/weights 829 MB (incl. the DEPLOYED flight/deploy/seeker_loop.py default weights/nn_tier/n-mono.onnx, 10.6 MB, mtime 2026-07-21), demo_out 867 MB, logs 379 MB — ALL gitignored (`git ls-files '*.onnx' '*.pt'` = 0). Meanwhile docs/next.md:843 says the private repo gives 'instant off-machine backup of the whole evidence base' and docs/publish_runbook.md:5 repeats the framing; git actually holds none of it. There is no backup/rsync/archive script anywhere in scripts/ and no cloud copy referenced in any doc. Compounding it: no checksum/provenance record exists for the deployed model (grep sha256/md5 in docs/project_state.json = none; the only hash in the repo is an md5 for the retired ground_v1 in docs/decisions.md:1274), so a corrupted or swapped n-mono.onnx is undetectable, and the training data that could regenerate it is on the same single disk. Disk itself is healthy (47G used of 1007G). Cheapest fixes: record sha256 for the two deployed weight files in the contract, and copy weights + docs/nn_tier eval CSVs (~1 GB) off-machine.

### [high] In-flight work no reader covered: two commits landed DURING the review window adding an untested PreToolUse hook that can silently DENY every subagent/workflow spawn

HEAD moved from 0a41fd9/eb748e1 (the map's baseline) to c289086 then fad4c11 while the readers worked; reader 5 saw the uncommitted files and correctly called it in-flight, but no reader audited the landed artifact. scripts/hooks/block_opus48.py is now wired in .claude/settings.json as a PreToolUse hook on `Agent|Workflow`. Risks nobody has checked: (1) ZERO test coverage — no test file references block_opus48, it is not in scripts/run_tests.sh or CI, and it is not mentioned in docs/project_state.json, so it is an orchestration instrument outside every gate the project just built; (2) the Workflow path applies BLOCKED_IN_SCRIPT = /model\s*:\s*['\"](opus|claude-opus-4[^'\"]*)['\"]/i to the ENTIRE script text including comments and prompt strings, so a workflow whose prompt merely quotes `model: 'opus'` (e.g. documenting this very rule) is denied — a false-deny that surfaces as a failed spawn, i.e. parallel work stranding silently, which is exactly the failure mode the standing order treats as worst-case; (3) it is a change to the permission/config surface, so it deserves an explicit builder-visible record (the commit cites the builder, but the contract does not carry it). Minimum fix: a tests/test_block_opus48.py with the deny/allow payload cases (opus, claude-opus-4-8, claude-opus-5, opus5-worker, comment-only mention), and one line in project_state.json.

### [medium] Reader 1's crossing_sign finding is WRONG (fixed 2026-07-24), and the thing that misled it is a stale docstring inside flight/guidance.py that now asserts the opposite of the code

Reader 1 reports 'crossing_sign float-dust latent bug ... the bug stands in the flown sim path' as a live P2 open item; reader 2 reports it CLOSED. Reader 2 is right: commit 1911ce9 (2026-07-24) routed m4's inline sign key through flight.guidance.crossing_sign — verified live at scripts/m4_intercept.py:252 (import), :2409, :2464, :2498 (all three call sites). The disagreement was manufactured by a code comment: flight/guidance.py's crossing_sign docstring still reads 'MIRRORS the sign key that scripts/m4_intercept.py computes inline ... that flown code path is deliberately left untouched (byte-identity)'. That sentence is now false and it is inside the function whose adoption refuted it — the same-turn drift-sweep rule applied to a docstring rather than a doc. Fix: correct the docstring (one edit) and strike the item from the map so it is not re-worked.

### [medium] Contradictory reader statuses resolved against the repo: CI counts, and audit_per_tick check (c) is TWO distinct defects, not one

(a) CI: reader 5 says 'failed on 40/40', reader 6 says '72/72, 71 failure + 1 blank'. Live: `gh run list --limit 100` returns 73 runs, Counter({'failure': 73}) — every run since the first push (2026-07-21T03:35:19Z) through the newest (2026-07-25T18:15:41Z, i.e. commit fad4c11) has FAILED. Reader 5's 40 was a --limit artifact; reader 6's blank conclusion has since resolved to failure. So docs/project_state.json headline's 'first CI GREEN' has never been true for any run, not merely stale. (b) audit_per_tick: reader 2 says check (c) WARN-skips when its columns are missing while the summary still prints '(a)/(b)/(c) passed'; reader 3 says (c) drops zero-command ticks, falls under the n<10 WARN branch and stops gating. BOTH are true and INDEPENDENT — scripts/audit_per_tick.py:206-208 (missing columns -> WARN-skip) and :436/:450 (rows with |cmd|<1e-6 excluded, then `if n < 10: WARN`), with the same unconditional summary at :849-851. A fix that only adds the third flight status (reader 2's ask) still leaves the zero-command exclusion silently non-gating, and vice versa. Treat as one item with two required code changes plus reader 3's proposed gating check (e).

### [medium] Uncovered category: dependency rot — `pip install -r requirements.txt` does not reproduce the test suite, and CI documents the drift instead of fixing it

requirements.txt is unchanged since 2026-07-05 and lists only mavsdk/numpy/opencv/pytest/pupil-apriltags/matplotlib. The tree actually imports onnxruntime (8 modules), ultralytics (17), pyulog and pymavlink; .github/workflows/ci.yml compensates with an inline `pip install pyulog==1.2.3 pymavlink==2.4.49` under a comment reading 'Local-.venv extras not in requirements.txt'. Consequence for the portfolio path the map cares about: a recruiter (or a fresh machine, or a rebuild after the disk loss in the backup finding) who clones and follows the README cannot run the gates, and the pinned set no longer describes the environment the numbers were produced in. By contrast scripts/pi_setup/requirements-pi.txt is genuinely good and current — except its header comment still says bench scoring matches 'drone_finetuned_quad_v2 @640', which adds a FIFTH surface to reader 3's deployed-weights-drift item (alongside seeker_loop default, check_deploy_bench.sh:86, flight/deploy/README.md, and the contract's detector `active`).

### [medium] Instrument gap the map missed: real_flight.py's onboard decision log has no consumer, no parser and no producer→consumer contract test

flight/deploy/real_flight.py writes a 36-column per-tick CSV via --log-csv (_CSV_FIELDS at :1355-1366: state, streak, trig_*, det_*, sp_source, lambda_dot, r_hat, terminal_coast, own_state_ok, stale, track_broken, health, safe_reason; header+row built from one tuple at :1367, so intra-file drift is already prevented). But `git grep -l log_csv` over scripts/ tests/ flight/tests/ returns only scripts/check_real_flight_sitl.sh, which merely passes the path and never parses it, and tests/contract_helpers.py pins ONLY scripts/m4_intercept.py's CSV_HEADER. So the single record of WHY the interceptor did what it did on the first real flights — the state machine transitions and failsafe reasons — has no scorer, no schema pin, and nothing that asserts it is even non-empty or well-formed after a flight. field_score.py consumes FC .BIN/ULog, not this. This is the same class as the seams review 2 pinned; it just wasn't on anyone's list because the file is new. Cheapest fix: a per-tick log reader used by the SITL gate to assert phase coverage, plus a contract test whose fixture comes from real_flight's own writer.

### [medium] Uncovered category: memory/docs drift — the always-injected memory index carries expired directives and has NO entry for the tripod day, the $740 gate, or the three pending builder decisions

Nobody audited ~/.claude/projects/-home-emerson-interceptor-sim/memory/, which is injected into every session and is (with repo files) the only thing that survives /clear. Stale entries: fable-limit-opus-override.md is an explicitly SESSION-SCOPED 2026-07-10 instruction ('use opus everywhere fable was') still listed in MEMORY.md as live guidance, and it now interacts badly with the brand-new hook that denies the bare 'opus' alias; worktree-jobs-venv-and-no-remote.md's NAME and MEMORY.md index line still say 'no remote' (the body was corrected — the index line a session actually reads was not); post-m5-roadmap.md still ends 'next: #35 up-tilt -> T25 video', a pre-pivot roadmap. More consequential is the omission: grep across all 31 memory files for 'ram radius|0.36|tripod|money gate|740' hits ONE file (real-build-pivot.md, and only for 'tripod' in passing). The unratified 0.36 m ram radius, the ~$740 gate structure and builder decisions #16/#17/#22 exist only in docs/next.md and the contract's updated_by blob — in a project whose #1 recorded frustration is context loss, the highest-stakes open decisions have no memory representation.

### [medium] The ram-radius ratification is not a scorer default — 0.5 m is baked into the launch mechanism's ±3° aim budget and six other docs

Every reader that raised #17 scopes the blast radius to field_score.py's DEFAULT_LETHAL_RADIUS_M=0.5 and the contract note. It is wider, and one of the surfaces is a DESIGN input, not a scoring constant: docs/launch_mechanism_plan.md:17 states the whole launch mechanism must deliver '±3° for a 0.5 m ram' — at 0.36 m that budget tightens roughly proportionally (~±2.2°), which bears directly on whether the static bearing latch (bench items L5/L6) is sufficient. Other quoting surfaces: docs/kill_mechanism.md:125 and :229 ('tolerable miss (0.5 m ram -> 1.5-2 m)', 'inside even the WORST-credible ram radius'), docs/launch_mechanism_options.md:47 and :139 ('Re-score existing batches against the ram radius'), docs/flight_plan_candidates.md:163 ('the 0.3-0.5 m ram bar'), docs/decisions.md:672 (the 0.5/0.30/0.15 BEST/EXPECTED/WORST tiering that the 7-inch premise came from) and the Pk-vs-radius tables at :946 and :1120, plus demo_out/README.md:43's caption. Whoever executes the ratification should treat it as a sweep with an aim-budget re-derivation, not a constant change.

### [medium] Two of the three selftest.sh packs are outside every gate — reader 4 caught configs/target_kakute, nobody caught scripts/pi_setup

`git ls-files | grep selftest.sh` returns three: configs/target_kakute/selftest.sh, scripts/field/selftest.sh, scripts/pi_setup/selftest.sh. scripts/run_tests.sh:106 and .github/workflows/ci.yml:124 wire ONLY scripts/field. Reader 4 correctly flagged that the ungated target-config pack is how the stale tripod mission geometry survived; the same hole covers the Pi provisioning pack, which validates the provisioning path for the machine that will run the field day's capture. Generalize the fix: wire both remaining packs into run_tests.sh stage 4 (they are sim-free and fast), and — since the field pack itself fails on a clean checkout (finding 1) — make stage 4 enumeration-driven rather than three hardcoded calls, which also closes reader 5's 'skip set is prose, not data' item.

### [low] The tripod producer→consumer contract test pins the SCHEMA but never drives the producer's writer

Refines the map's category (c). tests/test_tripod_pipeline_join.py:50 does `from pi_capture import INDEX_HEADER, TAGS_HEADER` — genuinely producer-sourced, which is better than the hand-typed fixtures the project got burned by. But the row VALUES are hand-written (`wr.writerow([i, rel, f"{i/FRAME_HZ:.6f}", ...])` at :113-133), so pi_capture's actual writer path is never exercised: a change in what the writer EMITS per column (units, formatting, ordering of the values it appends) passes both this test and pi_capture's own tests, which is precisely the pair-untested shape the rule targets. Low priority relative to the open blockers, but worth one refactor: have the test call pi_capture's frame-record/append function against a stub camera rather than composing rows itself.

### [low] Scope-clearing negatives (verified, so the parent does not fund this work)

(1) DOC→CLI FLAG DRIFT: I mechanically extracted every '--flag' appearing in command context in docs/tripod_test_protocol.md, field_bringup.md, real_build_coded_dash.md, launch_mechanism_plan.md, placard_mount.md and placard_sizing.md and checked each against the referenced script's source (script: scratchpad/flagcheck.py). Only two hits, both false positives from a markdown table in field_bringup.md:259-260. So apart from the already-known --quad-decimate, the field protocol's command lines are executable as written — no hidden second class of protocol-vs-tool drift. (2) FROZEN_VWORLD RE-FLY IS REPRODUCIBLE: the arm definitions are tracked at scripts/experiments/flight_plans/run_arm.sh:88-104 (G20/G20dash/AE5/AE5dash/AE15/AE15dash with exact flags) and the stdout logs record the batch plan and master seed (logs/mc_fp_armG20_line9_s123_stdout.log lines 3-6), so the ~40-flight camera-arm re-fly needs no archaeology. (3) The map's two P0 sim defects are still live at HEAD: scripts/m4_intercept.py:2529/:3703-3709 unchanged, last commit f56f9dc (pre-review); flight/ contains no AprilTag decode (only comment mentions), confirming reader 1's item 3. (4) Local repo hygiene is genuinely clean: working tree clean, `git log origin/main..main` empty, one worktree, no stray branches, no stash, PX4 build intact (2.3 GB), disk 5% used.

### Top recommendations

- Fix CI as a TWO-cause job, not one: skip-guard tests/test_ground_station.py's gitignored capture dir AND track a small fixture PNG under scripts/seeker/data (or guard scripts/field/selftest.sh:140), then add `if: always()` to CI stages 3/4 so a stage-1 failure can never again hide the drift check and the self-test gate. Verified by clean-clone reproduction; without the second half CI stays red after the prescribed fix. Then correct docs/project_state.json's 'first CI GREEN' headline — 73/73 runs have failed since the first push.
- Close the newest hole first because it is the one that can strand parallel work: put scripts/hooks/block_opus48.py under test (deny/allow payload cases incl. a comment-only 'model: opus' mention in a Workflow script, which today false-denies) and record the hook in the contract. It landed mid-review and is currently outside every gate the project just built.
- Treat off-machine backup as a first-class item: the claim in docs/next.md:843 / publish_runbook.md:5 that the private remote backs up 'the whole evidence base' is false — 18.5 GB of weights, the 15,391-image corpus, logs and demo_out are gitignored on one disk. Record sha256 for the two deployed weight files in the contract and copy weights + eval CSVs off-machine; correct the two claims the same turn.
- Strike reader 1's crossing_sign item from the map (fixed in 1911ce9, verified at m4_intercept.py:2409/2464/2498) and fix the stale docstring in flight/guidance.py that caused the false finding ('that flown code path is deliberately left untouched' is now the opposite of the code). Merge readers 2 and 3's audit_per_tick items into one task with THREE required changes — missing-column status, zero-command exclusion counting, and the new gating check (e) — since fixing either alone leaves the other silently non-gating.
- When the builder ratifies the 0.36 m ram radius, execute it as a sweep with a re-derivation, not a constant change: docs/launch_mechanism_plan.md:17's '±3° for a 0.5 m ram' aim budget is a design input to the launch mechanism and bench items L5/L6, and six other docs plus the demo caption quote the old radius.
- Add the missing durable-memory entries the same turn as any contract edit: the tripod day / $740 gate structure and builder decisions #16/#17/#22 appear in no memory file, while an expired session-scoped Opus override and a 'no remote' memory title are still injected every session.
- Give real_flight.py's onboard --log-csv a consumer and a contract test (fixture from its own writer) before the first real flight — today the only record of the state machine's decisions and failsafe reasons is written by the vehicle and read by nothing.

## Prioritization critic (ranked execution order)

### [high] RANK 1 — Plumb --quad-decimate through the field decode chain (desk, ~1-2 h). The single field day is otherwise flown in the config ADR-0082 predicts FAILS.

VERIFIED INDEPENDENTLY: grep of scripts/seeker/*.py shows quad_decimate appears ONLY in prose/comments. Every decode site is a bare Detector(families=...): ~/interceptor-sim/scripts/seeker/pi_capture.py:167, ~/interceptor-sim/scripts/seeker/tripod_score.py:456, ~/interceptor-sim/scripts/seeker/autolabel_from_apriltag.py:106, plus eval_classical.py:68, synth_tag_frames.py:116, validate_autolabel_sim.py:262. All therefore run the pupil-apriltags default 2.0. ADR-0082 promoted qd=1.0 from 'reclaim lever' to the PLANNED capture setting because it is the only config clearing t_go>=0.5 s at 20 and 14 fps; at the deployed qd=2.0 the adopted 0.35 m placard FAILS at both. Two compounding harms: (1) the day is captured in the predicted-FAIL config; (2) the value is not stamped into meta.json, so after the day the frames cannot be re-scored to recover the lever — it is lost with the session. docs/tripod_test_protocol.md:236-250 already carries a NOT-EXECUTABLE banner instructing to add it before the field day or strike the bullet. Highest value-per-effort item in the entire map: pure desk work, no parts dependency, and irrecoverable if skipped.

### [high] RANK 2 — Sweep gen_tripod_mission.py to the corrected station geometry and give its self-test protocol teeth (desk, ~1 h). It currently emits the exact geometry that manufactures a false NO-GO.

VERIFIED: ~/interceptor-sim/configs/target_kakute/gen_tripod_mission.py:367-373 defaults are --leg-length 30.0, --near-range 8.0, --standoff 17.0, with help text still citing the SUPERSEDED 'protocol 4.2 ~15-20'. The preset table at :225-233 and :311-323 carries the same. docs/tripod_test_protocol.md:162 corrected the stations on 2026-07-24 precisely because the old 8-30 m ladder sits entirely outside the predicted 7.10 m R_decode90 and returns an all-zero curve (a) = NO-GO on the $740 order; :177 names the near stations as 'the money stations'; :219 sets a ~5-6 m crossing standoff. With defaults, the 4 m and 6 m money stations are unreachable. Worse: `grep -rn gen_tripod_mission docs/ docs/next.md` = 0 hits and the string is absent from project_state.json, so a builder following the protocol may not know the tool exists — and its --self-test passes because it checks mission structure and round-trip, not protocol conformance. Fix bundle: sweep defaults/presets/help, add conformance asserts (near-range <=4 m, tag-envelope crossing standoff in 5-6 m, contiguous 2 m station bins the scorer's r90 walk requires), wire ~/interceptor-sim/configs/target_kakute/selftest.sh into scripts/run_tests.sh (currently only scripts/field/selftest.sh is wired), and reference the tool from the protocol. Same producer->consumer shape as the review-2 contract tests: the protocol is the producer, the mission file the consumer.

### [high] RANK 3 — Re-brief the capture matrix for the beam-facing placard and add incidence to curve (a) scoring (desk, ~2-3 h). Today the day can be flown by the book and still return a verdict the money gate cannot honestly consume.

Two halves of one defect. (a) ASPECT: docs/placard_mount.md DECISION 1 committed a single vertical panel EDGE-ON to flight (11.8 N nose-on drag at 9 m/s vs 0.28 N edge-on), which makes CROSSING the tag's primary aspect and APPROACH its ~90-degree-incidence NULL. docs/tripod_test_protocol.md:281-300 still schedules ~16 of ~28 passes as approach and labels them 'primary decode-envelope + recall data'. A large fraction of the day's passes will produce structural zeros that read as a tag failure. placard_mount.md §11 items 5-6 asked for the aspect re-brief plus a dedicated 90-degree-index nose-on block at <=4 m/s (also the ONLY source of approach-aspect frames for autolabel_from_apriltag.py); neither merged. (b) INCIDENCE SCORING: protocol §4.2b mandates recording the 15-degree-indexable mount angle per pass and scoring decode vs (range x incidence), but `grep -c incidence` = 0 in both scripts/seeker/tripod_score.py and scripts/seeker/range_truth_join.py, and §8.1 is still the range-only form. The cos-theta decode law is validated only to 32.6 deg in sim and is a listed HYPOTHESIS above that. A GO measured at 0 deg incidence and applied to a 30 deg engagement is not a GO. Zero field-time cost to collect; unimplemented it converts silently into an over-optimistic gate verdict.

### [high] RANK 4 — Produce the three print/buy artifacts today: placard PDF, checkerboard, mount STLs (desk + one $8 purchase). All three are lead-time items and one blocks the FIRST bench gate.

The placard is sized (0.35 m tag black square on a 450x450 mm sheet, ADR-0082/P0.2) and its mount fully engineered, but nothing printable exists: the only tag asset is models/apriltag_target/tag36h11_00000.png, a 512x512 px Gazebo texture with no scaled PDF; there is NO checkerboard file anywhere; the PETG index disc + sacrificial frangible shoe exist only as an ASCII sketch in docs/placard_mount.md §6 — no STL/SCAD/STEP in the repo (a find for those extensions returns only Gazebo prop meshes). 3D-printer access is itself an unconfirmed ASSUMPTION in the BOM's own register (docs/hardware_order_list.md:399). Critical path: camera calibration (build_tab skr-06, >=15 views, RMS <=1.0 px, plus re-measuring the delivered adjustable lens FoV) is the FIRST bench gate and every range and bearing number in the campaign is wrong until it runs — and it is blocked on a checkerboard that does not exist. bom_tiers already prices a rigid pre-printed board at ~$8. Buy that board TODAY; it has shipping lead time and gates the bench day.

### [high] RANK 5 — Place the remaining blocking-parts order today (~20 min). The Kakute microSD alone makes every scored pass unscoreable.

Converged across bom_tiers, build_tab and docs/launch_mechanism_plan.md §7, still must-add as of the last order update (2026-07-20b), with no record anywhere that any were ordered after or that the 2026-07-20 order has physically ARRIVED: 32-64 GB microSD for the Kakute (no card = no DataFlash .BIN = no target track = range_truth_join has nothing to join = EVERY curve returns UNCERTAIN — a single point of total data loss for the whole day); tripod + ball head + inclinometer (hard blocker for the tripod day itself AND for launch-mech bench L5); >=27 W USB-C PD power bank (the wall PSU is useless at the field); Pi 5 active cooler (the fps bench is a THERMAL soak); fireproof LiPo bag + ANSI Z87 glasses + fire blanket; lost-model buzzer; cones + tape measure. Newly surfaced and in NO BOM tier at all: a handheld anemometer (~$20) — without it protocol §8's <=3 m/s crosswind / <=5 m/s gust validity limits are unenforceable and crosswind becomes an uncontrolled variable in curve (a). Also: 2 carbon spars + PETG filament, M3 nylon hardware + rubber bands, 3 spare frangible shoes + 1 spare panel, and verify the ordered hex kit has an 8 mm prop socket. Pure shipping-latency risk; the cart is 20 minutes.

### [high] RANK 6 — Hoist range_truth_join's survey/datum/bias integrity checks onto the shared path (~1-2 h). Wave-2 routed the field day down the ONE path that has no checks.

VERIFIED unfixed (file untouched since 129ed45, pre-review). In ~/interceptor-sim/scripts/seeker/range_truth_join.py every residual/bias/identifiability test lives inside `if do_auto_sync:` (:421-533); the shared path (:583-600) checks coverage only. IMPLAUSIBLE_UP_M is still 150.0 at :172 and still only sets warn_alt at :565, and --max-bias-m's own help text (:1027) describes it as the '--auto-sync constant-bias ceiling'. Reproduced through the module's own fixture: a 1.1 km survey error, an AMSL-vs-AGL altitude-datum blunder (med_up ~106 m slips under the 150 m warn-only guard) and a 3 s clock error each exit 0 with empty warnings. Wave-2 made this SHARPER not safer: docs/tripod_test_protocol.md §7.0 makes range_truth_join a mandatory scoring step and §6.1/§4.6b tell the operator to use --clock-offset-s as the plan with --auto-sync only as a close-crossing cross-check (auto-sync refuses approach geometry by design). So the day's dominant geometry runs the unchecked path, and a survey given AGL instead of MSL propagates silently into true_range_m and into BOTH curves. This is the 'instruments are evidence' class sitting on the money gate's own truth source.

### [high] RANK 7 — One contract/docs/next.md/docs/progress.md sweep turn (~1-2 h) closes ~10 separate reader findings and puts the tripod day's readiness on the canonical surface.

VERIFIED: HEAD is fad4c11; docs/project_state.json, docs/dashboard.html and docs/next.md were all last written at 0a41fd9 — four commits back. f3a3bca (Wave-2 field tools + protocol, 11 files, +1063 lines) and eb748e1 are not folded in, so the contract still says 'Remaining: field-tools+protocol (in flight)' and contains zero mentions of range_truth_join / 05_pi_link_check / PULL_FAILED. No stage changelog anywhere carries a 2026-07-25 entry. Refuted or reversed prose live on the hosted artifact right now: narrative.big_lever body still asserts '(2) the camera defends imperfect aim' and '(3) the LAG knob... (GL, pending)' under a headline that already refutes both, and GL flew as a NULL; narrative.program still leads with Phase A at tone 'next' while the same file's levers table calls it 'NOT a do-first lever'; docs/next.md:37 still states the refuted 'real value = defending imperfect real-world aim'. Build-facing staleness that would cause a WRONG PHYSICAL ACTION: the placard parts card says 'SIZE TBD BY P0.2... has NOT run yet' and gnd-02 says 'hold the placard print' although tgt-08 in the same file quotes 0.35 m (a builder defers the print or prints the non-clearing ~0.3 m fallback); the must-add list says the soldering iron/zip ties/multimeter are STILL MISSING while build_tab records them OWNED (double-buy); detector stage `active` still names drone_finetuned_quad_v2.onnx though flight/deploy/seeker_loop.py defaults to n-mono and states quad_v2 'must never be the default on hardware' (and scripts/check_deploy_bench.sh:86 still benches quad_v2 — the bench gate validates the wrong model); kill stage says field_score.py is 'planned, not built' while its own changelog and scripts/field_score.py:282 prove otherwise; real_data says the IoU validation 'has NOT run' while its changelog reports IoU 0.965; FAA reads 'BUILDER UNBLOCKED' in the stage note and 'STILL OPEN' in build_tab and bom_tiers. docs/progress.md is frozen at 2026-07-20 and missing the entire 07-21..07-25 arc. Also fold in: docs/real_build_coded_dash.md:129 still says 'no fixed tilt adopted, adaptive tilt #46 is the open lever' — the opposite of the builder's FINAL 2026-07-17 ruling, on a doc docs/goals.md names as current truth. Highest findings-closed-per-hour in the map, and it is the project's own #1 housekeeping rule.

### [high] RANK 8 — Fix CI (~30 min). 72/72 runs red, and the drift check + self-test coverage added 2026-07-25 have NEVER executed remotely.

VERIFIED live: gh run list shows failure on every run back to the first push 2026-07-21T03:35:19Z. Single root cause: tests/test_ground_station.py:87 sets CAPTURE_DIR = DEFAULT_CAPTURE_DIR -> logs/rig_captures/full_sweep_20260709T015530Z/, which is gitignored (`git ls-files logs/rig_captures/ | wc -l` = 0) with no existence guard, so a clean runner gets FileNotFoundError. It passes locally only because this machine holds the capture. Second, independent defect: .github/workflows/ci.yml has no `if: always()` on the later steps, so stage 3 (project-state drift check) and stage 4 (uncovered --self-test entry points) are SKIPPED, not run — meaning the entire review-2 'green != ran' prevention tranche committed 2026-07-25 is inert on the remote. That is the project's own silent-failure pattern inverted: prevention infrastructure committed, documented as landed, never once executed. Fix both: module-level pytest.skip(allow_module_level=True) on absence of the capture dir (matching the stage-2 gitignored-data rationale the ci.yml header already articulates), plus if: always() on stages 3/4. Prerequisite for the public flip and for any CI badge — a badge added today would advertise 72 consecutive failures. Also bump actions/checkout@v4 -> v5 (deprecated Node 20 warning) while in the file.

### [high] RANK 9 — BUILDER DECISION: ratify the 0.36 m ram radius now, but split it from the camera-terminal inference (its evidence is downstream of an unfixed sim defect).

The contract's kill stage calls this 'the single most consequential number in the project' and says it must be settled BEFORE the Tier-2 order (~$510 this tier, ~$740 all-in), which the tripod day's verdict authorizes. The 0.5 m radius quoted project-wide was derived for a 7-INCH airframe; both ordered aircraft are 5-inch, giving ~0.36 m tip-to-tip. PM framing for the builder: the GEOMETRY half is pure arithmetic and not in doubt — ratify it, and sweep scripts/field_score.py:120 DEFAULT_LETHAL_RADIUS_M = 0.5 plus every Pk claim on a visible surface (all currently quoted against a radius the hardware cannot deliver). The INFERENCE half — 'against 0.36 m the camera terminal has to earn ~a metre and at correct aim it does not, so it is decoration' — rests on camera-arm batches measured through the frozen_vworld defect (rank 11) and should be held, not ratified. Two other builder decisions are carried in docs/next.md and updated_by with NO first-class representation anywhere (no decision object, no ADR; docs/decisions.md still ends at ADR-0083): #16 'vertical channel + altitude datum' exists only as a five-word label with the question itself never written down, and #22 post-GO OFFBOARD loss, whose code at flight/deploy/real_flight.py:1724-1731 points at an 'open ADR-0081 addendum' that does not exist (failsafes 1-6 and 8 are defined; 7 is conspicuously unused). Write all three up as decision records with question/options/recommendation before they evaporate — this is the project's named #1 frustration.

### [high] RANK 10 — Day-parts-arrive #1: the skr-07 Pi 5 sustained-decode fps bench. It is the money gate's divisor and it cannot be measured after the fact.

scripts/seeker/tripod_score.py deliberately ships no stream_fps default (gate_verdict, 'STREAM FPS IS REQUIRED, NEVER INVENTED (2026-07-24)') and returns UNCERTAIN without it — correct fail-closed behaviour after the invented-30-fps defect that once flipped this gate. The burn model scales as 1/fps and the verdict flips across ~24 fps; the deployed loop runs 20 Hz and the only measured AprilTag cadence in the repo is ~14 Hz. It must be a SUSTAINED thermal-soak decode cadence on the real Pi 5 at the FLYING quad_decimate (hence sequenced strictly after rank 1), and must not be confused with the recorder's capture rate (pi_capture pays a PNG write the flight loop does not). Sequencing hazard: measuring after the field day cannot rescue a session captured at the wrong rate. Second half of the same risk, ~3 lines: resolve_stream_fps (tripod_score.py:1219-1266) is a BLACKLIST — it rejects only sources whose stream_fps_source contains 'replay' (FPS_SOURCE_REJECT_MARK at :144-147) and never checks meta['source'], which pi_capture.py:552 already writes. So a --local/v4l2 laptop-webcam desk rehearsal with decode on still supplies the $740 gate's divisor. The review's recommendation was a whitelist (accept only meta['tag_decode'] True AND meta['source']=='picamera2'); the builder question 'may a desk rehearsal ever feed the gate fps?' (recommended NO) is unanswered and unlogged.

### [high] RANK 11 — frozen_vworld: fix the CODE and the AUDITOR now (~2 h); DEFER the ~40-flight Gazebo re-fly. This is where I diverge from the readers' P0 framing.

The defect is real and I confirmed it unfixed: scripts/m4_intercept.py:2529 still initialises `vh0 = vh1 = 0.0` and :3703-3704 still latches `frozen_vworld = (vh0, vh1)`; under --coded-dash --fpv the NN's short r_hat puts the first ENGAGE tick inside TERMINAL_FREEZE_RANGE_M so the block computing vh0/vh1 never runs, and the whole ENGAGE phase re-issues cmd=(0,0,...). A reader re-measured 29/44 engaged flights >75% zero-command across G20/AE5/AE15/GL/CG arms. The file has not been touched since f56f9dc (pre-review); the flight-code stream explicitly scoped itself to flight/ and nobody picked up scripts/m4. PM CALL: (i) DO fix the latch by porting flight/deploy/seeker_loop.py:311-315 (one code path, sim==hardware) — cheap, and it removes a landmine if the sim is ever reopened; (ii) DO fix the auditor, which is the real lesson: scripts/audit_per_tick.py check (c) skips rows where |cmd_horiz| ~ 0, so on exactly these flights it drops nearly every row, falls under the n<10 WARN branch, stops gating, and :849-851 still prints 'PASS: N/N audited flights passed (a)/(b)/(c)' — the auditor is anti-correlated with the defect. Add n_zerocmd counting, a third flight status so the summary cannot claim (c) ran, and a new gating check (e) 'the terminal actually actuated' (FAIL when >50% of detected pre-CPA ENGAGE ticks are zero-command); (iii) DO downgrade the affected contract verdicts to PENDING RE-FLY and open a ledger entry the same turn — 'the camera terminal NEVER earns its handoff / REFUTED IN SIM', 'G20 1.02 vs 0.75 m', 'both terminal compensations are NULL' are all published as settled and are all measured on a braking terminal; ring-fence what survives (ADR-0080/0083, the dash-only 0.71/0.43 m floor, the hardware path); (iv) DO NOT spend a batch day on the ~40-flight re-fly right now — sim exploration is declared complete, the hardware executor is unaffected, and the re-fly buys a sim-only conclusion. Reopen ONLY if the ram-radius ruling makes 'does the camera terminal earn a metre' a live purchasing question. Same file, same owner, fold in while there: the wall-clock lever bug (sim_clock is built only under `if args.handoff:` at :4391-4395, mutually exclusive with --coded-dash, so --dash-accel-cap / --dash-loft-dive-s / --coded-dash-max-s all ran on WALL time at RTF 0.80-0.84, violating ADR-0009) and the silently-inert --terminal-bearing-bias-* / --terminal-los-lag-ms under --law pip (:3640-3677, which logs 'compensation ACTIVE' and would manufacture a fourth phantom NULL). Both are ~10-line fixes plus a dash_clock CSV column.

### [medium] RANK 12 — Write the kill-day protocol (sim-free, ~half a day). The binary kill IS the success criterion and it has 5 bullets against the tripod day's 46 KB.

docs/tripod_test_protocol.md is a decision-grade 46 KB / 12-section document: pre-session checklist, survey inputs, capture matrix, three-step scoring sequence, pass/no-go thresholds, done-definition. The first-kill day (build_plan P5: static intercept -> moving straight-line -> the speed ladder to 9 m/s) has four task bullets in the contract plus docs/launch_mechanism_plan.md §4's narrative, and its evidence chain (seeker video + phone slow-mo + both ULogs, scored by scripts/field_score.py) has NO pre-registered success/abort criteria, no safety geometry, no pass card and no clock-sync procedure equivalent to §4.6b. Since binary-kill evidence is the project's stated deliverable, an unwritten protocol is the single largest remaining process gap — and it should be written NOW while the tripod protocol's hard-won lessons are fresh (survey first, sync pass per battery segment, no vacuous verdicts, incidence recorded per pass). Two adjacent items belong in the same write-up: the missing FAILSAFE 7 for post-GO OFFBOARD loss (builder decision #22), and a mapping table from each of the 20 TODO-BUILDER constants in flight/deploy/real_flight.py (standby alt, dash speed, terminal/breakoff, all failsafe thresholds, RC channel index, geofence, camera intrinsics) to the bench step (L3/L5/L6/L9/L10) that measures it — that table converts a vague 'NOT flight-ready' warning into a runnable bring-up checklist. Note also that brn-05 and scripts/check_deploy_bench.sh drive the OLDER flight/deploy/seeker_loop.py, so the actual flight executor real_flight.py has NO hardware bench check at all.

### [medium] DEAD WEIGHT — strike these; they move no gate. Chief among them: the 'ten unverified review-2 findings', which cannot be executed because the list was never written down.

(1) THE 10 UNVERIFIED FINDINGS — docs/review2_silent_failure_findings.md:12 promises '10 unverified titles are listed at the end'; the file ends mid-LOW at :1261 and contains exactly 73 headings, all verified. Three readers independently searched docs/, logs/, the contract and ~500 MB of session transcripts; the only hit for 'unverified titles' is the doc's own broken promise. Strike the item from docs/next.md and the contract with an honest note that the list evaporated with the session. If any replacement is wanted, scope it to scripts/m4_intercept.py — the one file no Wave-1 stream owned and the home of the two biggest orphaned defects. Do NOT spend a session searching. (2) THE WEDGE-SIZING CONTRADICTION'S GAZEBO RUN — the sole open ledger entry directs 'a serialized Gazebo confirmation of the accel-capped dash' as its resolution path, but the cap was REJECTED (EAL: costs ~0.34 m at correct aim; the contract's own coded_dash note now reads 'don't cap'). Do not queue that batch. It is a 10-minute doc edit: re-state the contradiction as 'wedge angle vs prop clearance on an UNCAPPED ~35-40 deg dash' (the flush mount clears ~11 deg, hence the boom), note that the build side is already correctly hedged (print an ADJUSTABLE 10-30 deg bracket, lock after the first dash ULog), and create the dangling 'wedge-vs-level' entry the narrative references but which does not exist. (3) T25 VIDEO RENDER TIME — do not render shot 7 or re-cut until a finish-vs-park decision is made. The 30.4 s file is ~19 s static cards / ~11.4 s footage, its opener showcases the ground-stereo cue stack the pivot CUT, its 'DATALINK DENIED' beat is exactly the scope publish_runbook.md:52-56 holds most tightly, and demo_out/* is gitignored so it reaches no cloner. Its story predates two headline reversals. (4) SIM-ERA TEST HYGIENE — COAST_STALE_S value pin, solve_intercept_time coverage, stage-4 self-test registry, dead-script attic move (12 orphans), run_tests.sh logs/field/ pollution. Polish on a lane the builder has declared complete. (5) README/PROGRESS PORTFOLIO SWEEP — real drift (README is 5 days stale, states as 'open' two questions settled 2026-07-21, UNDER-claims the Arm-A camera win, carries none of the big_lever finding, embeds zero of the 29 tracked docs/images/ assets, and its resume line at README.md:17-20 welds the cue-era MC statistics onto the coded-dash architecture). But it is NOT dead weight so much as MIS-SEQUENCED: sweep it ONCE, after the field results land, not twice. The public flip, Pages, MIT confirmation, badge and topics all queue behind that same moment.

### [medium] CHEAP KEEPERS — three sub-30-minute items with genuine field-day or integrity value, worth folding into an existing turn rather than scheduling.

(1) --help SMOKE SWEEP over the python field-day tools (~10 lines). eb748e1 fixed tripod_score.py --help crashing on a bare %% in an argparse string — a tool committed in the Wave-2 tranche whose own entry point was never exercised, i.e. policy §8 slipping inside the tranche that codified it. scripts/field/selftest.sh:100-103 sweeps --help over scripts/field/*.sh ONLY; nothing covers tripod_score.py, range_truth_join.py, pi_capture.py, field_score.py, camera_session_summary.py, and no regression test pins eb748e1. On the tripod day a --help crash costs a drive home. (2) CONTRADICTION-LEDGER INTEGRITY (~15 min, patch already specified by review 2). docs/project_state_readme.md documents `verdict` and `resolution` in the contradiction schema, but scripts/render_dashboard.py CONTRA_KEYS (:71) omits both, so an entry can flip open->resolved with the resolution note DELETED and --check stays green (mutation-tested). The ledger is the project's guard against a resolved-but-not-fixed claim and is currently honest only by discipline; there is also no tests/test_render_dashboard.py at all. Two conditional asserts plus a unit test. (3) 02_apriltag_desk_check.sh REPLAY SELECTION (~10 min). scripts/field/02_apriltag_desk_check.sh:111 takes only `head -1` of the session glob; if the newest session is a replay it blanks LATEST (:114) and falls through to the SIM fixture instead of scanning further back for the newest NON-replay session. run_tests.sh creates replay sessions in exactly that glob (one invocation adds ~8 dirs to logs/field/, now at 34). So on the field day: capture real frames, run the suite, run the desk check with no args -> it grades sim frames. It is loud (fld_miss + DRY=1) so it cannot produce a false PASS, but it burns field time. Also worth 5 minutes in the same lane: scripts/field/00_detect_devices.sh:188-190 emits fld_warn for an unidentified serial port and leaves STATUS at OK, so the field-day hard gate --require passes with NO flight controller identified.

### [medium] STRUCTURAL — the build phase is not auditable: no per-step status, no delivery record, and physical progress lives in one browser's localStorage.

All 30 build_tab steps (tgt-01..08, skr-01..08, brn-01..06, gnd-01..08) carry only {id, text, why, gate} — no status field — and docs/dashboard.html:4022-4116 persists the checkboxes via localStorage.getItem('buildtab:'+id). So the one thing the builder is about to spend weeks physically doing survives no /clear, appears in no git diff, is invisible to the drift check, and is absent from the hosted artifact on any other device. Nothing records DELIVERY either: the order was placed 2026-07-20 and there is no arrival/inventory state anywhere, so a fresh session cannot tell whether the boxes are on the table. Same gap in build_plan: phases[*].tasks have no status key, so P0 task strings still read 'not yet run' / 'still open' while the phase summary says 'P0 SOFTWARE IS CLOSED'. Recommended schema addition before the build starts in earnest: a `status`/`done_date` per build_tab step and build_plan task (dashboard seeds from it rather than owning it), plus a `received` flag per ordered part. This is the highest-value schema change for the phase the project is now entering — it is the difference between the build being contract-tracked and chat-tracked, and the project's documented #1 failure mode is exactly that evaporation. Related and cheap: docs/placard_mount.md (67 KB, three ADR-lite decisions, NINE $0 pre-field bench gates including the GPS-transparency test, the 60-80 N frangible pull test, target-side prop clearance at ~19 mm from two prop discs, and hover VIBE) is referenced NOWHERE in project_state.json — 'placard_mount', 'frangible', 'anemometer' all return False. Nine bench gates and ten of its own logged contradictions sit entirely outside the compass a fresh session orients from.

### [medium] ALSO NOT DEAD, BUT CORRECTLY GATED — items that look like slack and are not; do not spend effort re-deciding them.

Listing these so the parent does not double-count them as drift or schedule work against them. (1) Three external-verdict constraints are genuinely unresolved and ALL resolve in the tripod session: transfer-bet (smoke read says NOT-DEAD but per-frame discrimination is ~zero; the deciding variable is the 5-streak handoff gate on real clutter, measurable only by curve (b)); pi5-emulation-gap / pi5-compute (real AprilTag and CPU-YOLO fps cannot be pre-measured — QEMU proved the stack loads, not the throughput; this is why the Pi 5 is a Tier-1 buy); wide-fov (118 deg paper-verified, the delivered adjustable lens must be re-measured at calibration, skr-06). Correctly sequenced, not neglected. (2) Curve (b)'s far band (protocol §8.2, 10-25 m) is unscoreable because tripod_score.curve_b builds its truth box from the TAG's recovered pose, so past the decode ceiling the NN read degenerates to a fire-rate, not recall. The tool now fails honestly (curve_b_band_coverage + section_8_2_answerable:false), so it is NOT a correctness risk. But it gates only the DEFERRED Hailo/markerless door, not the $740 order — so the decision to make before the day is cheap re-planning (fly the NN crossing passes closer in) rather than building log-derived truth boxes. Decide it, do not build it. (3) Launch-mechanism bench L5 (bearing-latch 1-sigma against the <<3 deg budget), L6 (trigger latency, which decides whether the bearing latch is mandatory) and L7 (print the launch plate) appear in NO build_tab rung and no contract step — L5 needs the same unbought tripod so it should ride that purchase; L6 needs only TX+FC+Pi. The §3 aim-error budget — which decides whether the launch mechanism works at all, and aim is the project's dominant PROVEN lever (ADR-0083, 0.71 -> 0.43 m) — stays unmeasured until they run. Add them to the ladder in the rank-7 sweep. (4) One number to correct while sweeping: ADR-0083's headline 'r2l 0.33 m = INSIDE the ~0.36 m ram envelope' is seed-123-only; recomputed from the committed CSVs it is 0.3315 m on seed 123, 0.355 m pooled across 123+777, and 0.395 m on the disjoint seed — i.e. outside the envelope on the replication seed and a coin flip pooled. It is repeated verbatim in two contract notes and interacts directly with the unratified 0.36 m bar.

### Top recommendations

- (a) TODAY AT THE DESK — the single highest-leverage thing: plumb `--quad-decimate` through pi_capture -> tripod_score -> autolabel_from_apriltag and STAMP IT INTO meta.json, and in the same turn sweep configs/target_kakute/gen_tripod_mission.py's defaults (8/17/30 -> the corrected 4-20 m contiguous grid with a 5-6 m tag-envelope crossing standoff), add protocol-conformance asserts to its --self-test, and wire configs/target_kakute/selftest.sh into scripts/run_tests.sh. RATIONALE: these are the two ways the single field day can be flown exactly by the book and still return a verdict the $740 money gate cannot honestly consume — one captures in the config ADR-0082 predicts FAILS, the other flies stations outside the predicted 7.10 m decode envelope and manufactures a false NO-GO. Both are pure desk work with zero parts dependency. IF ONLY ONE: quad_decimate. The mission geometry has a human workaround (fly the stations by hand off the protocol); the decimation value has none, and because it is never recorded the frames cannot be re-scored after the day to recover the lever.
- (b) THE DAY PARTS ARRIVE — the single highest-leverage thing: bench the real Pi 5 for SUSTAINED AprilTag decode fps through a thermal soak at the flying quad_decimate (build_tab skr-07), before anything else touches the bench. RATIONALE: it is the money gate's divisor, the verdict flips across ~24 fps, tripod_score correctly refuses to invent it (returns UNCERTAIN — the fail-closed fix that followed the invented-30-fps defect), it is measurable ONLY on real hardware, and measuring it after the field day cannot rescue a session captured at the wrong rate. Immediately second on that day: camera calibration (skr-06) — every range and bearing number in the campaign is wrong until it runs — which is precisely why the ~$8 rigid checkerboard must be bought TODAY alongside the parts order.
- ORDER THE BLOCKING PARTS TODAY, in parallel with the desk work — shipping latency is the only irreducible delay. The Kakute microSD is the one that matters most: no card = no DataFlash .BIN = no target track = range_truth_join has nothing to join = every curve returns UNCERTAIN and the whole day is unscoreable. Then tripod + ball head + inclinometer, >=27 W PD power bank, Pi 5 active cooler, checkerboard, LiPo bag/glasses/blanket, buzzer, cones + tape measure, and a ~$20 handheld anemometer (in no BOM tier at all, and without it protocol §8's wind validity limits are unenforceable).
- SPEND ONE 1-2 HOUR TURN ON THE CONTRACT SWEEP and treat it as gate work, not housekeeping. It is four commits stale (HEAD fad4c11 vs contract 0a41fd9), so the canonical surface does not know the tripod day can now produce a verdict — and it currently instructs a builder to HOLD THE PLACARD PRINT and to re-buy an owned soldering iron, publishes two refuted claims ('the camera defends imperfect aim', GL 'pending') on the hosted artifact, names the wrong deployed detector weights, and reads simultaneously 'FAA cleared' and 'FAA still open'. Republish the artifact in the same turn. This single edit closes roughly ten independent reader findings.
- BUILDER DECISIONS TO SURFACE, SPLIT INTO RATIFIABLE AND HELD: ratify the ram-radius GEOMETRY now (5-inch airframes -> ~0.36 m tip-to-tip; pure arithmetic, gates the ~$740 Tier-2 order, and every Pk number on every visible surface is currently quoted against a radius the hardware cannot deliver — sweep scripts/field_score.py:120 with it). HOLD the inference that follows from it ('the camera terminal is decoration'), because its supporting camera-arm batches were measured through the unfixed frozen_vworld zero-command defect. Also write up #16 (vertical channel / altitude datum — the question itself has never been written down anywhere) and #22 (post-GO OFFBOARD loss; the code points at an ADR-0081 addendum that does not exist and FAILSAFE 7 is unimplemented) as real decision records; docs/decisions.md still ends at ADR-0083.
- ON frozen_vworld, DIVERGE FROM THE READERS' P0 FRAMING: fix the latch (port flight/deploy/seeker_loop.py:311-315 into scripts/m4_intercept.py) and fix the AUDITOR (audit_per_tick check (c) skips zero-command rows, so it is anti-correlated with the defect and still prints 'PASS: N/N ... (a)/(b)/(c)' — add n_zerocmd counting, a third flight status, and a gating check (e) 'the terminal actually actuated'), then downgrade the affected contract verdicts to PENDING RE-FLY with a ledger entry the same turn. But DEFER the ~40-flight Gazebo re-fly: sim exploration is declared complete, the hardware executor is unaffected, and it buys a sim-only conclusion at the cost of a batch day. Reopen it only if the ram-radius ruling turns 'does the camera terminal earn a metre' back into a live purchasing question.
- STRIKE THE DEAD WEIGHT EXPLICITLY so it stops reading as pending work: the 'ten unverified review-2 findings' (the list was never written — three readers searched the repo and ~500 MB of transcripts; close it honestly or re-scope it to a pass over scripts/m4_intercept.py), the wedge-sizing contradiction's Gazebo resolution run (its premise, the accel cap, was rejected — this is now a 10-minute doc re-statement as 'wedge vs prop clearance on an uncapped dash', and the adjustable 10-30 deg bracket already hedges it), and all further T25 render time until a finish-vs-park decision is made. Defer the README/portfolio sweep until after the field results land so it is done once, not twice.
- WRITE THE KILL-DAY PROTOCOL while the tripod protocol's lessons are fresh — it is sim-free, it can proceed in parallel with everything above, and the binary kill is the project's stated success criterion yet has four task bullets against the tripod day's 46 KB. Include pre-registered success/abort criteria, safety geometry, a pass card, a clock-sync procedure, and a mapping table from each of the 20 TODO-BUILDER constants in flight/deploy/real_flight.py to the bench step that retires it. Note that no script currently drives real_flight.py against a real FC at all — brn-05 and check_deploy_bench.sh both exercise the older seeker_loop.py path.